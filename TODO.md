# TODO.md — FamilyFinance PRO

מבוסס על מה שניתן לזהות מהקוד, מה-README, ומהמצב הנוכחי בלבד. אין רשומות מומצאות.

## דחוף
- **Version 1.0 נסגרה (31/07/2026, שלב 4.8)** — `index.html` הראשי הוא כעת ארכיטקטורת Preview v2 המלאה (Cockpit) עם מפתחות ה-localStorage האמיתיים, לאחר Cutover מלא (שלבים 4.4–4.8, ראו למטה). Rollback זמין דרך Tag `pre-preview-v2-cutover`.
- **Version 1.1 בתהליך (החל 01/08/2026, ישירות על `index.html`)** — ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.1", לפירוט מלא של כל שלב. סיכום מצב:
  1. ~~אריחי קטגוריה ניתנים לסידור מחדש במסך הבית~~ — הושלם (01/08/2026, commit `1041092`).
  2. ~~עמודי קטגוריה + זרימות הוספה ספציפיות לסוג~~ — הושלם (01/08/2026, commit `e7a0465`, אחרי גרסת ביניים ב-`8e8be7d`).
  3. ~~אריחי סיכום חדשים במסך הבית (יתרת תשלומים שונים/הלוואות)~~ — הושלם (01/08/2026, commit `a683a8e`).
  4. ~~כותרת מסך תנועות מציגה שם קטגוריה~~ — הושלם (01/08/2026, commit `a107247`).
  5. ~~שלב 4.0.2 — ימי כניסה/ירידה ברירת מחדל לקטגוריה~~ — הושלם (02/08/2026, commit `223c730`).
  6. ~~שלב 4.0.2.1 — פתיחת עריכת תנועה בלחיצה על השורה~~ — הושלם (02/08/2026, commit `b74ccdc`).
  7. ~~שלב 4.0.2.2 — ארכוב אוטומטי להתחייבויות שהסתיימו~~ — הושלם (02/08/2026, commit `197535f`).
  8. ~~שלב 4.0.3 — הפרדת "קטגוריות" מ"הגדרות" ובניית מסך הגדרות מלא~~ — הושלם, אומת (45/45+58/58+9/9+רגרסיה מלאה), נשמר ונדחף (03/08/2026, commit `251b3e3`).
  - **הוחלף ע"י Version 1.2 (31/08/2026)**: הבדיקה הידנית הממתינה כאן התייחסה למסך ה-5-טאבים של שלב 4.0.3, שכבר הוחלף ע"י מבנה הניווט/הגדרות של Version 1.2 (4 טאבים, ראו למטה) — פריט זה אינו רלוונטי יותר כפעולה פתוחה.
  - **הערה מתודולוגית**: שלבים 1–4 לעיל (01/08/2026) שוחזרו בתיעוד ב-03/08/2026 מניתוח Git ישיר — לא תועדו בזמן אמת. ראו הערה מקבילה ב-CURRENT_STATUS.md/CHANGELOG.md.
- **Version 1.2 — UI/UX Refinement: ניווט ותפריטי הגדרות (03/08/2026, ישירות על `index.html`)** — ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.2", לפירוט מלא.
  1. ~~שלב A — `showScreen()` + `SCREEN_NAV_ALIAS`~~ — הושלם, אומת (6/6 בדיקות).
  2. ~~שלב B — ביטול טאב "תנועות", שורת "📋 כל התנועות" בקטגוריות~~ — הושלם, אומת (22/22 בדיקות).
  3. ~~שלב C — פיצול מסך הגדרות לתפריט-נושאים + מסכי-משנה~~ — הושלם, אומת (57/57 בדיקות; כולל תיקון באג hoisting אמיתי שנמצא ותוקן באותו שלב — ראו CHANGELOG.md).
  4. ~~שלב D — רגרסיה משולבת (תנועות+הגדרות ביחד, כולל PIN lock מכל מסך)~~ — הושלם, אומת (14/14 בדיקות).
  - **סה"כ 99/99 בדיקות VM/Node עברו** (על ה-`<script>` האמיתי מהקובץ, לא עותק). `APP_VERSION` עודכן ל-`'1.2.0 Preview'`.
  - **הושלם ואושר (31/08/2026, תיקון תיעודי)**: נשמר ב-commit `3f232c787fde75f40b630e9fcac8756c9066f471`. Version 1.2 עברה בדיקת דפדפן אמיתית ב-Desktop וב-Mobile ואושרה במפורש ע"י המשתמש. הניסוח הקודם כאן ("ממתין לאישור/Commit") היה שגוי — התיעוד עצמו נכלל באותו commit.
- ~~**Version 1.3.0 — מנוע תזרים מזומנים מאוחד + עוגן יתרה + מסך תובנות חדש (01/09/2026, ישירות על `index.html`)**~~ — **הושלם במלואו, שוחרר ל-Production**: `v1.3.0` (commit `ee6701ae276c0fba668c7c23a345c528e64326ab`), אומת ידנית ב-Production כ-LIVE. ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.3", לפירוט מלא של כל 5 השערים (Phase 2A–2E).
  1. ~~Phase 2A — Discovery: מיפוי מודל פיננסי + חוזה מוצע~~ — הושלם, ללא שינוי קוד.
  2. ~~Phase 2B — מנוע תזרים מזומנים מאוחד (`generateCashflowEvents`/`buildCashflowSummary`)~~ — הושלם, אומת (40+ בדיקות VM/Node, שני סבבי שער כולל תיקון גבול "היום").
  3. ~~Phase 2C — חיבור ל-UI + מסך Insights חדש (מודל `currentBalance` ראשוני)~~ — הושלם, אומת (שני סבבי בדיקה מקבילים + שער תיקון סמנטי, כולל Desktop+Mobile Edge/CDP אמיתי, אפס שגיאות קונסולה). **הוחלף ב-Phase 2E** — ראו סעיף 5 למטה.
  4. ~~Phase 2D — תיעוד וסגירה~~ — הושלם (מסמך זה + CURRENT_STATUS.md/CHANGELOG.md/PROJECT_CONTEXT.md).
  5. ~~Phase 2E — עוגן יתרה (Balance Anchor), מחליף את מודל ה-`currentBalance` הידני מ-Phase 2C~~ — **הושלם, אומת, זהו המודל הסופי המאושר**. `anchorBalance`/`anchorDate` חדשים ב-`family_finance_settings`; "יתרה משוערת להיום" מחושבת אוטומטית (לא מוזנת ידנית); פישוב דרך "עדכן יתרה בפועל"; `currentBalance` הישן נשאר כ-fallback קריאה-בלבד לתאימות לאחור. אומת: 11/11 חישוב, 8/8 התמדה/תאימות, 6/6 מקור-אמת, שני סבבי Edge/CDP (כולל תיקון באג collapse-אחרי-רענון שנמצא ותוקן). אפס שגיאות קונסולה.
  - `APP_VERSION` עודכן `'1.2.0 Preview'` → `'1.3.0 Preview'` (ללא שינוי נוסף ב-Phase 2E).
  - **החלטת סמנטיקה מאושרת**: מסך הבית (`hero-amount`/`snapshot-income`/`snapshot-expenses`) **נשאר ללא שינוי במכוון** — "תמונת מצב חודשית" הוא מושג שונה מהותית מתחזית התזרים המעוגנת-יתרה של מסך תובנות. כרטיס "אזהרת תחזית" הישן הוסר מהבית לחלוטין; מסך תובנות הוא הבעלים הבלעדי של אזהרות תחזית קדימה מעתה.
  - **מפת דרכים מאושרת**: ~~אישור Commit מפורש~~ ← ~~Push~~ ← ~~Production Release~~ — **הושלם (01/09/2026)**. `APP_VERSION` עודכן `'1.3.0 Preview'` → `'1.3.0'` (הכנת שחרור) ← מוזג ל-`main` ← Production LIVE, מתויג `v1.3.0`.
- **Version 1.3.1 — Patch: שיפורי UI בקטגוריות + תיקון שורש-בעיה בשחזור גיבוי (01/09/2026, ישירות על `index.html`)** — **הושלם, אומת. תיקון עובדתי (02/09/2026, Milestone 7.1): כבר הייתה חלק מ-`origin/main`/Production (commits `bb6d8cd`/`3b93d86`) לפני תחילת השיחה הנוכחית — לא נשמרה יחד עם Milestones 3–6 באותו commit, כפי שנטען כאן קודם בטעות.** `APP_VERSION` עודכן `'1.3.0'` → `'1.3.1'`. ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.3.1".
  1. ~~כרטיס מכווץ להלוואות: תשלום X/Y + יתרה Z~~ — הושלם, אומת (VM equivalence + Edge/CDP Desktop 1280/Mobile 390/360).
  2. ~~אותו כרטיס ל-תשלומים שונים (variable, tracking-only, ללא שינוי בזרם אירועי תזרים)~~ — הושלם, אומת.
  3. ~~קריסת פריט קטגוריה מורחב בעת עזיבת הקטגוריה~~ — הושלם, אומת (הלוואות/משתנה/קבוע, מעבר קטגוריה-לקטגוריה, יציאה לבית/תובנות/הגדרות וחזרה).
  4. ~~תיקון שורש-בעיה בשחזור גיבוי (family_finance_loan_balance_view לא-JSON, isValidBackupShape נכשל)~~ — הושלם, אומת. saveLoanBalanceView/loadLoanBalanceView תואמים-לאחור; isValidBackupShape עם חריג צר למפתח הזה בלבד.
  5. ~~הקשחת בטיחות שחזור: best-effort transactional restore + compensating rollback עם snapshot וסדר-עדיפות~~ — הושלם, אומת (כשל-כתיבה-חלקי + rollback מוצלח, וגם תרחיש כשל-rollback-עצמו — שתי הודעות נפרדות ומדויקות, ללא הצלחה כוזבת).
  - **מפת דרכים**: ~~אישור Commit מפורש~~ **הושלם (02/09/2026)** ← Push (ממתין לאישור) ← Production Release (ממתין לאישור).
- **Milestones 3–6 — מסך תחזית (Forecast) + מערכת יעדי חיסכון (Goals) + תזכורת חודשית + Backup schemaVersion 2 (02/09/2026, ישירות על `index.html`, על גבי Version 1.3.1 שכבר הייתה ב-`origin/main`, נשמר ב-commit `4fc1b81d382b7e5a1024abdbf8d8e181ff1ea005`)** — **הושלם, אומת בהיקף רחב (מעל 200 assertions, Edge headless אמיתי), commit מקומי בלבד, טרם push**. ראו CURRENT_STATUS.md, סעיף "Milestones 3–6", לפירוט המלא. סיכום:
  1. ~~Milestone 3 — מסך "תחזית" (📈, מחליף בתוכן את "תובנות" באותו `screen-insights`): 30/60/90 יום עם cutoff מדויק, תחזית 6-חודשים מכווצת ("תחזית חודשית מורחבת"), הכל מבוסס `buildCashflowSummary()`/מנוע התזרים המאוחד בלבד~~ — הושלם, אומת.
  2. ~~Milestone 4 — מסך "יעדים" (Goals): יעד חיסכון עם רכיבים (components) בעלי מועד-יעד עצמאי, הקצאה earliest-deadline-first, Backup schemaVersion 2 (`family_finance_goals` תמיד כלול בייצוא V2, שחזור V1 משמר יעדים כברירת מחדל), הגנת שלמות-נתונים all-or-nothing~~ — הושלם, אומת.
  3. ~~Milestone 5 — תזכורת חיסכון חודשית: נפתחת אוטומטית ב/אחרי ה-2 לחודש, דחייה (`postponeGoalsReminder`)/Escape session-only per-goal/per-period (**לא** גלובלי — תוקן ב-Milestone 6 audit), ledger `confirmedTransfers` (אישור מלא/הקצאה מותאמת-אישית), guard נגד כתיבה כפולה~~ — הושלם, אומת.
  4. ~~Milestone 6 — ביקורת אינטגרטיבית + תיקונים: overflow 35px/5px ב-360/390px (מסך בית, grid `min-width:0`), suppression גלובלי→per-goal/per-period, כפתור דחייה 36px→44px, קוד מת (`computeForecast`+3 עוטפות) הוסר לאחר אימות call-graph מלא, ולידציית Goals strict אמיתית (לא coercion), תיקון ניגודיות WCAG (`--color-text-muted` light)~~ — הושלם, אומת.
  - **מפת דרכים**: Commit מקומי **הושלם (02/09/2026)** ← Push (ממתין לאישור) ← Production Release (ממתין לאישור).
  - **השלב הבא (מודולריזציה) בוצע — ראו Milestones 8–9 למטה.**
- **Milestones 8–9 — מודולריזציה: פיצול `index.html` ל-3 קבצים (HTML/CSS/JS), אימות מלא, ותיעוד (03/09/2026, ישירות על `index.html`, על גבי Milestones 3–6)** — **הושלם, אומת, commit מקומי בלבד, טרם push**. `index.html` הפך למבנה+markup בלבד (328 שורות); ה-CSS המלא הועתק במדויק אל `styles.css` הקיים (שהוחלף במלואו — הגרסה הישנה שם הייתה עותק מיושן/שגוי, ראו למטה); ה-JavaScript המלא הועתק במדויק אל `app.js` הקיים (הוחלף במלואו, אותה סיבה); `index.html` כעת טוען אותם דרך `<link rel="stylesheet" href="styles.css">`/`<script src="app.js"></script>` (נתיבים **יחסיים** — נבחר במפורש כדי לשמר את היכולת לפתוח את `index.html` ישירות דרך `file://` ללא שרת; נתיב מוחלט-משורש שנוסה תחילה הוכח שובר את היכולת הזו ותוקן, ראו CURRENT_STATUS.md) — ללא bundler/module/תלות חיצונית חדשה, האפליקציה נשארת SPA יחיד. **אימות שלמות**: כל 289 הצהרות ה-`function` הועברו במדויק (289/289 תואמות, אפס נשאר ב-`index.html`), כל 254 חוקי ה-CSS (ספירת `{` בבלוק הסגנון בלבד) הועברו במדויק, כל 7 מפתחות `family_finance_*` זהים, כל 12 מזהי `screen-*`/`nav-*` זהים, מדגם רחב של שמות handler (`onclick=`/`onkeydown=`) נבדק בנפרד ותאם 1:1. **אימות דפדפן אמיתי (Edge headless, פרופיל זמני מבודד)**: `index.html`/`styles.css`/`app.js`/`manifest.webmanifest` נטענים 200 עם MIME נכון; אפס שגיאות Console/Page; כל 5 מסכי הניווט (בית/תובנות/יעדים/קטגוריות/הגדרות) נלחצים ללא שגיאה; אפס גלישה אופקית ב-360/390/768/1280px; ערכות light/dark מוחלות נכון; מסך יעדים מוצג ללא שגיאה; פונקציית `collectAppLocalStorageBackup()` נגישה ומתנהגת כמתועד בקוד עצמו. **השוואת Baseline**: אותם נתוני-סימון בדיוק על גבי הגרסה שלפני הפיצול (`git show HEAD` הקודם, קובץ יחיד) הפיקו פלט זהה-ביט (hero-amount/goalCardPresent) — מוכיח אפס סטייה התנהגותית מהפיצול עצמו. אפס שינוי בלוגיקה/פונקציות/חישובים; `APP_VERSION`/`schemaVersion`/Service Worker/מבנה 5-המסכים — ללא שינוי. אימות מכשיר פיזי ו-Production **לא בוצע** (ראו CURRENT_STATUS.md).
  - **מפת דרכים**: Commit מקומי **הושלם (03/09/2026)** ← Push (ממתין לאישור) ← Production Release (ממתין לאישור).
- **Version 1.4.0 — שחרור: Forecast + Goals + תזכורת חודשית + Backup schemaVersion 2 + מודולריזציה + תיקוני נתיב GitHub Pages (03/09/2026, ישירות על `index.html`)** — מאחד לשחרור אחד את Milestones 3–6 (תחזית/יעדים/תזכורת/schemaVersion 2) ו-8–9 (מודולריזציה ל-3 קבצים), בתוספת שני תיקונים שאושרו במפורש לפני ה-push: (1) טקסט תת-הכותרת בראש האפליקציה (`Cockpit — Preview v2 · נתוני Preview`, שריד היסטורי מלפני Cutover, כבר היה חי ב-Production) הוחלף ל-"ניהול תקציב משפחתי"; (2) `index.html`/`manifest.webmanifest` תוקנו מנתיבים מוחלטים-משורש (`/manifest.webmanifest`, `/icons/...`, `start_url`/`scope`/`icons[].src` של המניפסט) לנתיבים **יחסיים** — אומת שנתיב מוחלט שובר את הפעלת ה-PWA תחת GitHub Pages (`/finance/` subpath, אתר ציבורי נפרד שהתגלה פעיל באותו push), בעוד נתיב יחסי עובד נכון גם שם וגם ב-Vercel (root). `APP_VERSION`: `'1.3.1'` → `'1.4.0'`. `schemaVersion` נשאר `2`. ראו CURRENT_STATUS.md, סעיף "Version 1.4.0", לפירוט האימות המלא (כולל אימות כפול root+subpath).
  - **מפת דרכים**: Commit מקומי + Push — ראו CURRENT_STATUS.md לתוצאות האימות המדויקות (Vercel מוגן ב-SSO, נבדק דרך GitHub Deployment API בלבד; GitHub Pages נבדק בדפדפן אמיתי).
- **שלב 3ব — הוספת ניהול קטגוריות + תחזית ל-Preview v2 (הוחלט 31/07/2026)** — 5 תתי-שלבים, אחד בכל פעם עם עצירה לאישור. **העבודה כולה בתוך `index-preview-v2.html` בלבד; `index.html`/`styles.css`/`app.js` לא ישתנו; כתיבה רק ל-`PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY`.**
  1. ~~**3ב.1 — לוגיקת ניהול קטגוריות (ללא UI)**~~ — **הושלם ומאומת, נשמר ב-commit `d67b1db` (31/07/2026)**: נוספו `addPreviewCategory`/`renamePreviewCategory`/`deletePreviewCategory` וכלי עזר (`isBuiltInPreviewCategoryKey`, `categoryHasPreviewItems`, `getAllowedPreviewCustomCategoryBaseTypes` ועוד) — לוגיקה בלבד, ללא UI/HTML/CSS/event listeners. כולל תיקון שני פערי הגנה שנמצאו בסקירת קוד: ולידציית `baseType` מול `PREVIEW_CUSTOM_CATEGORY_TYPE_OPTIONS`, ומניעת התנגשות מפתחות `custom_<timestamp>` (suffix מספרי). **108/108 בדיקות VM/Node עברו.** ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  2. ~~**3ב.2 — UI ניהול קטגוריות**~~ (טפסים בתוך הממשק, ללא `prompt()` ילידי) — **הושלם ומאומת, נשמר ב-commit `fb05be8`**.
     - **כללי עריכה/מחיקה שאושרו (31/07/2026)**: קטגוריות מובנות — ניתן לשנות שם, לא ניתן לשנות `baseType`, לא ניתן למחוק. קטגוריות מותאמות אישית — ניתן לשנות שם, שינוי `baseType` אינו בהיקף 3ב.2, ניתן למחוק רק כשאין תנועות פעילות ואין תנועות בארכיון.
     - **כללי UI שאושרו**: כפתור עריכה מוצג לכל הקטגוריות; כפתור מחיקה מוצג רק למותאמות אישית; אין `prompt()`/`confirm()`/טפסים ילידיים; הוספה ועריכה בטפסים inline בתוך מסך ההגדרות.
     - ראו CURRENT_STATUS.md ("שלב 3ב.2 — החלטות מוצר") לפירוט המלא.
  3. ~~**3ב.3 — לוגיקת התחזית**~~ — **הושלם ללא שינוי קוד (31/07/2026)**: נמצא שהלוגיקה (`computeForecast`/`getBillingRange`/`isBillingActiveInMonth` ופונקציות הנרטיב הבנויות עליהן) כבר הייתה קיימת, זהה ל-`index.html` (אומת בהשוואה אוטומטית), וכבר מחוברת בפועל למסך התובנות. נוספה סוויטת בדיקות ייעודית (זמנית, לא בפרויקט) — **38/38 עברו**; סוויטות 3ב.1/3ב.2 הורצו מחדש ללא רגרסיה. ראו CURRENT_STATUS.md ("שלב 3ב.3").
  4. ~~**3ב.4 — שילוב התחזית במסך התובנות (Insights)**~~ — **הושלם, אומת ונשמר ב-commit `eeed2d0` (31/07/2026)**: גרף+טבלת תחזית (6 חודשים) נוספו למסך Insights, תמיד גלויים (ללא כפתור קיפול, החלטת מוצר מאושרת), מבוססים על `computeForecast(items)` הקיים והמאומת בלבד. `niceRoundUp`/`barPath`/`attachForecastHandlers`/`renderForecast`/`toggleForecastView` הועתקו כלשונן מ-`index.html` עם namespace `preview-forecast-*` נפרד. 34 בדיקות VM/Node עברו. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  5. ~~**3ב.5 — Regression מלא**~~ — **הושלם, אומת ונשמר ב-commits `46ad2eb`+`053a42b` (31/07/2026)**: 295 בדיקות VM/Node (3ב.1–3ב.4, ללא רגרסיה) + 52 בדיקות Edge/CDP אמיתיות (ניהול קטגוריות — הוספה/שינוי שם/חסימת מחיקה/מחיקה מוצלחת; Forecast בגרף+טבלה כולל עדכון אחרי שינוי נתונים אמיתי; שאר מסכי Preview v2; בידוד `PREVIEW_*` מלא) — **347/347 עברו** לאחר תיקון באג אמיתי שנמצא באמצע הסבב (ראו למטה). `index.html`/`app.js`/`styles.css` אומתו כלא-שונים (`git diff --stat` ריק עבורם).
  - **תיקון באג שנמצא ותוקן בשלב 3ב.5 (31/07/2026)**: תצוגת יתרה שלילית בתחזית (טולטיפ הגרף + תא בטבלה) הציגה `₪-1,234` במקום `-₪1,234` — אותה תבנית שגויה (`'₪' + Math.round(net).toLocaleString()`) שתוקנה בעבר במקומות אחרים בקובץ זה (שלב ו׳.1) דרך `formatHomeCurrency()`, אך לא הוחלה על שני מקומות התחזית שנוספו בשלב 3ব.4 (הועתקו כלשונם מ-`index.html`, שם הבאג עדיין קיים ולא תוקן — ראו "פער ידוע ב-index.html" למטה). תוקן ע"י שימוש ב-`formatHomeCurrency()` הקיים בשני המקומות — שינוי תצוגה בלבד, ללא נגיעה בחישוב/בלוגיקה העסקית. `index.html` לא נגע בו.
  - **כללי מחיקת קטגוריות שאושרו**: קטגוריות מובנות אינן ניתנות למחיקה; קטגוריה מותאמת אישית עם תנועות (כולל בארכיון) אינה ניתנת למחיקה — הודעה ברורה במקום; לעולם לא נמחקות תנועות אוטומטית.
  - ראו CURRENT_STATUS.md ("שלב 3ב") לפירוט ההחלטות המלא.
  - **שלב 3ב כולו הושלם.** נקודת ההמשך הבאה (שילוב Preview v2 במערכת הראשית, שלב 3 שנמפה קודם) טרם אושרה.
- **שלב 3 — מיפוי שילוב Preview v2 במערכת הראשית (הושלם, מיפוי בלבד, 31/07/2026)**: נמצא ש-`app.js` מכיל לוגיקה עסקית ישנה/שגויה ביחס ל-`index.html` — לא לשמש כמקור להעתקה. שתי אלטרנטיבות שילוב הוצגו (inline יחיד / 3 קבצים אמיתיים) — **טרם נבחרה**, ממתין להחלטה עתידית (לאחר רצף 3ב).
- **שלב 4 — שילוב Preview v2 במערכת הראשית בפועל (מיפוי מדויק אושר 31/07/2026)** — תוכנית מפורטת בת 7 תתי-שלבים (4.2–4.8), אחד בכל פעם עם עצירה לאישור. **אסטרטגיה A (Cutover, מפוצל לתתי-שלבים) הומלצה — טרם אושרה סופית. אין להחליף את `index.html`; אין לכתוב ל-`family_finance_data`/`family_finance_cat_config` מ-Preview v2 עד להחלטה מפורשת.**
  - ~~**4.1 — מיפוי ותכנון שילוב**~~ — **הושלם, ללא שינוי קוד (31/07/2026)**: מיפוי השוואתי מלא ב-10 אזורים; אומת ב-diff ישיר ש-`app.js` הוא תמונת מצב ישנה/שגויה (אסור כמקור העתקה); `styles.css` כמעט תואם; לוגיקת Forecast זהה ביט-לביט; כלל מחיקת קטגוריה שונה במכוון בין הקבצים. 3 אסטרטגיות דורגו — A (Cutover) הומלצה. ראו CURRENT_STATUS.md ("שלב 4.1").
  - ~~**4.2 — הגנות תאימות לנתונים ישנים**~~ — **הושלם, ללא צורך בשינוי קוד (31/07/2026)**: מיפוי שדות חסרים אפשריים (`cardLast4`, `where` ישן, `notes`/`originalAmount`/`interest`/`day`/`total`/`start` חסרים, `categoryConfig` חלקי, `displayCategory` תלוי, JSON פגום). נמצא שדפוסי ההגנה כבר קיימים בקוד (בנויים הדרגתית בשלבים ד׳.2/E.1/E.2/G.2–G.5/3ব.1) — אין migration אוטומטי, אין כתיבה חזרה בזמן טעינה. **389/389 בדיקות עברו** (295 רגרסיה קיימת + 81 VM/Node חדשות לנתונים ישנים + 13 Edge/CDP). `REAL_DATA_KEY`/`REAL_CONFIG_KEY` ללא שינוי, אפס כתיבות. `index-preview-v2.html`/`index.html`/`app.js`/`styles.css` לא שונו. ראו CURRENT_STATUS.md ("שלב 4.2").
  - ~~**4.3 — גיבוי נתוני משתמש אמיתי מתועד**~~ — **הושלם כ"לא נדרש במצב הנוכחי" (31/07/2026)**: נבדק ונמצא שאין כרגע נתונים אמיתיים תחת `family_finance_data`/`family_finance_cat_config` במחשב זה (רק מפתחות Preview קיימים). בטלפון קיימים נתוני Preview שהמשתמש אישר שאינם חשובים ואין צורך לגבותם. **לא נוצר קובץ גיבוי** — אין מה לגבות. לא בוצעה כתיבה/מחיקה/שינוי ב-`localStorage`, לא בוצע שינוי קוד. **תזכורת לעתיד**: אם יתווספו נתונים אמיתיים לפני ה-cutover בפועל (4.6–4.8), יש לחזור על שלב הגיבוי מחדש. ראו CURRENT_STATUS.md ("שלב 4.3").
  - ~~**4.4 — קובץ מועמד עם מפתחות אמיתיים**~~ — **הושלם, נשמר ב-commit `72a315a` (31/07/2026)**: `index-v3-candidate.html` נוצר, מכוון ל-`family_finance_data`/`family_finance_cat_config`, מנגנון seed/איפוס הוסר לחלוטין. 359/359 בדיקות עברו.
  - ~~**4.5 — אימות מלא לפני Decision Gate**~~ — **הושלם, ללא שינוי קוד (31/07/2026)**: 432 בדיקות (כולל round-trip integrity ושימור שדות legacy). לא נמצא באג באפליקציה.
  - ~~**4.6 — Decision Gate מוצרי**~~ — **הושלם (31/07/2026)**: אושרו חסימת מחיקת קטגוריה עם תנועות (כולל בארכיון), ואי-מחיקת קבצים ישנים ב-Cutover.
  - ~~**4.7 — Cutover בפועל**~~ — **הושלם, נשמר ב-commit `c63a43e` (31/07/2026)**: `index.html` הוחלף בתוכן `index-v3-candidate.html`; `index-v3-candidate.html` אינו קיים יותר; Tag `pre-preview-v2-cutover`→`72a315a` נוצר כמנגנון Rollback (ללא קובץ legacy נוסף, לפי בקשת המשתמש). 432/432 בדיקות עברו מול ה-`index.html` החדש.
  - ~~**4.8 — אימות סופי, תיעוד, וסגירת Version 1.0**~~ — **הושלם (31/07/2026)**: מצב Git אומת, 432/432 בדיקות עברו שוב, קבצים מוגנים ומבנה סופי אומתו, תיעוד עודכן. **Version 1.0 נסגרה.**
  - **כל רצף 4.4–4.8 (Cutover) הושלם. `index.html` הראשי הוא כעת ארכיטקטורת Preview v2 המלאה עם מפתחות אמיתיים.**
- **מימוש "Preview v2" (Cockpit) לפי התוכנית המאושרת (29/07/2026) — 5 שלבים** — שלב אחד בכל פעם, עם עצירה לאישור לפני המעבר הבא. **העבודה כולה מתבצעת אך ורק בתוך `index-preview-v2.html`; המערכת הפעילה (`index.html`, `styles.css`, `app.js`) עדיין לא שונתה כלל.**
  1. ~~**שלב א׳ — קובץ ואסטרטגיית בטיחות נתונים**~~ — **הושלם ומאומת, נשמר ב-commit (29/07/2026)**: `index-preview-v2.html` נוצר עם מפתחות localStorage נפרדים (`family_finance_preview_v2_data`/`_cat_config`), seed חד-פעמי, וכפתור איפוס. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  2. ~~**שלב ב׳ — מבנה+CSS חדש עם נתוני Mock**~~ — **הושלם ומאומת (29/07/2026)**: HTML+CSS חדש לגמרי לפי ה-Wireframe שאושר (בית/Cockpit, תנועות, תובנות, הגדרות + ניווט תחתון + Quick Actions), עם נתוני דמה קבועים בקוד בלבד (`MOCK_DATA`), **בלי** התחלה מ-`index.html` הקיים, כולל סבב ליטוש חזותי קצר. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  3. ~~**שלב ג׳ — שכבת "מנוע הסיפור" התוספתית**~~ — **הושלם ומאומת (30/07/2026)**: נוספו `mockItems`/`mockCategoryConfig`/`mockSnapshot`, 7 פונקציות מלאות (`getFixedCreditCardTotals`, `getFixedBankVsCreditSplit`, `getTotalFixedCommitments`, `getMonthSnapshot`, `getRecentActivity`, `getLoansRemainingSummary`, `buildNarrative`) ו-4 Stubs (`getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming`, `getForecastWarning`, `getDatedMonthOverMonthChange`) שמחזירים `null`. `getMonthSnapshot` היא עטיפה בלבד סביב `mockSnapshot`, ללא חישוב עסקי. 38 בדיקות Node עברו. אין עדיין חיבור ל-UI/localStorage/נתונים אמיתיים/לוגיקה עסקית קיימת. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  4. ~~**שלב ד׳ — חיבור בפועל ללוגיקה הקיימת**~~ — **הושלם, אומת ונשמר ב-commit `136200d` (30/07/2026)**: כל 7 תתי-השלבים (ד׳.1–ד׳.7) בוצעו בתוך `index-preview-v2.html` — העתקת `getBillingRange`/`parseDatesAndGetLeft`/`computeForecast`/`isBillingActiveInMonth` כלשונן, קריאה בטוחה ממפתחות ה-Preview (`loadPreviewItems`/`loadPreviewCategoryConfig`, קריאה-בלבד), חיבור 6 הפונקציות משלב ג׳ + 4 ה-Stubs לנתונים אמיתיים, חיבור מסכי בית ותובנות, ותיקון באג audit (ערבוב `items` גלובלי מול פרמטרי). דוח בדיקות אינטגרציה (349 בדיקות, 342 עברו) הסתיים ב-PASS עם הערות ידועות בלבד. מנגנון `PREVIEW_*` נבדק ונשאר מבודד; `index.html` לא נגע בו. **הערה**: `addNewItem`/`saveInlineEdit`/`archiveItem`/`deletePermanently` (פעולות כתיבה) **לא** הועתקו/חוברו בשלב זה — נותרו לשלב עתידי, ראו סעיף 5. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  5. **שלב ה׳ — רצף השלמות למסכי Preview v2, בביצוע הדרגתי (מופה 30/07/2026)**: לפי מיפוי שאושר, שלב ה׳ פוצל לתתי-שלבים קטנים ונמוכי-סיכון:
     - ~~**ה׳.1 — חיבור מסך הגדרות (רשימת קטגוריות, תצוגה בלבד)**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: `category-list` מוצג מ-`categoryConfig` האמיתי (Preview, קריאה-בלבד) במקום `MOCK_DATA.categories`; 20 בדיקות Node/VM + בדיקת Edge headless עברו; אין לוגיקת כתיבה. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
     - ~~**ה׳.2 — חיבור מסך תנועות**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: `transactions-list` מוצג מ-`items` האמיתיים (Preview, קריאה-בלבד) במקום `MOCK_DATA.transactions`; מסנן פעיל/ארכיון הפך לפונקציונלי בפועל (לפי `item.isArchived`, זהה ל-`index.html`); מיון לפי `id` יורד; חיפוש נשאר מנוטרל; כולל תיקון תצוגת "Invalid Date" לתאריכים חסרים/ריקים/פגומים (`-` במקום זאת). 60 בדיקות Node/VM + בדיקת Edge headless עברו; אין לוגיקת כתיבה. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
     - ~~**ה׳.3 — חיבור "מה דורש תשומת לב" (attention-list)**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: מחובר אך ורק ל-`getBiggestUpcomingCharge(items)`/`getForecastWarning(items)` הקיימות משלב ד׳.6 (הוחלט מפורשות שלא לבנות לוגיקה עסקית חדשה ל"חיובים קרובים" מרובים, בניגוד ל-Mock) — רשימה מוגבלת במכוון ל-0–2 כרטיסים; מצב ריק כשאין אותות, ללא הודעת "הכל בסדר" מומצאת. 27 בדיקות Node/VM + בדיקת Edge headless אמיתית עברו; אין לוגיקת כתיבה. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
     - ~~**ה׳.4 — בדיקת דפדפן מלאה (Edge/CDP) על כל ה-Cockpit המחובר**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: חיבור CDP אמיתי (לא רק `--dump-dom`) עם קליקים אמיתיים על ניווט/פילטרים, 3 תרחישי נתונים (עשיר/ללא-אזהרה/ריק-מלא), **49/49 בדיקות עברו** — כל 4 המסכים מציגים נתונים אמיתיים, מסנן פעיל⇄ארכיון פונקציונלי בקליק אמיתי, אפס כתיבה למפתחות אמיתיים, אפס שגיאות Console, אפס באגים, אין שימוש שיורי ב-Mock. Preview v2 (read-only) מוכן טכנית להמשך פיתוח. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - **כל 4 תתי-שלבי ה׳ (ה׳.1–ה׳.4) הושלמו.**
   - ~~**ז׳.3 — מחיקה סופית מתוך תפריט ⋮**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: פריט "מחק לצמיתות" נוסף לתפריט ⋮ הקיים (מסך תנועות בלבד), עם `confirm()` פשוט של הדפדפן (ללא חלונית מותאמת אישית) לפי החלטת מוצר. `deletePreviewItem(id)`/`handleDeleteMenuAction(actionBtn)` חדשות, כותבות רק דרך `savePreviewItems()`/`renderAllPreviewScreens()` הקיימות. id חסר וביטול confirm הם no-op מלא. 24 בדיקות VM/Node + 20 בדיקות Edge/CDP אמיתיות (כולל טיפול אמיתי בדיאלוג confirm דרך CDP) עברו. אין נגיעה בנתונים אמיתיים/`categoryConfig`/`index.html`. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - ~~**ז׳.4 — עריכה Inline מתפריט ⋮**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: פריט "✏️ עריכה" נוסף לתפריט ⋮ הקיים (מסך תנועות בלבד), זמין רק לפריטים פעילים. לחיצה מחליפה את השורה עצמה בטופס Inline לפי 5 סוגי הפריטים, בהעתקה מילולית של הלוגיקה/הולידציה מ-`index.html` (`buildPreviewEditFormHtml`, `savePreviewInlineEdit`, `cancelPreviewEdit`, `handleEditMenuAction`, `togglePreviewCardLast4Field` חדשות). כשל ולידציה מציג `alert()` בלבד ואינו יוצא ממצב עריכה; ביטול הוא no-op מלא. כותבת רק דרך `savePreviewItems()`/`renderAllPreviewScreens()` הקיימות. 26 בדיקות VM/Node + 18 בדיקות Edge/CDP אמיתיות עברו. אין נגיעה בנתונים אמיתיים/`categoryConfig`/`index.html`. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - ~~**ז׳.5 — הוספת הכנסה/הוצאה/הלוואה מ-Quick Actions**~~ — **הושלם, אומת ונשמר ב-commit `4c46b58` (30/07/2026)**: 3 מסלולי הוספה נפרדים דרך חלונית ה-Quick Actions הקיימת (⋯ עד כה מוצגת בלבד), טופס באזור ייעודי חדש בראש מסך התנועות (`preview-add-form-area`), בורר סוג הוצאה (קבוע/משתנה/מתוארך — "מתוארך" מבוטל כשאין קטגוריה תואמת), רק טופס אחד פעיל בכל רגע (אכיפה דו-כיוונית מול עריכה Inline), ולוגיקה/ולידציות זהות ל-`addNewItem()` של `index.html` (`addPreviewItem`/`startPreviewAdd`/`chooseExpenseAddType`/`cancelPreviewAdd`/`buildPreviewAddFormHtml` חדשות). לאחר הוספה מוצלחת: איפוס state, מעבר למסנן "פעיל", ואז `renderAllPreviewScreens()`. 58 בדיקות VM/Node + 30 בדיקות Edge/CDP אמיתיות עברו. אין נגיעה בנתונים אמיתיים/`categoryConfig`/`index.html`. **כל 7 יכולות הכתיבה מהמיפוי המקורי (רצף ז׳) הושלמו.** ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  6. ~~**שלב ו׳ — סקירת עיצוב סופית (Design Review)**~~ — **הושלם (30/07/2026)**: ביקורת UX/UI מלאה, 10 ממצאים דורגו לפי חומרה/עלות (Critical: Quick Actions נראים פעילים אך אינם; Medium-High: bidi בסכומים שליליים; Medium: פורמט מספרים לא-עקבי, כותרת Mock מטעה, FAB חופף; High target-fit: אין גאוג׳ ויזואלי; Medium: status-pill סטטי). שלב סקירה בלבד, ללא שינוי קוד. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
  7. ~~**שלב ו׳.1 — תיקוני UX ממוקדים**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: תוקנו 5 הממצאים בעלות קטנה (Quick Actions מסומנים "(בקרוב)", תיקון RTL/Bidi ב-`.tx-amount`, אחידות פורמט מספרים בתובנות, כותרת "נתוני Preview" (לא Mock), ריווח FAB) + תיקון המשך ל-2 מקומות RTL/Bidi נוספים שהתגלו (`hero-amount`/`snapshot-balance`, `insight-value`) כולל תיקון באג אמיתי ב-`formatHomeCurrency()`. 44 בדיקות Node/VM + Edge/CDP עם צילומי מסך עברו. **Preview v2 (read-only) הגיע לרמת Release Candidate מבחינת UX/UI.** הוחלט במפורש שלא להוסיף פוליש נוסף (גאוג׳, status-pill דינמי) בשלב זה. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - ~~**מיפוי ותכנון יכולות כתיבה**~~ — **הושלם (30/07/2026)**: דוח מיפוי מלא (מפת קבצים/נתונים/פונקציות, מצב כל אחת מ-7 היכולות, פערים וסיכונים, תוכנית מימוש בת 8 תת-שלבים) — ללא קוד. אושר.
   - ~~**שלב ז׳.1 — תשתית כתיבה בסיסית ל-Preview v2**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: נוספו `savePreviewItems()`, `savePreviewCategoryConfig()`, `getCurrentTxFilterName()`, `renderAllPreviewScreens()` ב-`index-preview-v2.html` בלבד. כותבות רק למפתחות `PREVIEW_*`; אף אחת אינה נקראת אוטומטית — אין עדיין חיבור ל-UI ואין יכולת כתיבה זמינה למשתמש; Preview v2 נשאר בפועל Read Only. 32 בדיקות VM/Node + בדיקת Edge/CDP אמיתית עברו. `index.html` לא נגע בו. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - ~~**שלב ז׳.2 — תפריט פעולות שורה: ארכוב ושחזור**~~ — **הושלם ומאומת, נשמר ב-commit (30/07/2026)**: תפריט ⋮ **במסך התנועות בלבד** (`renderTxListWithActions()`, `toggleRowMenu()`/`closeAllRowMenus()`, `handleRowMenuAction()`) מחובר ל-`archivePreviewItem()`/`unarchivePreviewItem()` — יכולת כתיבה ראשונה **פעילה בפועל** ב-Preview v2. מסך הבית ("פעילות אחרונה") ללא שינוי. כתיבה מוגבלת ל-`PREVIEW_DATA_KEY` בלבד. בדיקת End-to-End מלאה בדפדפן אמיתי (קליקים אמיתיים) עברה במלואה. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - ~~**שלב ז׳.4 — עריכה Inline מתפריט ⋮**~~ — **הושלם ומאומת, נשמר ב-commit `c609459` (30/07/2026)**: פריט "✏️ עריכה" נוסף לתפריט ⋮ הקיים (מסך תנועות בלבד), זמין רק לפריטים פעילים (החלטת מוצר 1א). לחיצה מחליפה את השורה עצמה בטופס Inline (החלטה 2א) לפי 5 סוגי הפריטים, בהעתקה מילולית של הלוגיקה/הולידציה מ-`index.html` (`buildPreviewEditFormHtml`, `savePreviewInlineEdit`, `cancelPreviewEdit`, `handleEditMenuAction`, `togglePreviewCardLast4Field` חדשות). כשל ולידציה מציג `alert()` בלבד, ללא רכיב שגיאה חדש (החלטה 3א), ואינו יוצא ממצב עריכה; ביטול הוא no-op מלא; מסנן התנועות הנוכחי נשמר גם אחרי ביטול וגם אחרי שמירה. כותבת רק דרך `savePreviewItems()`/`renderAllPreviewScreens()` הקיימות. 26 בדיקות VM/Node + 18 בדיקות Edge/CDP אמיתיות (כולל `alert()` אמיתי דרך CDP) עברו. אין נגיעה בנתונים אמיתיים/`categoryConfig`/`index.html`. ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - **כל 7 יכולות הכתיבה מהמיפוי המקורי הושלמו** (ארכוב/שחזור/מחיקה/עריכה/הוספה) — ראו ז׳.5 למעלה. השלב הבא ברצף ז׳ טרם הוגדר.
  8. ~~**שלב ח׳ — סבב בדיקות End-to-End מסכם ל-Preview v2**~~ — **הושלם (31/07/2026)**: PASS מלא, 200/200 בדיקות (132 VM/Node + 68 Edge/CDP) על כל 4 המסכים וכל 7 יכולות הכתיבה יחד, כולל Persistence, בידוד `PREVIEW_*` מלא, ו-Console נקי. אפס כשלים, לא נדרש תיקון קוד. `index.html`/`app.js`/`styles.css` לא נבדקו (מחוץ להיקף). ראו CURRENT_STATUS.md ו-CHANGELOG.md.
   - **השלב הבא ל-Preview v2 (אחרי רצפי ז׳+ח׳) טרם הוגדר.**
- **תוקן** — פער היתרה השלילית (`₪-1,234` במקום `-₪1,234`) שתועד כאן כפער ידוע ב-`index.html` הישן: התבנית השגויה כבר תוקנה בתוך Preview v2 בשלב 3ব.5 (דרך `formatHomeCurrency()`), ומאז ה-Cutover (שלב 4.7, commit `c63a43e`) התיקון קיים גם ב-`index.html` הראשי החדש. אין פעולה נוספת נדרשת.
- **ניקוי קבצים ישנים לאחר Cutover** (מחוץ להיקף Version 1.0, הוחלט בשלב 4.6): `index-preview-v2.html`, `app.js`, `styles.css` נשארים זמנית בריפו — אינם נמחקים כחלק מ-1.0. ניקוי/הסרה יהיה שלב עתידי נפרד, רק לאחר תקופת אימות חי של `index.html` הראשי ואישור מפורש נוסף. טרם תוזמן.
- ~~**ניקוי פונקציות legacy מ-Version 1.3**~~ — **הושלם (Milestone 6, 02/09/2026)**: `computeForecast`/`getForecastWarning`/`getBiggestUpcomingCharge`/`getProjectedBalanceAfterUpcoming` הוסרו לאחר אימות call-graph מלא (אפס קריאה חיה — ראו CURRENT_STATUS.md, סעיף "Milestones 3–6").
- **הבזק ויזואלי קצר של כרטיס "אזהרת תחזית" ממוק במסך תובנות (זוהה בביקורת Version 1.3, לא רגרסיה)**: `renderMockUI()`/`MOCK_DATA.insights` מציירים לרגע כרטיס אזהרה קבוע-מראש לפני שהרינדור האמיתי דורס אותו — התנהגות bootstrap קיימת מלפני Version 1.3 כולה. לא תוקן, נדחה.
- **`edge_profile*.tmp/` (untracked, לא חלק מהפרויקט)** — מספר תיקיות פרופיל Edge זמניות שנוצרו לצורך בדיקות CDP (שלבים ה׳.4, ו׳, ו׳.1), נותרו נעולות חלקית ע"י תהליך אנטי-וירוס בזמן ניסיונות מחיקה. אינן מכילות תוכן מהפרויקט, אינן tracked. ניתן למחוק ידנית מ-File Explorer בכל עת; לא הוחלט לבצע פעולות מערכת (הרשאות/`.gitignore`) רק כדי להסירן.
- ~~**להחליט מה עושים עם `app.js` ו-`styles.css`**~~ — **הוכרע ובוצע (Milestones 8–9, 03/09/2026)**: נבחרה אופציה 2 — חוברו בפועל ל-`index.html` (מבנה 3 קבצים אמיתי), התוכן הישן/המיושן בשניהם הוחלף במלואו בתוכן המדויק שחולץ מה-`index.html` הפעיל. ראו TODO.md סעיף "Milestones 8–9" למעלה ו-CURRENT_STATUS.md לפירוט האימות.
- ~~**לתקן חוסר עקביות ב-styles.css: `tabs { ... }` במקום `.tabs { ... }`**~~ — **לא רלוונטי יותר**: הכלל הישן הזה לא קיים כלל ב-CSS הנוכחי של האפליקציה; לאחר ההחלפה המלאה ב-Milestones 8–9, `styles.css` תואם 1:1 לסגנון האמיתי של `index.html` (הבאג לא "עבר" כי המקור לא הכיל אותו).
- ~~**להחליט על גורל `index-preview.html` (v1)**~~ — **נסגר, לא רלוונטי יותר (מאומת 31/08/2026)**: הקובץ כבר אינו קיים בפרויקט (untracked ולכן לא ניתן לשחזור מ-Git) — אין יותר החלטה לקבל.
- **להחליט על גורל `Design/`** — untracked, לא שלנו. להחליט אם להישאר untracked לצמיתות (`.gitignore`), או טיפול אחר — לא להעלות ל-Git כמות שהוא (נכסים ממותגים).

## חשוב
- **תיעוד לא עודכן בזמן אמת עבור 4 השלבים הראשונים של Version 1.1 (01/08/2026)** — שוחזר ב-03/08/2026 מניתוח Git ישיר (git show/diff), לא מזיכרון שיחה. אין ודאות לגבי מספור/שם "שלב" מדויק שנעשה בו שימוש במקור בשיחות ההן. **לקח לעתיד**: לשמור על עדכון תיעוד בסיום כל שלב Version 1.1, כפי שנעשה שוב החל משלב 4.0.2.
- **שני אריחי-הסטטיסטיקה במסך הבית ("יתרת תשלומים שונים"/"יתרת הלוואות") עם handler ריק** (`handleStatTileClick`) — placeholder מכוון למסך פירוט עתידי, לא באג. אין תוכנית מאושרת למימוש.
- **אין בדיקות (tests) אוטומטיות** בפרויקט — בוצעו שתי סבבי בדיקת end-to-end ידניים (29/07/2026, ראו CHANGELOG.md), אך אין חבילת בדיקות קבועה/אוטומטית. הלוגיקה העסקית (חישובי תשלומים שנותרו, תחזית 6 חודשים) מורכבת יחסית ותלויה בחישובי תאריכים; שווה לשקול בדיקות שיטתיות/אוטומטיות.
- **אין תיעוד inline (הערות קוד) להסברת הלוגיקה המורכבת** מעבר לכותרת אחת ("לוגיקת חישוב מועדים ותשלומים שנותרו") — הבנת הפונקציות `getBillingRange` / `parseDatesAndGetLeft` דורשת קריאה מדוקדקת.
- **שגיאת Console: `favicon.ico` מחזיר 404** — נמצא ב-29/07/2026 בבדיקת end-to-end. הדפדפן שולף אוטומטית `/favicon.ico` בנוסף לאייקון ה-data-URI המוגדר ב-`<link rel="icon">`, והשרת (או כל שרת סטטי) מחזיר 404 עבורו. לא משפיע על תפקוד האפליקציה, אך יוצר שורת שגיאה מיותרת ב-Console. תיקון אפשרי: הוספת קובץ `favicon.ico` אמיתי לשורש הפרויקט.
- **UX/CSS עתידי — הפרש 5px ב-`scrollWidth` מול `clientWidth` במובייל (390px רוחב)**: נמצא ונחקר באימות Edge/CDP אמיתי ל-Version 1.2 (03/08/2026). המקור: `.bottom-nav` (`position:fixed; left:0; right:0`) מקבל `width=395`/`left=-5` בגלל אינטראקציה בין `dir="rtl"` וגריר-הגלילה האנכי (scrollbar-gutter) בדפדפן. **אומת מול ה-baseline שלפני Version 1.2** (`git show HEAD:index.html` לפני שינויי Version 1.2) — **אותה תוצאה בדיוק** קיימת שם — כלומר תופעה קיימת מראש ב-CSS של `.bottom-nav`, לא רגרסיה של Version 1.2 ולא נגרמת משום שינוי בשלב הזה. אינה גורמת לחפיפה/חיתוך/גלישה נראית לעין (אומת בצילומי מסך). לא תוקנה — נרשמת כאן כפריט עתידי בלבד, ללא תיקון במסגרת Version 1.2.

## בדיקות עתידיות
זרימות שממומשות בקוד אך **טרם נבדקו בהרצה בפועל**:
- **בדיקת הלוואות** — הוספה/עריכה של פריט מסוג `loan`, כולל ריבית ויום חיוב (הלוגיקה לא שונתה באף אחד מהשינויים האחרונים ונשארה כפי שהייתה)
- **בדיקת חישובי תאריכים במקרי קצה** — אימות נכונות `getBillingRange` / `parseDatesAndGetLeft` / `computeForecast` (למשל: יום התחלה אחרי/לפני יום החיוב, תחילת/סוף שנה)
- **בדיקת עריכה/ארכוב/מחיקה על סוגים/מסלולים נוספים** — נבדקו עד כה: `variable` (29/07/2026), `fixed` עם מעבר בנק↔כרטיס (29/07/2026). עדיין לא נבדקו במפורש: `loan`, `dated`, `income`

## שיפור עתידי
(מבוסס ישירות על סעיף "רעיונות לשיפור עתידי" ב-[README.md](README.md))
- סנכרון ענן בין מכשירים
- ייצוא/יבוא של נתונים
- דוחות חודשיים וגרפים נוספים
- הגדרות נוספות למעקב אחר מטרות תקציביות

## לא ברור / דורש בירור עם המשתמש
- ~~איזו משלוש האפשרויות ל-app.js/styles.css להעדיף~~ — **הוכרע (Milestones 8–9, 03/09/2026)**, ראו "דחוף" למעלה.
- האם קיימת כוונה עתידית לעבור לארכיטקטורת שרת/סנכרון, או שהעיקרון "ללא שרת" ישאר קבוע.
- האם יש דרישת תאימות דפדפנים ישנים שמסבירה את סגנון הקוד ה-ES5.
