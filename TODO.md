# TODO.md — FamilyFinance PRO

מבוסס על מה שניתן לזהות מהקוד, מה-README, ומהמצב הנוכחי בלבד. אין רשומות מומצאות.

## דחוף
- **להחליט מה עושים עם app.js ו-styles.css** — מאומת (29/07/2026) שאינם מקושרים ל-index.html בפועל ואינם בשימוש כלל (ראו CURRENT_STATUS.md). יש לבחור אחת מהאפשרויות:
  1. למחוק את שני הקבצים
  2. לחבר אותם בפועל ל-index.html (מעבר למבנה 3 קבצים: HTML+CSS+JS נפרדים) ולהסיר את הקוד ה-inline המקביל
  3. להשאיר אותם, אך לתעד במפורש (למשל בהערה בראש הקובץ) שהם טיוטה לא פעילה, כדי למנוע עריכה בטעות מתוך מחשבה שהם משפיעים על האפליקציה
- **לתקן חוסר עקביות ב-[styles.css:47](styles.css#L47)**: `tabs { ... }` במקום `.tabs { ... }` (רלוונטי רק אם/כאשר styles.css יחובר בפועל — אופציה 2 לעיל).

## חשוב
- **אין Git repository** — כדאי לשקול אתחול Git כדי לאפשר מעקב שינויים, גיבוי, ו-rollback (בכפוף לאישור המשתמש; לא בוצע בשלב זה לפי הנחיה מפורשת).
- **אין בדיקות (tests) אוטומטיות** בפרויקט — בוצעה בדיקת end-to-end ידנית חד-פעמית (29/07/2026, ראו CHANGELOG.md), אך אין חבילת בדיקות קבועה. הלוגיקה העסקית (חישובי תשלומים שנותרו, תחזית 6 חודשים) מורכבת יחסית ותלויה בחישובי תאריכים; שווה לשקול בדיקות שיטתיות/אוטומטיות.
- **אין תיעוד inline (הערות קוד) להסברת הלוגיקה המורכבת** מעבר לכותרת אחת ("לוגיקת חישוב מועדים ותשלומים שנותרו") — הבנת הפונקציות `getBillingRange` / `parseDatesAndGetLeft` דורשת קריאה מדוקדקת.

## בדיקות עתידיות
זרימות שממומשות בקוד אך **טרם נבדקו בהרצה בפועל** (הבדיקה מ-29/07/2026 כיסתה רק דשבורד, הוספת תנועה, localStorage, קטגוריות, תחזית וארכיון — ראו CHANGELOG.md):
- **בדיקת הלוואות** — הוספה/עריכה של פריט מסוג `loan`, כולל ריבית ויום חיוב
- **בדיקת תשלומים** — הוספה/עריכה של פריט מסוג `variable` (תשלומים שונים)
- **בדיקת חישובי תאריכים** — אימות נכונות `getBillingRange` / `parseDatesAndGetLeft` / `computeForecast` במקרי קצה (למשל: יום התחלה אחרי/לפני יום החיוב, תחילת/סוף שנה)
- **בדיקת עריכה inline** — `startEdit` / `saveInlineEdit` על כל סוגי התנועות, כולל ולידציה בשמירה

## שיפור עתידי
(מבוסס ישירות על סעיף "רעיונות לשיפור עתידי" ב-[README.md](README.md))
- סנכרון ענן בין מכשירים
- ייצוא/יבוא של נתונים
- דוחות חודשיים וגרפים נוספים
- הגדרות נוספות למעקב אחר מטרות תקציביות

## לא ברור / דורש בירור עם המשתמש
- איזו משלוש האפשרויות ל-app.js/styles.css להעדיף — ראו "דחוף" למעלה.
- האם קיימת כוונה עתידית לעבור לארכיטקטורת שרת/סנכרון, או שהעיקרון "ללא שרת" ישאר קבוע.
- האם יש דרישת תאימות דפדפנים ישנים שמסבירה את סגנון הקוד ה-ES5.
