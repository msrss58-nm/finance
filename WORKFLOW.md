# WORKFLOW.md — נוהל עבודה קבוע לפרויקט FamilyFinance PRO

פרויקט זה **נפרד לחלוטין** מפרויקטים אחרים (כגון My Event). אין לערבב מידע, זיכרון, קבצים, כללי עבודה או הנחות בין הפרויקטים.

## בתחילת כל שיחה חדשה על הפרויקט
1. לקרוא את [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md), [CURRENT_STATUS.md](CURRENT_STATUS.md) ו-[CHANGELOG.md](CHANGELOG.md).
2. לבדוק `git status`, את הענף הנוכחי, ואת חמשת ה-commits האחרונים — **בכפוף לכך שיאותחל Git repository בעתיד**; נכון להיום אין Git בפרויקט, ויש לציין זאת בפירוש בתחילת השיחה במקום לדלג על הבדיקה בשתיקה.
3. להשוות בין התיעוד לבין מצב הקוד בפועל (למשל: לוודא שאין קבצים חדשים/שהשתנו מאז כתיבת התיעוד).
4. לדווח למשתמש על כל אי-התאמה שנמצאה בין התיעוד למצב הקוד בפועל **לפני** ביצוע כל שינוי.

## במהלך העבודה
5. לעבוד שלב אחד בכל פעם — לא לבצע כמה שינויים מקבילים בלי אישור ביניים.
6. **לא לבצע** שינוי קוד, commit, push, migration, deploy, או התקנת חבילות — **ללא אישור מפורש ומיידי** מהמשתמש לאותה פעולה ספציפית. אישור על פעולה אחת אינו אישור גורף להמשך.
7. בכל החלטה או ממצא הנוגעים לאי-ודאות (במיוחד לגבי app.js / styles.css, ראו CURRENT_STATUS.md ו-TODO.md), לציין זאת במפורש כ"דורש אימות" ולא להניח הנחות.

## בסיום כל שלב עבודה
8. לעדכן את קובצי התיעוד הרלוונטיים (PROJECT_CONTEXT.md / CURRENT_STATUS.md / CHANGELOG.md / TODO.md) בהתאם למה שהשתנה בפועל.
9. לשמור נקודת המשך ברורה בסוף [CURRENT_STATUS.md](CURRENT_STATUS.md) (סעיף "נקודת ההמשך הנוכחית") כדי שאפשר יהיה להמשיך בשיחה הבאה בלי לאבד הקשר.

## עקרונות כלליים לפרויקט הזה
- קובץ יחיד (index.html) הוא מקור האמת הפעיל של האפליקציה — כל שינוי לוגי חייב להתבצע שם, אלא אם התקבלה החלטה מפורשת לעבור למבנה מרובה-קבצים (ואז לעדכן גם את app.js/styles.css ואת index.html כך שיטען אותם).
- לשמור על עקרון "ללא שרת, ללא build, ללא ספריות חיצוניות" אלא אם המשתמש מבקש לשנות זאת במפורש.
- לשמור על תאימות מבנה הנתונים ב-localStorage (`family_finance_data`, `family_finance_cat_config`) כדי לא לאבד נתוני משתמשים קיימים.
