# Do not auto-install optional/peer native modules (react-native-reanimated,
# react-native-worklets, gesture-handler) that expo-router lists as peers of
# features this app does not use. Keeps the native surface to approved
# first-party Expo modules - the same resolution Stage 0 verified on device.
legacy-peer-deps=true
