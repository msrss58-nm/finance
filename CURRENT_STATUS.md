# CURRENT_STATUS.md — FamilyFinance PRO

## מצב הפרויקט כרגע
פרויקט בשלב מוקדם-בינוני. יש אפליקציה עובדת בקובץ [index.html](index.html) יחיד, עם כל הפיצ'רים המתוארים ב-README ממומשים בקוד **ומאומתים בהרצה בפועל** (ראו סעיף הבא). **קיים Git repository** בתיקייה (אותחל ב-29/07/2026), ענף `main`, **מחובר ל-remote בשם `origin`** (`https://github.com/msrss58-nm/finance.git`) ומסונכרן עם `origin/main` (סונכרן ב-29/07/2026 באמצעות `git push --force-with-lease`, לאחר גיבוי מלא של הגרסה הקודמת בענף `backup-before-local-sync-2026-07-29` — ראו CHANGELOG.md). הפרויקט **כבר נדחף ל-GitHub בפועל**.

**עדכון (03/08/2026) — Version 1.1 בתהליך, שלב 4.0.3 הושלם:** לאחר סגירת Version 1.0, בוצע רצף פיתוח נוסף על גבי `index.html` (מפתחות אמיתיים, ללא Preview נפרד) שכונה בשיחות "Version 1.1": אריחי קטגוריה ניתנים לסידור מחדש במסך הבית, הפיכת כל קטגוריה ל"עמוד" עצמאי (סינון/הוספה/עריכה/מחיקה הקשריים), אריחי-סיכום חדשים במסך הבית ("יתרת תשלומים שונים", "יתרת הלוואות"), ימי כניסה/ירידה ברירת מחדל לקטגוריה, פתיחת עריכת תנועה בלחיצה על השורה עצמה, ארכוב אוטומטי להתחייבויות שהסתיימו, והפרדת "קטגוריות" מ"הגדרות" עם בניית מסך הגדרות מלא (אבטחה/PIN, מראה, התראות, נתונים/גיבוי/CSV, אפשרויות ניסיוניות, אודות, יומן פעילות). `APP_VERSION` בקוד כרגע: `'1.1.0 Preview'`. פירוט מלא בסעיף "Version 1.1" בתחתית מסמך זה. **הערה מתודולוגית חשובה**: 5 השלבים הראשונים ברצף זה (01/08/2026) לא תועדו בזמן אמת בשיחות שביצעו אותם — התיעוד שלהם למטה שוחזר ב-03/08/2026 מניתוח ישיר של היסטוריית Git (הודעות commit + diff מלא מול `index.html`), ולא מזיכרון שיחה. משלב 4.0.2 (02/08/2026) ואילך התיעוד חזר להיכתב כרגיל תוך כדי העבודה.

**מצב נוכחי (31/07/2026) — Version 1.0 הושלמה:** ה-Cockpit ("Preview v2") עבר **Cutover מלא** והפך ל-`index.html` הראשי והחי של האפליקציה (commit `c63a43e`, ענף `feature/cockpit-preview-v2`). כל 432 הבדיקות (367 VM/Node + 65 Edge/CDP) עברו מול ה-`index.html` הסופי. אין migration ואין שינוי סכימת נתונים — האפליקציה החדשה קוראת/כותבת לאותם מפתחות `family_finance_data`/`family_finance_cat_config` שהיו קיימים תמיד. ראו סעיפי "שלב 4.4"–"שלב 4.8" בתחתית המסמך לפירוט המלא של ה-Cutover. שלבי הרקע ("שלב האפיון לעיצוב מחדש (Cockpit)" ואילך) מתועדים למטה כפי שהיו, כהיסטוריה מלאה של איך שהתקבלה לגרסה הנוכחית.

## מה עובד — מאומת בהרצה בפועל (לא רק קריאת קוד)
בתאריך 29/07/2026 בוצעה בדיקת end-to-end אמיתית: הפעלת שרת מקומי (`python -m http.server`), טעינת index.html בדפדפן Edge headless, ואינטראקציה בפועל (מילוי טפסים, לחיצות כפתורים אמיתיות, רענון דף) דרך Chrome DevTools Protocol. כל הבדיקות הבאות **עברו בהצלחה**:
- **טעינת הדף** — נטען עד `document.readyState === 'complete'` ללא שגיאות
- **דשבורד** — מוצג עם יתרה פנויה ואריחי סיכום לכל קטגוריה
- **הוספת תנועה** — נבדק עם תנועת הכנסה: מילוי טופס + לחיצת כפתור אמיתית עדכנו את היתרה מיידית
- **שמירה ב-localStorage** — הנתונים נשמרו ושרדו רענון דף מלא (Page.navigate מחדש), מספר הפריטים והיתרה זהים לפני ואחרי
- **ניהול קטגוריות** — יצירת קטגוריה מותאמת אישית עבדה והשתקפה גם ברשימת הניהול וגם בטופס ההוספה
- **תחזית חודשית** — הגרף (SVG) הציג 6 עמודות כצפוי לאחר פתיחת האקורדיון
- **ארכיון** — ארכוב תנועה ומעבר בין הטאבים "פעילות"/"ארכיון" עבדו כמצופה
- **Console** — לא נלכדה אף שגיאה או אזהרה (`exceptionThrown`, `console.error/warn`, `Log` ברמת error/warning) לאורך כל תרחיש הבדיקה
- טעינה/שמירה ל-localStorage (מפתחות `family_finance_data`, `family_finance_cat_config`)
- עריכה inline, שחזור מארכיון, מחיקה סופית — **נבדקו בפועל (29/07/2026) על פריט מסוג `variable`** במסגרת בדיקת השינוי בלוגיקת התזרים (ראו סעיף למטה); שאר סוגי הפריטים (fixed/loan/dated/income) — עדיין לא נבדקו במפורש לעריכה/מחיקה, ראו TODO.md
- הגנת XSS בסיסית באמצעות `escapeHtml()`

הבדיקה בוצעה על פרופיל דפדפן זמני ומבודד — נתוני localStorage האמיתיים של המשתמש בדפדפן שלו לא נגעו כלל.

## app.js ו-styles.css — מאומת שאינם מקושרים ואינם בשימוש בפועל
נבדק ישירות ב-29/07/2026 על ה-HTML **כפי שנטען בפועל בדפדפן** (`document.documentElement.outerHTML`): אין שום הפניה ל-`app.js` (לא `<script src>`) ואין שום הפניה ל-`styles.css` (לא `<link>`). כלומר [app.js](app.js) ו-[styles.css](styles.css) הם קבצים כפולים לחלוטין מבחינת תוכן (עותק כמעט זהה של הלוגיקה וה-CSS מתוך index.html), אך **אינם פעילים באפליקציה בשום צורה**.

**נקודה פתוחה שנותרה ללא הכרעה** (לא הוכרעה בשלב הזה, ממתינה להנחיית המשתמש): מה לעשות עם app.js ו-styles.css — למחוק, לחבר בפועל ל-index.html, או להשאיר כקבצי טיוטה מתועדים במפורש. ראו TODO.md.

נמצא גם הבדל תחביר קטן בין הגרסאות: ב-[styles.css:47](styles.css#L47) הכלל `tabs { ... }` חסר נקודה בתחילת הסלקטור (צריך להיות `.tabs`), בעוד שבגרסה ה-inline בתוך index.html זה נכון (`.tabs { ... }`). לא משפיע על התנהגות האפליקציה כל עוד styles.css אינו נטען.

**עדכון (03/09/2026, Milestones 8–9): הסעיף הזה כולו הפך היסטורי בלבד ואינו משקף עוד את המצב הנוכחי.** `app.js` ו-`styles.css` **כן** מקושרים בפועל כעת (`<link rel="stylesheet" href="styles.css">`/`<script src="app.js"></script>` ב-`index.html`, נתיבים יחסיים), התוכן הישן/המיושן שתואר כאן הוחלף במלואו בתוכן שחולץ במדויק מ-`index.html` החי, וה-`.tabs`/`tabs` שתואר לעיל אינו קיים כלל ב-CSS הנוכחי. ראו סעיף "Milestones 8–9" למטה לפירוט המלא והמאומת.

## מה לא עובד
לא זוהו תקלות בבדיקת ה-end-to-end שבוצעה (ראו זרימות שנבדקו לעיל). זרימות שטרם נבדקו בפועל (הלוואות, תשלומים, חישובי תאריכים, עריכה inline) מפורטות ב-TODO.md תחת "בדיקות עתידיות" — עד שייבדקו, מצבן נשאר "ממומש בקוד, לא מאומת בהרצה".

## תקלות ידועות
- הבדל תחביר CSS ב-[styles.css:47](styles.css#L47) שתואר לעיל — לא משפיע על index.html כי הקובץ אינו נטען בפועל.
- אין הודעות שגיאה, TODO, FIXME, או הערות "known issue" בקוד עצמו.

## שינויים מקומיים שלא נשמרו (לא ב-commit)
לפני ה-commit הנוכחי: `index.html` שונה (פיצ'ר אמצעי חיוב ל"הוצאות קבועות", ראו למטה), וקובצי התיעוד (CURRENT_STATUS.md, CHANGELOG.md, TODO.md, WORKFLOW.md) עודכנו במקביל — כולם נשמרים יחד ב-commit אחד בסיום העדכון הזה.

## שינוי לוגי #1: "תשלומים שונים" הוצאו מחישוב התזרים (29/07/2026, נשמר ב-commit `056e7c4`)
לפי בקשת המשתמש: "תשלומים שונים" (`variable`) מייצגים רכישה בכרטיס אשראי, וההשפעה בפועל על התזרים אמורה להגיע רק מהחיוב בכרטיס האשראי (שהמשתמש רושם בנפרד, ידנית, כפריט `dated` נפרד) — לא מהעסקה בתשלומים עצמה. שינוי זה מונע ספירה כפולה.

**מה שונה ב-[index.html](index.html):**
- ב-`renderAll()`: הוסרה הוספת `item.amount` ל-`monthlyExpenses` מענף `variable` — כך "תשלומים שונים" כבר לא מופחתים מ"יתרה פנויה". חישובי `catStats[cKey].monthly`/`totalLeft` (אריחי המעקב) **נשארו כפי שהיו**.
- ב-`computeForecast()`: ענף `variable` כבר לא מוסיף ל-`sumExp`.
- **לא שונה**: לוגיקת `loan`, מבנה הנתונים ב-localStorage, עריכה, ארכוב, שחזור, מחיקה, `app.js`, `styles.css`.

**בדיקה (29/07/2026, שרת מקומי + Edge headless, פרופיל מבודד):** תרחיש הכנסה ₪10,000 + כרטיס אשראי ₪2,000 + תשלומים שונים ₪500/חודש → יתרה פנויה ₪8,000 (לא ₪7,500), תחזית מפחיתה רק את חיוב הכרטיס, ארכוב/עריכה/מחיקה פועלים, נתונים ישנים נטענים ללא שינוי מבנה. כל 9 הבדיקות עברו. נמצאה שגיאת Console אחת לא-קשורה (`favicon.ico` 404, ראו TODO.md).

## שינוי לוגי #2: אמצעי חיוב ל"הוצאות קבועות" — בנק / כרטיס אשראי (29/07/2026)
לפי בקשת המשתמש: השדה הקיים "איפה יורד" (עבור `fixed` בלבד) הפך משדה טקסט חופשי לשדה בחירה (`select`) עם שתי אפשרויות בדיוק: **חשבון בנק** (ברירת מחדל) ו-**כרטיס אשראי**. בבחירת "כרטיס אשראי" מוצג שדה נוסף ומותנה ל-4 הספרות האחרונות של הכרטיס (`cardLast4`), עם ולידציה שדורשת בדיוק 4 ספרות.

**החלטה מאושרת לגבי אריח הדשבורד "הוצאות קבועות":** האריח ממשיך להציג את **סך כל** ההוצאות הקבועות — גם בנק וגם כרטיס אשראי — לצורך מעקב מלא, ללא קשר לאמצעי החיוב. לעומת זאת, **היתרה הפנויה** ו-**התחזית ל-6 חודשים** כוללות **רק** הוצאות קבועות שאמצעי החיוב שלהן הוא "חשבון בנק"; הוצאות קבועות בכרטיס אשראי ממשיכות להישמר, להופיע בפירוט, בעריכה ובארכיון, אך אינן נכללות ביתרה הפנויה או בתחזית (ההשפעה בפועל מגיעה מחיוב כרטיס האשראי שהמשתמש רושם בנפרד — כמו בשינוי #1).

**מה שונה ב-[index.html](index.html):**
- טופס הוספה: `fix-where` הפך מ-`<input type="text">` ל-`<select>` (bank/credit), עם שדה מותנה חדש `fix-card-last4`.
- פונקציה חדשה `toggleCardLast4Field()` להצגה/הסתרה של שדה 4 הספרות.
- `addNewItem()` ו-`saveInlineEdit()` (ענף `fixed`): ולידציה בדיוק 4 ספרות (`/^\d{4}$/`) בשמירה כשנבחר כרטיס אשראי; איפוס `cardLast4` ל-`''` כשנבחר בנק. שמירה נכשלת (עם `alert`, ללא איבוד נתונים) אם הערך אינו תקין.
- טופס העריכה inline: אותה בחירה + שדה מותנה, עם ברירת מחדל לפי `item.where === 'credit'`.
- תצוגת פריט: "איפה יורד" מציג כעת "חשבון בנק" או "כרטיס אשראי ****XXXX".
- `renderAll()` ו-`computeForecast()`: הוצאה קבועה עם `item.where === 'credit'` לא נכללת ב-`monthlyExpenses`/`sumExp`; `catStats[cKey].monthly` (אריח הדשבורד) **לא שונה** — ממשיך לכלול הכל, כפי שאושר.
- **לא שונה כלל**: `loan`, `variable`, `dated`, `income`, ארכיון/שחזור/מחיקה (גנריים), `app.js`, `styles.css`.

**בדיקה (29/07/2026, שרת מקומי + Edge headless, פרופיל מבודד):** תרחיש הכנסה ₪10,000 + שכירות-בנק ₪3,000 + מנוי חדר כושר-כרטיס ₪200 → יתרה פנויה ₪7,000, תחזית ₪7,000, תצוגה "כרטיס אשראי ****1234" תקינה. ולידציה חסמה הוספה/עריכה עם ערך לא-מספרי ועם פחות מ-4 ספרות. עריכה בין בנק↔כרטיס עדכנה את `cardLast4` והחישוב כראוי בשני הכיוונים. ארכוב/שחזור/מחיקה על פריט כזה פעלו. אריח "הוצאות קבועות" הציג ₪3,150 (סכום מלא, בנק+כרטיס) בעוד היתרה הפנויה נשארה ₪7,000 — בדיוק ההתנהגות שאושרה. כל 14 הבדיקות המהותיות עברו; נמצאה שוב אותה שגיאת Console לא-קשורה (`favicon.ico` 404).

## סנכרון Git עם GitHub (29/07/2026)
לאחר commit `0c2efa6`, חובר remote `origin` (`https://github.com/msrss58-nm/finance.git`). התגלה ש-`origin/main` (`dd74136`) הוא **היסטוריה לא-קשורה** לחלוטין (`git merge-base` ריק) — תוכן זהה כמעט ב-100% לנקודת ההתחלה המקומית שלנו (הבדל CRLF/LF בלבד), אך ללא אף אחד מהשינויים המקומיים שבוצעו. לפני כל דריסה: נוצר ואומת ענף גיבוי `backup-before-local-sync-2026-07-29` שמצביע על `dd74136` (הגרסה הישנה נשמרת שם במלואה ונגישה). לאחר האימות בוצע `git push --force-with-lease origin main:main` (לא `--force` רגיל) — הצליח, `main` המקומי ו-`origin/main` מסונכרנים כעת על `0c2efa6`.

## ניסיון PWA — בוצע ובוטל במלואו (29/07/2026)
נבנתה תמיכת PWA בסיסית (`manifest.webmanifest`, `service-worker.js`, `icon-192.png`, `icon-512.png`, עדכוני `<head>` ורישום Service Worker ב-`index.html`), נבדקה בהצלחה (טעינה, manifest, SW פעיל, פעולה ב-offline, שמירת נתונים) — **אך לבקשת המשתמש הוחזרה במלואה לפני commit**: 4 הקבצים החדשים נמחקו, ו-`index.html` + קובצי התיעוד שוחזרו במדויק לגרסת `HEAD` (`git restore`, ללא `reset --hard`). **התיעוד עצמו נכתב מחדש בשיחה הזו כי גם הוא שוחזר יחד עם השאר** (`git restore` מחזיר לגרסת ה-commit האחרון, כולל שינויי תיעוד לא-committed). כרגע **אין שום שריד ל-PWA** בפרויקט.

## גילוי וניתוח תיקיית Design/ (29/07/2026)
התגלתה תיקיית `Design/` (untracked, לא נוצרה על ידי, לא נגעתי בתוכנה) עם 5 מסכי Google Stitch: `dashboard_pulse`, `financial_coach`, `onboarding_bank_connection`, `transactions_feed` (כל אחד עם `code.html` + `screen.png`), ו-`riseup_financial/DESIGN.md` (מסמך design-tokens בלבד — צבעים/טיפוגרפיה/spacing, לא מסך). כל 4 המסכים טוענים Tailwind CDN + Google Fonts + Material Symbols חיצוניים, ומכילים תמונות/לוגואים ממותגים (בנקים ישראליים אמיתיים, Apple, Netflix, אווטארים) שהם נכסי-הדגמה של הכלי עצמו — **לא לשימוש ישיר**, לא לצורך זכויות יוצרים ולא כי זה סותר את עקרון "ללא תלות בספריות חיצוניות". `financial_coach` (צ'אט AI) ו-`onboarding_bank_connection` (חיבור בנק אמיתי) הם פיצ'רים חדשים לגמרי שדורשים backend — **לא רלוונטיים לשלב זה** (אסור עדיין להוסיף backend/DB/Supabase).

## Preview v1 — נבנה, נבדק, ונמצא לא-מספק (29/07/2026)
נבנה `index-preview.html`: עיצוב חדש (טיל, RTL, בהשראת טוקני `riseup_financial/DESIGN.md`, ללא CDN חיצוני) על גבי **אותו מבנה HTML ואותה לוגיקה** של `index.html` (script מועתק 1:1), עם מפתחות localStorage **נפרדים** (`family_finance_preview_data`, `family_finance_preview_cat_config`) שמאותחלים פעם אחת מהנתונים האמיתיים ולעולם לא כותבים אליהם. כלל 11 בדיקות הבטיחות/פונקציונליות עברו (כולל וידוא מוחלט שהמפתחות האמיתיים לא נכתבים/נמחקים). **קובץ זה עדיין קיים בפרויקט, untracked, לא הוסר** — אך **הוחלט במפורש שהוא אינו התשובה**: זו הייתה בעיקר "מתיחת פנים" (reskin) על אותו מבנה, layout, והיררכיית מידע — לא ממשק חדש כפי שהתבקש.

## שלב האפיון לעיצוב מחדש (Cockpit) — הושלם ואושר (29/07/2026)
בעקבות המסקנה על Preview v1, בוצע סבב תכנון מלא **בלי קוד**, כמעצב-מוצר החושב על אפליקציה חדשה לגמרי (לא מתחיל מ-`index.html` הקיים):

**עקרונות UX שאושרו:**
- עקרון 10 השניות: פתיחת האפליקציה חייבת לענות מיידית על "מה מצבי / האם אני בסדר / מה הבא שדורש תשומת לב" — בלי אינטראקציה.
- מטאפורת **Cockpit** (לוח מחוונים): לא "רשימת נתונים" אלא מבנה של מד ראשי + מדי-משנה + נורות אזהרה + תצוגת מצב אחרונה, ברור במבט אחד.
- מזעור לחיצות ל-3 הפעולות התכופות ביותר (הוספת הוצאה/הכנסה/הלוואה).
- **עקרון "סיפור הכסף"**: כל מספר מלווה במשפט אנושי שמסביר מצב/סיבה/מה בקרוב/מה דורש תשומת לב — לא מספרים יבשים.

**Wireframe מילולי שאושר** (4 מסכים + ניווט תחתון קבוע + פעולת הוספה צפה):
1. **בית (Cockpit)**: מד ראשי ("כמה פנוי לי להוציא" + סטטוס+נרטיב), Snapshot חודשי קומפקטי (הכנסות/הוצאות/יתרה), "מה דורש תשומת לב" (חיובים קרובים + דגלי חריגה), "פעילות אחרונה" (3-5 תנועות אחרונות).
2. **תנועות**: רשימה כרונולוגית מאוחדת (לא מקובצת לפי סוג פנימי), חיפוש, מסנן פעיל/ארכיון.
3. **תובנות**: פיד כרטיסי-תובנה **מבוססי תזרים והתחייבויות בלבד** (לא הרגלי צריכה/קטגוריות צריכה — האפליקציה לא מנהלת פירוט כזה). כל תובנה ממופה לנתון קיים ספציפי (ראו רשימה מלאה למטה).
4. **הגדרות**: ניהול קטגוריות (ללא שינוי מהותי מהקיים).

**Quick Actions:** לחיצה על [+] מציגה מיידית 3 כפתורים (הוצאה/הכנסה/הלוואה); לכל אחד טופס מינימלי עם ברירות מחדל חכמות ושדות-חובה מצומצמים ככל האפשר לשמירה ראשונית.

**תובנות מסך "תובנות" — כל אחת ממופה לנתון קיים בפועל (ללא המצאה):**
| תובנה | מקור נתון קיים |
|---|---|
| סך חיובי כרטיס אשראי (הוצאות קבועות) | סכימת `fixed` פעילים עם `where==='credit'` |
| הכרטיס עם החיוב הגבוה ביותר | קיבוץ אותם פריטים לפי `cardLast4` |
| חלוקת הוצאות קבועות בנק/כרטיס | סכימה לפי `where==='bank'` מול `'credit'` — **מוגבל ל-`fixed` בלבד**, כי זה השדה היחיד עם תיוג בנק/כרטיס מובנה |
| החיוב הקרוב והגדול ביותר | בין `loan`/`dated` בעלי תאריך ידוע |
| סך ההתחייבויות הקבועות | סכימת כל `fixed` הפעילים |
| יתרת הלוואות ותשלומים שנותרו | `amount * dt.left` הקיים מ-`parseDatesAndGetLeft` |
| יתרה צפויה אחרי חיובים קרובים | יתרה נוכחית פחות `loan`/`dated` קרובים |
| אזהרת יתרה שלילית עתידית | תוצאת `computeForecast()` הקיימת |
| שינוי בחיובים חד-פעמיים בין חודשים | סכימת `dated` לפי `start` בפועל — **רק אלה יש להם היסטוריה אמיתית**; אם אין נתון להשוואה, הכרטיס פשוט לא מוצג |

**הוחלט במפורש לא לכלול:** תובנות "הרגלי צריכה" (מזון/ביגוד/בילויים) — אין לאפליקציה נתונים ברמת פירוט כזו; המשתמש מזין סכומים כוללים ברמת כרטיס/בנק/הוצאה קבועה/הכנסה/הלוואה/חיוב חד-פעמי בלבד.

**תוכנית מימוש מאושרת — 5 שלבים (שלבים א׳-ב׳ הושלמו, שלב ג׳ הוא נקודת ההתחלה הבאה):**
- ~~**שלב א׳ — קובץ ואסטרטגיית בטיחות נתונים**~~ — **הושלם**: קביעת שם הקובץ (`index-preview-v2.html`), מפתחות localStorage **נפרדים** (Preview), מנגנון seed חד-פעמי + כפתור איפוס.
- ~~**שלב ב׳ — בניית המבנה החדש עם נתוני Mock**~~ — **הושלם**: HTML+CSS חדש לגמרי לפי ה-Wireframe שאושר, עם נתוני דמה קבועים בקוד, בלי תלות ב-`index.html` הקיים.
- **שלב ג׳ — שכבת "מנוע הסיפור" (חדשה, תוספתית)**: `getMonthSnapshot`, `getUpcomingKnownCharges`, `getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming`, `getFixedCreditCardTotals`, `getFixedBankVsCreditSplit`, `getTotalFixedCommitments`, `getLoansRemainingSummary`, `getForecastWarning`, `getDatedMonthOverMonthChange`, `getRecentActivity`, `buildNarrative` — קוראת בלבד מ-`items`/`categoryConfig`, לא כותבת/משנה סכימה.
- **שלב ד׳ — חיבור בפועל ל-localStorage וללוגיקה הקיימת**: העתקת פונקציות הלוגיקה הקיימות כלשונן (`addNewItem`, `saveInlineEdit`, `archiveItem`, `deletePermanently`, `computeForecast` וכו') וחיבורן למפתחות ה-Preview מ-שלב א׳ ולמבנה מ-שלב ב׳.
- **שלב ה׳ — בדיקות מקיפות**: שרת מקומי + Edge headless מבודד, לכל תובנה בנפרד מול Mock עם ערך צפוי מדויק, כולל וידוא בטיחות-נתונים מלאה (מפתחות אמיתיים לא נכתבים/נמחקים).

בהתאם לשיטת העבודה המוסכמת: **שלב אחד בכל פעם, בדיקה/אימות בכל שלב, ועצירה לאישור מפורש לפני מעבר לשלב הבא** — לא מבצעים את כל 5 השלבים ברצף ללא אישורים ביניים.

**החלטה קבועה שאושרה מחדש:** הלוגיקה העסקית, מבנה הנתונים (`items`/`categoryConfig`), וכללי בנק/כרטיס אשראי/תזרים **לא ישתנו כלל** בעיצוב החדש — רק ייעטפו בממשק חדש ובשכבת נרטיב תוספתית.

## שלב א׳ של תוכנית המימוש — הושלם (29/07/2026)
נוצר קובץ חדש **[index-preview-v2.html](index-preview-v2.html)** (untracked, טרם ב-commit) — שלד בלבד, **בלי** ה-UI של ה-Cockpit (זה שלב ב׳). מכיל:
- מפתחות localStorage נפרדים לחלוטין: `family_finance_preview_v2_data`, `family_finance_preview_v2_cat_config` — שונים גם מהמפתחות האמיתיים (`family_finance_data`/`family_finance_cat_config`) וגם ממפתחות Preview v1 (`family_finance_preview_data`/`family_finance_preview_cat_config`), כדי שהשלושה יוכלו להתקיים זה לצד זה בלי התנגשות.
- מנגנון seed חד-פעמי (`seedPreviewV2IfNeeded`): בפעם הראשונה בלבד מעתיק את הנתונים האמיתיים (אם קיימים) למפתחות ה-Preview v2; בכל טעינה נוספת לא דורס נתוני Preview v2 שכבר קיימים (גם אם המשתמש ערך אותם).
- כפתור איפוס (`resetPreviewV2Data`) עם אישור (`confirm`) — מוחק **רק** את שני מפתחות ה-Preview v2 ומרענן את הדף (מה שגורם ל-seed מחדש מהנתונים האמיתיים); לא נוגע במפתחות האמיתיים או במפתחות v1.
- שורת סטטוס שמציגה כמה תנועות/קטגוריות נטענו מ-Preview v2, לאימות חזותי.

**אימות שבוצע (29/07/2026, לוגי — לא דפדפן):** מאחר ששלב א׳ הוא שכבת בטיחות-נתונים בלבד (עדיין אין UI לבדוק בדפדפן), הורץ סקריפט Node.js שמחלץ את בלוק ה-`<script>` **המדויק** מתוך הקובץ בפועל (לא עותק משוכתב) ומריץ אותו מול `localStorage` מדומה + `confirm`/`document`/`location.reload` מדומים. 20 בדיקות עברו בהצלחה: seed ראשוני מעתיק נכון מהנתונים האמיתיים; המפתחות האמיתיים ומפתחות v1 לא נגעו בהם באף תרחיש; טעינה שנייה לא דורסת נתוני Preview v2 שכבר נערכו; איפוס מוחק רק את מפתחות v2 ומפעיל `location.reload`; ביטול האיפוס (`confirm=false`) לא משנה כלום; וסביבה ריקה (בלי נתונים אמיתיים בכלל) מקבלת ברירות מחדל תקינות בלי קריסה ובלי "להמציא" מפתחות אמיתיים. **בדיקת דפדפן מלאה (Edge headless) תבוצע בשלב ה׳**, לאחר שה-UI וה-wiring ללוגיקה הקיימים ייבנו (שלבים ב׳-ד׳) — בשלב הזה אין עדיין מה להראות בדפדפן מעבר לשלד.

## שלב ב׳ של תוכנית המימוש — הושלם (29/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** מבנה ה-UI המלא של ה-Cockpit + CSS, לפי ה-Wireframe שאושר, מוזן אך ורק ע"י אובייקט `MOCK_DATA` קבוע — **ללא כל חיבור ללוגיקה עסקית, ללא חישובים, וללא נגיעה בנתונים אמיתיים**. מנגנון הבטיחות של שלב א׳ (מפתחות Preview v2 נפרדים, seed חד-פעמי, כפתור איפוס) נשאר **ללא שינוי בקוד**, רק הועבר ויזואלית למסך ההגדרות.

**מבנה שנבנה:** מעטפת אפליקציה (כותרת + ניווט תחתון קבוע + כפתור הוספה צף) + 4 מסכים: **בית** (מד ראשי, Snapshot חודשי, "מה דורש תשומת לב", פעילות אחרונה), **תנועות** (חיפוש ומסנן פעיל/ארכיון — חזותיים בלבד, ללא סינון נתונים בפועל, רשימה כרונולוגית מאוחדת), **תובנות** (3 כרטיסי Mock עם אייקון בולט, רקע עדין לפי טון, פס-הדגשה), **הגדרות** (רשימת קטגוריות Mock + אזור המפתחים עם מנגנון הבטיחות המקורי). חלונית "פעולות מהירות" (Quick Actions) נפתחת/נסגרת חזותית מה-FAB, כפתוריה עדיין ללא שמירה בפועל.

**סבב ליטוש חזותי קצר** בוצע בהמשך: הרמת ה-FAB למרווח ברור מהניווט, הבלטת הלשונית הפעילה בניווט (pill רקע עדין), שיפור כרטיסי התובנות (אייקון תג לבן לפי `tone` הקיים, רקע צבעוני עדין, היררכיית טיפוגרפיה משופרת), וליטושי spacing/טיפוגרפיה נקודתיים.

**אימות שבוצע:** `node --check` על בלוק ה-`<script>` המדויק מתוך הקובץ בפועל — ללא שגיאות תחביר; אימות אוטומטי שכל קריאות `getElementById` (סטטיות ודינמיות: `screen-*`, `nav-*`, `filter-*`) תואמות ל-id-ים קיימים ב-HTML. **לא בוצעה עדיין בדיקת דפדפן מלאה (Edge headless)** — זו מתוכננת לשלב ה׳, לאחר חיבור ללוגיקה האמיתית בשלב ד׳.

**חשוב להדגיש:** זהו עדיין קובץ **Preview v2 נפרד ובלתי-תלוי** — `index.html` (המערכת הפעילה), `styles.css`, ו-`app.js` **לא נגעו כלל**, ואין שום שינוי בהתנהגות האפליקציה הקיימת שהמשתמש משתמש בה בפועל.

## שלב ג׳ של תוכנית המימוש — הושלם (30/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** שכבת "מנוע הסיפור" — תוספתית-בלבד, קוראת-בלבד, **ללא חיבור ל-UI, ל-localStorage, לנתונים אמיתיים, או ללוגיקה העסקית הקיימת**.

**מבני נתונים חדשים:** `mockItems` (11 פריטים, מחקים את צורת `items[]` האמיתית — כולל פריט אחד מאורכב בכוונה לבדיקת סינון), `mockCategoryConfig` (מחקה את `categoryConfig` האמיתי, 4 קטגוריות מובנות), `mockSnapshot` (ערכי הכנסות/הוצאות/יתרה קבועים). כל אלה **נפרדים לגמרי** מ-`MOCK_DATA` של שלב ב׳ שממשיך להזין את התצוגה ללא שינוי.

**7 פונקציות מלאות (Pure Functions בטוחות, על Mock בלבד):** `getFixedCreditCardTotals`, `getFixedBankVsCreditSplit`, `getTotalFixedCommitments`, `getMonthSnapshot`, `getRecentActivity`, `getLoansRemainingSummary`, `buildNarrative`. **חשוב:** לאחר תיקון שבוצע באותו שלב, `getMonthSnapshot()` היא **עטיפה טהורה בלבד** סביב `mockSnapshot` — היא **אינה מבצעת שום חישוב עסקי** (לא סכימה, לא סינון), כדי שלא תוכל לסתור בשום צורה את כללי היתרה הפנויה של המערכת האמיתית.

**4 Stubs מפורשים (ללא חישוב, ללא המצאת נתון):** `getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming`, `getForecastWarning`, `getDatedMonthOverMonthChange` — כולן מחזירות `null`, עם הערה בקוד שהמימוש האמיתי (`getBillingRange`/`parseDatesAndGetLeft`/`computeForecast`, מועתקות כלשונן) שייך לשלב ד׳ בלבד.

**אימות שבוצע:** סקריפט Node.js שמחלץ את בלוק ה-`<script>` המדויק מתוך הקובץ בפועל ומריץ אותו ב-VM עם `localStorage`/`document`/`confirm` מדומים. **38 בדיקות עברו** — סכימות, הדרת פריט מאורכב, `getMonthSnapshot` (כולל הוכחה שקריאה עם `items=[]`/`items=null` מחזירה תוצאה זהה — כלומר אין חישוב על items בכלל), `getRecentActivity`, `getLoansRemainingSummary`, כל 4 ה-Stubs (מחזירות `null`, לא זורקות שגיאה), `buildNarrative` (כולל ניסוח שונה ליתרה חיובית/שלילית ול"אין הלוואות"), ואימות שאף קריאה לא כתבה למפתח `localStorage` אמיתי כלשהו.

**אין עדיין חיבור ל-UI, ל-localStorage, לנתונים אמיתיים, או ללוגיקה העסקית הקיימת** — כל הפונקציות מוגדרות אך אינן נקראות מ-`renderMockUI()` או מכל handler קיים אחר.

## שלב ד׳ של תוכנית המימוש — הושלם, אומת ונשמר ב-commit `136200d` (30/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** חיבור מלא בין שכבת "מנוע הסיפור" (שלב ג׳) לבין לוגיקת החישוב האמיתית ונתוני ה-Preview ב-localStorage. כל 7 תתי-השלבים (ד׳.1–ד׳.7) הושלמו ואומתו.

- **ד׳.1 — העתקה מילולית**: `getBillingRange` ו-`parseDatesAndGetLeft` הועתקו כלשונן מ-`index.html`, ללא שינוי לוגי.
- **ד׳.2 — קריאה בטוחה מ-Preview**: נוספו `loadPreviewItems()`/`loadPreviewCategoryConfig()` — קוראות **אך ורק** מ-`PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY` (לעולם לא מהמפתחות האמיתיים), עם נפילה בטוחה למערך/אובייקט ריק בפני JSON פגום או מפתח חסר.
- **ד׳.3 — חיבור 6 פונקציות שלב ג׳ לנתונים אמיתיים**: `getFixedCreditCardTotals`, `getFixedBankVsCreditSplit`, `getTotalFixedCommitments`, `getMonthSnapshot`, `getRecentActivity`, `getLoansRemainingSummary` — עברו מחישוב על Mock קבוע לחישוב אמיתי על `items`/`categoryConfig` שנטענו מה-Preview, כולל תיקון המרת הוצאה קבועה שנתית לחודשית (`amount / 12`) בשלוש הפונקציות הראשונות, תואם בדיוק ל-`renderAll()`.
- **ד׳.4 — `buildPreviewNarrativeData()`**: עטיפה חדשה שקוראת ל-`buildNarrative(items, categoryConfig)` **פעם אחת בלבד** — נמנע חישוב כפול.
- **ד׳.5 — חיבור מסך הבית**: `renderHomeScreenFromRealData()` מעדכן מנתונים אמיתיים את `hero-amount`, `snapshot-income`, `snapshot-expenses`, `snapshot-balance`, `hero-narrative` ו-`recent-activity-list`. **`hero-status` נשאר במכוון לא מחובר** (מציג טקסט קבוע "תמונת מצב חודשית") — אין שדה נרטיב מתאים בלי המצאת לוגיקה חדשה.
- **ד׳.6 — 4 ה-Stubs ומסך התובנות**: `getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming`, `getForecastWarning`, `getDatedMonthOverMonthChange` מומשו במלואן על בסיס `computeForecast`/`getBillingRange`/`isBillingActiveInMonth` (מועתקות כלשונן). `renderInsightsScreenFromRealData()` מציג כרטיסי תובנה אמיתיים (חיובי כרטיס אשראי, יתרת הלוואות, ואזהרת תחזית) — כרטיס "אזהרת תחזית" **מושמט ולא מוצג** כשאין אזהרה, בהיעדר מצב-ריק מוגדר בעיצוב.
  - **תיקון באג (audit, באותו שלב)**: נמצא ותוקן ערבוב בין `items` הגלובלי לבין הפרמטר שהתקבל בפונקציות `getBiggestUpcomingCharge`/`getProjectedBalanceAfterUpcoming`/`getForecastWarning` — הפרמטר שונה ל-`sourceItems` ומועבר במפורש ל-`computeForecast()`, כך שהתחזית תמיד מחושבת על אותו מערך שהפונקציה נקראה איתו, ולא על עירוב עם הגלובלי.
- **ד׳.7 — בדיקות אינטגרציה מקיפות**: דוח בדיקות מול `index.html` (349 בדיקות, 342 עברו) הסתיים ב-**PASS**, עם ההערות הידועות בלבד (ראו "נקודות פתוחות" למטה).

**מנגנון בטיחות `PREVIEW_*`**: נבדק מחדש לאורך כל שלב ד׳ ונשאר מבודד לחלוטין ממפתחות הנתונים האמיתיים (`family_finance_data`/`family_finance_cat_config`) — אין אף קריאת כתיבה חדשה אליהם או קריאה מהם בשלב ד׳; כל קריאות ה-`setItem`/`removeItem` בקובץ שייכות אך ורק למנגנון ה-seed/איפוס המקורי משלב א׳.

**`index.html`** (המערכת הפעילה) — **לא נגע בו כלל** לאורך שלב ד׳.

**סטטוס**: הושלם, אומת ונשמר ב-commit `136200d`.

## מיפוי שלב ה׳ — הושלם (30/07/2026)
לפני תחילת ביצוע, בוצע סבב מיפוי (ללא קוד) של מה שנותר פתוח ב-`index-preview-v2.html` לאחר שלב ד׳. נמצא: מסכי הגדרות/תנועות ו-attention-list ("מה דורש תשומת לב") עדיין Mock מלא; פילטר פעיל/ארכיון קוסמטי בלבד; חיפוש מנוטרל במפורש; Quick Actions (FAB) קיים ויזואלית אך לא שומר כלום; אין שום פעולת כתיבה (`addNewItem`/`saveInlineEdit`/`archiveItem`/`deletePermanently`) מחוברת. הוחלט לפצל את שלב ה׳ לרצף תתי-שלבים קטנים ונמוכי-סיכון (ה׳.1–ה׳.4), כאשר חיבור פעולות כתיבה נשאר מחוץ לרצף — פיתוח חדש שטרם אושר. ראו TODO.md לרשימה המלאה.

## שלב ה׳.1 — חיבור מסך ההגדרות לנתוני קטגוריות אמיתיים — הושלם (30/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** חיבור מסך ההגדרות (רשימת קטגוריות) לנתונים אמיתיים — **תצוגה בלבד**, ללא הוספה/עריכה/מחיקה/שמירה.

- **מה השתנה**: הוסרה הלולאה שמילאה את `category-list` מ-`MOCK_DATA.categories` בתוך `renderMockUI()` — `renderMockUI()` אינו קורא יותר מ-`MOCK_DATA.categories` בשום צורה. נוספה פונקציה חדשה `renderSettingsScreenFromRealData()` שמציגה את `categoryConfig` האמיתי (נטען כבר בשלב ד׳.2 דרך `loadPreviewCategoryConfig()`, קריאה-בלבד מ-`PREVIEW_CONFIG_KEY`).
- **מבנה הנתונים**: `categoryConfig` הוא אובייקט `key → {label, baseType}`; הרינדור עובר על `Object.keys(categoryConfig)` ומציג את `label` (כולל האמוג׳י שכבר מוטמע בו, כמו באפליקציה האמיתית).
- **טיפול בטוח במצבי קצה**: `categoryConfig` ריק (`{}`) → רשימה ריקה, ללא קריסה; `null`/`undefined` → מוגן ע"י `|| {}`; קטגוריה עם `label` חסר/לא-מחרוזתי → נופלת לשם המפתח עצמו.
- **בדיקות**: `node --check` — תחביר תקין. 20 בדיקות VM/Node (קונפיגורציה רגילה + קטגוריה מותאמת אישית, קונפיגורציה ריקה, מפתח אמיתי חסר, ערכים פגומים, `null` מאולץ) — **20/20 עברו**, כולן אימתו **היעדר כתיבה/מחיקה** מהמפתחות האמיתיים (`family_finance_data`/`family_finance_cat_config`) — כל כתיבה שקרתה הייתה אך ורק למפתחות `PREVIEW_*` דרך מנגנון ה-seed הקיים משלב א׳. בדיקת דפדפן ממוקדת (Edge headless, `--dump-dom`) אישרה טעינה ללא שגיאות JS/Console, עם קטגוריות אמיתיות מוצגות ב-DOM בפועל (כולל תרחיש קטגוריה מותאמת אישית שהוכיח חיבור לנתונים אמיתיים ולא ל-Mock).
- **`index.html`** — לא נגע בו כלל.

## שלב ה׳.2 — חיבור מסך התנועות לנתונים אמיתיים — הושלם (30/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** חיבור מסך התנועות (רשימת התנועות + מסנן פעיל/ארכיון) לנתונים אמיתיים — **תצוגה בלבד**, ללא הוספה/עריכה/ארכוב/שחזור/מחיקה/שמירה.

- **מה השתנה**: הוסרה השורה שמילאה את `transactions-list` מ-`MOCK_DATA.transactions` בתוך `renderMockUI()` — `renderMockUI()` אינו קורא יותר מ-`MOCK_DATA.transactions` בשום צורה. נוספה פונקציה חדשה `renderTransactionsScreenFromRealData(filterName)` שמציגה את `items` האמיתיים (נטענו כבר בשלב ד׳.2 דרך `loadPreviewItems()`, קריאה-בלבד מ-`PREVIEW_DATA_KEY`), תוך שימוש חוזר ללא שינוי ב-`mapItemToHomeTxRow()`/`renderTxList()` (שלב ד׳.5).
- **מסנן פעיל/ארכיון**: הפך מקוסמטי-בלבד (רק החלפת מחלקת CSS) לפונקציונלי בפועל — `setTxFilter()` קורא כעת גם ל-`renderTransactionsScreenFromRealData(name)`. הסינון לפי `item.isArchived`, **זהה** ללוגיקת `renderAll()` ב-`index.html` (`(currentView === 'active' && !item.isArchived) || (currentView === 'archived' && item.isArchived)`); פריט עם `isArchived` חסר נחשב פעיל.
- **סדר התנועות**: מיון לפי `item.id` (מספרי, `Date.now()` בעת יצירה ב-`index.html`) בסדר יורד — החדש ביותר קודם. נבחר `id` (ולא `start`) כי הוא קיים בכל סוג פריט, בעוד `start` לא קיים כלל ב-`income`/`fixed`.
- **חיפוש**: נשאר `disabled` בקוד ה-HTML, ללא כל הפעלה — לא נגע בו.
- **תיקון Invalid Date (אותו שלב)**: `mapItemToHomeTxRow()` תוקנה כך שתאריך (`item.start`) שחסר, ריק, או לא ניתן לפענוח לתאריך תקין (`isNaN(getTime())`) — מוצג כ-`-`, לעולם לא כמחרוזת `"Invalid Date"` הגולמית של JS. תיקון זה חל גם על "פעילות אחרונה" במסך הבית (משתמש באותה פונקציה).
- **בדיקות**: `node --check` — תחביר תקין. **43 בדיקות VM/Node** (רשימה מעורבת פעיל+ארכיון, מעבר דו-כיווני בין הפילטרים, מערך ריק תחת שני הפילטרים, פריטים עם שדות חלקיים/`id` פגום, בידוד `PREVIEW_*` מלא) + **17 בדיקות VM/Node ממוקדות לתיקון התאריך** (start תקין/חסר/ריק/פגום, ללא "Invalid Date", סדר וסינון ללא שינוי) — **60/60 עברו**. בדיקת Edge headless אמיתית (לא רק פרופיל ריק): shim זמני מילא נתוני בדיקה ישירות למפתחות `PREVIEW_*` בלבד (לא במפתחות האמיתיים) לפני טעינת הדף, ואישרה בפועל ב-DOM: תאריך תקין מוצג בפורמט `he-IL`, ושלושת תרחישי התאריך הבעייתיים (חסר/ריק/פגום) מוצגים כ-`-` — ללא "Invalid Date" בתצוגה, ללא שגיאות Console.
- **`index.html`** — לא נגע בו כלל.
- **גישה למפתחות**: לא נוספה שום גישה חדשה למפתחות הנתונים האמיתיים (`family_finance_data`/`family_finance_cat_config`) — קריאה בלבד מ-`PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY`, כמו בכל שלב ה׳ עד כה.

## שלב ה׳.3 — חיבור "מה דורש תשומת לב" לנתונים אמיתיים — הושלם (30/07/2026)
נבנה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** חיבור רשימת "מה דורש תשומת לב" (`attention-list`) במסך הבית לנתונים אמיתיים — **תצוגה בלבד**.

- **הכרעת היקף (לפני כתיבת קוד)**: נמצאה אי-בהירות מהותית — `index.html` אינו מכיל שום מושג של "חיובים קרובים" ברשימה מרובה כפי שה-Mock הציג (3 פריטים, ניסוח יומי "בעוד X ימים"). הוצגו למשתמש 3 אפשרויות; **נבחרה האפשרות המינימלית**: שימוש רק בשתי הפונקציות הקיימות והמאומתות משלב ד׳.6, **ללא כל לוגיקה עסקית חדשה**, גם במחיר רשימה מוגבלת ל-0–2 כרטיסים.
- **מה השתנה**: הוסרה הלולאה שמילאה את `attention-list` מ-`MOCK_DATA.attention` בתוך `renderMockUI()` — `renderMockUI()` אינו קורא יותר מ-`MOCK_DATA.attention` בשום צורה. נוספה פונקציה חדשה `renderAttentionListFromRealData()`.
- **מקור הנתונים**: אך ורק `getBiggestUpcomingCharge(items)` ו-`getForecastWarning(items)` (שתיהן קיימות, מאומתות, מבוססות `computeForecast`/`getBillingRange`/`isBillingActiveInMonth` המועתקות כלשונן מ-`index.html`) — לא נכתבה שום לוגיקה עסקית חדשה.
- **סדר ההצגה**: קבוע — קודם כרטיס החיוב הגדול הצפוי (אם `getBiggestUpcomingCharge()` מחזירה תוצאה), ואז כרטיס אזהרת תחזית (אם `getForecastWarning()` מחזירה תוצאה). הרשימה מוגבלת במכוון ל-0–2 כרטיסים, בהתאם להחלטת ההיקף.
- **מצב ריק**: כששני האותות `null` → `attention-list` נשאר ריק (`innerHTML = ''`), **ללא המצאת הודעת "הכל בסדר"** — אותה החלטת עיצוב שכבר אושרה ותועדה בשלב ד׳.6 לכרטיס "אזהרת תחזית" במסך התובנות, נשמרה לעקביות.
- **בדיקות**: `node --check` תקין; **27 בדיקות VM/Node** (תרחיש עם שני האותות פעילים, תרחיש ללא אותות, מערך ריק, פריטים חלקיים/פגומים) — כולן עברו ואימתו היעדר כתיבה/מחיקה למפתחות האמיתיים. בדיקת **Edge headless אמיתית** (shim ממלא נתוני בדיקה ישירות ל-`PREVIEW_*` בלבד) אישרה בפועל ב-DOM שני כרטיסים אמיתיים ("ביטוח רכב... — צפוי ב-יולי — ₪5,000" ו-"אזהרת תחזית — ב-יולי צפויה יתרה שלילית של ₪6000. — ₪6,000"), ללא שגיאות Console.
- **`index.html`** — לא נגע בו כלל. לא נוספה גישה חדשה למפתחות אמיתיים כלשהם.

## שלב ה׳.4 — בדיקת דפדפן מלאה של Preview v2 — הושלם (30/07/2026)
בוצעה בדיקת אינטגרציה מלאה של **[index-preview-v2.html](index-preview-v2.html)** לאחר השלמת כל שלבי א׳–ה׳.3 — **שלב בדיקות בלבד, ללא שום שינוי קוד**.

- **מתודולוגיה**: בניגוד לבדיקות הקודמות (`--dump-dom`, צילום סטטי), כאן בוצע חיבור **CDP אמיתי** (WebSocket ל-Edge headless) עם **קליקים אמיתיים** (`.click()` על כפתורי ניווט/פילטר — אותו נתיב handler שקליק משתמש עובר), האזנה רציפה ל-`Runtime.exceptionThrown`/`Runtime.consoleAPICalled` לאורך כל התרחיש, ו-hook על `Storage.prototype.setItem/removeItem` שהוזרק לפני כל טעינת דף לתיעוד כל כתיבה בפועל (לא רק מצב סופי).
- **3 תרחישי נתונים** (אותו טאב, ניווטים אמיתיים, נתוני בדיקה במפתחות `PREVIEW_*` בלבד): (A) נתונים עשירים — 7 פריטים מכל הסוגים, פריט בארכיון, קטגוריה מותאמת אישית; (B) יתרה בריאה — ללא אזהרת תחזית; (C) `items=[]`/`categoryConfig={}` — מצב ריק מלא.
- **תוצאה: 49/49 בדיקות עברו.** מסך הבית (hero/snapshot/narrative/פעילות אחרונה/attention-list — כולם נתונים אמיתיים), מסך התובנות (3 כרטיסים במצב עשיר, 2 כרטיסים במצב "ללא אזהרה" — ללא כרטיס מומצא), מסך התנועות (הצגה נכונה, **קליק אמיתי** על מסנן פעיל⇄ארכיון עובד בשני הכיוונים, שדה חיפוש נשאר `disabled`, אין "Invalid Date"), מסך ההגדרות (5 קטגוריות כולל מותאמת אישית), ומצב ריק מלא בכל המסכים (ללא קריסה).
- **בדיקות בטיחות כלליות**: לאורך כל 3 התרחישים — `family_finance_data`/`family_finance_cat_config` **מעולם לא** נכתבו/נמחקו/הופיעו ב-`localStorage`; כל כתיבה הייתה אך ורק למפתחות `PREVIEW_*`; **אפס** `Runtime.exceptionThrown`; **אפס** `console.error`/`console.warning`.
- **לא נמצא אף באג.** לא נמצא שימוש שיורי ב-`MOCK_DATA` בשום מסך. לא נמצאו בעיות UX חדשות (רק הערות ידועות/מתועדות מראש: `hero-status` קבוע במכוון, attention-list מוגבל ל-0–2 כרטיסים בהחלטת היקף מפורשת, כרטיס "אזהרת תחזית" מושמט ולא ריק-עם-טקסט כשאין אזהרה).
- **מסקנה**: Preview v2 **read-only** (ללא הוספה/עריכה/ארכוב/מחיקה — שלב נפרד, טרם אושר) מוכן להמשך פיתוח מבחינה טכנית-פונקציונלית: כל הצגת הנתונים מאומתת במלואה ב-VM/Node ובדפדפן אמיתי, כולל אינטראקציה.
- **`index.html`** — לא נגע בו כלל.
- **שארית סביבת בדיקה (לא חלק מהפרויקט)**: תיקיית `edge_profile.tmp/` (פרופיל Edge זמני שנוצר לצורך חיבור ה-CDP) נותרה בשורש הפרויקט, untracked, נעולה חלקית ע"י תהליך אנטי-וירוס (ESET) פעיל בזמן הניסיון למחיקתה. **לא מכילה שום תוכן מהפרויקט, אינה tracked ב-Git, ואינה משפיעה על שום קובץ**. הוחלט במפורש שלא לבצע פעולות מערכת (הרשאות מנהל, שינויי הרשאות, `.gitignore`) רק כדי להסירה — ניתן למחוק ידנית מ-File Explorer בכל עת.

## שלב ו׳ — סקירת עיצוב סופית (Design Review) — הושלם (30/07/2026)
בוצעה ביקורת UX/UI מלאה על 4 מסכי **[index-preview-v2.html](index-preview-v2.html)** — **שלב סקירה/בדיקות בלבד, ללא שום שינוי קוד**. מתודולוגיה: צילומי מסך אמיתיים דרך Edge/CDP עם נתוני בדיקה עשירים (8 פריטים מכל הסוגים, פריט בארכיון, קטגוריה מותאמת אישית) על כל 4 המסכים + חלונית Quick Actions, בהשוואה גם ל-`Design/dashboard_pulse/screen.png` (מקור ההשראה העיצובית המתועד).

**10 ממצאים** דורגו לפי חומרה/עלות. המרכזיים: **(Critical, עלות קטנה)** כפתורי Quick Actions נראים פעילים לגמרי אך אינם מבצעים דבר — בדיוק "פעולה שנראית לחיצה אך אינה פעילה"; **(Medium-High, עלות קטנה)** סכומים שליליים מוצגים בסדר הפוך (bug ב-bidi/RTL); **(Medium, עלות קטנה)** חוסר עקביות בפורמט מספרים בין `insight-value` למשפטי הנרטיב; **(Medium, עלות קטנה)** כותרת המשנה עדיין טוענת "נתוני Mock בלבד" — כבר לא נכון; **(Medium, עלות קטנה)** FAB חופף ויזואלית לתוכן; **(High target-fit, עלות גדולה)** "מד ראשי" הוא כרטיס טקסט שטוח, לא גאוג׳ ויזואלי כפי שהמטאפורה/ה-mockup המקורי מרמזים; **(Medium, עלות בינונית)** status-pill סטטי, לא נושא סיגנל בריאות דינמי.

## שלב ו׳.1 — תיקוני UX ממוקדים — הושלם (30/07/2026)
תוקנו **חמשת הממצאים בעלי העלות הקטנה** מתוך סקירת שלב ו׳ (לא הגאוג׳/ה-status-pill — נדחו במפורש), הכל ב-**[index-preview-v2.html](index-preview-v2.html) בלבד**:
1. **Quick Actions**: נוסף class `disabled` (opacity מופחת, `cursor:not-allowed`) + תווית "(בקרוב)" + `title` tooltip לכל אחד מ-3 הכפתורים. `onclick` נשאר ללא שינוי (סוגר sheet בלבד, ללא לוגיקת כתיבה חדשה).
2. **RTL/Bidi**: `direction: ltr; unicode-bidi: isolate;` נוסף ל-`.tx-amount`.
3. **אחידות פורמט מספרים בתובנות**: `sentences.creditCardTotal`, `sentences.loansRemaining` (ב-`buildNarrative`), ו-`getForecastWarning().message` משתמשים כעת ב-`formatHomeCurrency()` במקום שרשור מספר גולמי (תופעת-לוואי חיובית: `getForecastWarning().message` משותף גם ל-attention-list במסך הבית, כך שהתיקון שיפר גם שם).
4. **כותרת Preview**: "נתוני Mock בלבד" → "נתוני Preview".
5. **FAB**: `padding-bottom` של `.app-shell` הוגדל מ-130px ל-176px (מבטיח שהשורה האחרונה ניתנת לגלילה מלאה מעבר ל-FAB, לא רק "לא חופפת בתצוגה הראשונית" — התנהגות FAB צפה סטנדרטית).

**תיקון המשך (אושר בנפרד, באותו שלב)**: בבדיקת תיקון #3 התגלו **2 מקומות נוספים** עם תופעה דומה — `hero-amount`/`snapshot-balance` ו-`insight-value` בכרטיס "אזהרת תחזית" הציגו `₪-13,050` (סמל לפני מינוס) ביתרה שלילית. **שורש הבעיה זוהה כבאג אמיתי בבניית המחרוזת** בתוך `formatHomeCurrency()` עצמה (לא רק bidi ויזואלי) — `'₪' + Math.round(n).toLocaleString()` מכניס את הסימן השלילי *אחרי* ה-₪ שכבר קדם לו. תוקן: הפונקציה בודקת סימן במפורש ובונה `-₪` + גודל מוחלט לערכים שליליים (ערכים חיוביים — זהה-ביט לקודם); נוסף גם `direction:ltr; unicode-bidi:isolate` ל-3 המקומות (`.hero-amount`, `.snapshot-card .value`, `.insight-value`) כשכבת הגנה נוספת. **גילוי נלווה**: `index.html` (המערכת האמיתית) משתמש **בדיוק באותה תבנית** (`'₪' + Math.round(net).toLocaleString()`) לחישוב `#net-balance` — סביר שאותו פגם קיים גם שם ליתרה שלילית. **לא טופל בכלל, ולא ייגע בו בשלב זה** — מתועד כפער ידוע, לטיפול עתידי במסגרת חיבור מבוקר ומאושר בנפרד ל-`index.html`.

**בדיקות מצטברות**: `node --check` תקין; **44 בדיקות VM/Node** (18 לחמשת התיקונים המקוריים + 26 לתיקון ההמשך, כולל בדיקות יחידה ל-`formatHomeCurrency` ובדיקות רגרסיה מפורשות שהערכים המספריים/הלוגיקה העסקית לא השתנו); **בדיקות Edge/CDP אמיתיות עם צילומי מסך לפני/אחרי** — כל 5+2 התיקונים אומתו הן טקסטואלית (`textContent`/`innerHTML`) והן ויזואלית. אפס Regression, אפס שגיאות Console, אפס `exceptionThrown`, בידוד `PREVIEW_*` נשמר במלואו.

**מסקנה**: Preview v2 (**read-only** — יכולות כתיבה עדיין לא מומשו) הגיע לרמת **Release Candidate מבחינת UX/UI**. **החלטת היקף סופית שאושרה**: לא להוסיף פוליש נוסף שאינו חוסם השלמת המוצר (גאוג׳ ויזואלי, status-pill דינמי) בשלב זה — נדחים לעתיד.

**`index.html`** — לא נגע בו כלל לאורך כל שלב ו׳ ושלב ו׳.1 (כולל תיקון ההמשך).

## שלב ז׳.1 — תשתית כתיבה ל-Preview v2 — הושלם (30/07/2026)
לפי אישור מפורש, ולאחר סבב מיפוי מלא של יכולות הכתיבה (הוספה/עריכה/ארכוב/שחזור/מחיקה) שבוצע ואושר בנפרד (ללא קוד): נוספה בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** תשתית כתיבה בסיסית — **בלי להפעיל אף יכולת כתיבה למשתמש**.

**מה נוסף:**
- `savePreviewItems()` — מקבילה מדויקת ל-`saveToLocalStorage()` ב-`index.html` (אותה צורת `JSON.stringify` + `try/catch`), כותבת **רק** ל-`PREVIEW_DATA_KEY`.
- `savePreviewCategoryConfig()` — מקבילה מדויקת ל-`saveConfigToLocalStorage()` ב-`index.html`, כותבת **רק** ל-`PREVIEW_CONFIG_KEY`.
- `getCurrentTxFilterName()` — פונקציית עזר קריאה-בלבד שקוראת מה-DOM איזה מסנן תנועות (פעיל/ארכיון) פעיל כרגע, ללא state חדש.
- `renderAllPreviewScreens()` — פונקציית אורקסטרציה שקוראת ברצף ל-5 פונקציות הרינדור הקיימות (`renderHomeScreenFromRealData`, `renderAttentionListFromRealData`, `renderInsightsScreenFromRealData`, `renderSettingsScreenFromRealData`, `renderTransactionsScreenFromRealData`) — אינה משנה את הלוגיקה הפנימית של אף אחת מהן.

**בטיחות**: כל 4 קריאות ה-`localStorage.setItem` בקובץ כולו (כולל מנגנון ה-seed משלב א׳) כותבות אך ורק למשתנים `PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY` — אומת ב-grep מפורש שלא קיימת אף קריאה למחרוזות `family_finance_data`/`family_finance_cat_config` מעבר להגדרת הקבועים `REAL_DATA_KEY`/`REAL_CONFIG_KEY` עצמם. **אף אחת מ-4 הפונקציות אינה נקראת אוטומטית** — לא בזמן טעינת הדף ולא מכל handler קיים — ולכן פתיחת Preview v2 נשארת **בפועל Read Only מבחינת המשתמש**, בדיוק כמו לפני השלב.

**בדיקות**: `node --check` תקין; **32 בדיקות VM/Node** (טעינת דף ללא כתיבה לאף מפתח, קריאה ידנית ל-2 פונקציות השמירה כותבת בדיוק פעם אחת ורק ל-`PREVIEW_*`, `renderAllPreviewScreens()` קוראת לכל 5 הפונקציות ומבצעת אפס כתיבות, ובדיקת אי-רגרסיה שהרינדור הקיים לא השתנה) — **32/32 עברו**. בדיקת **Edge/CDP אמיתית** עם נתוני בדיקה עשירים שהוזרקו ישירות ל-`PREVIEW_*` **וגם** ערכי-סמן שהוזרקו ל-`REAL_DATA_KEY`/`REAL_CONFIG_KEY` (כדי להוכיח בוודאות שהם לא נגעו) — אפס שגיאות Console, אפס `exceptionThrown`, כל 4 המסכים הציגו נתונים אמיתיים נכונים, וערכי-הסמן ב-`REAL_*` נשארו זהים ב-100% לאחר טעינת הדף.

**`index.html`** — לא נגע בו כלל.

**המשך**: נקודת ההמשך הבאה היא **שלב ז׳.2 — ארכוב ושחזור** (אחת מ-7 יכולות הכתיבה שמופו, בעלת הסיכון הנמוך ביותר מבין השלוש הראשונות שתוכננו) — **טרם התחיל, טרם אושר**.

## שלב ז׳.2 — תפריט פעולות שורה: ארכוב ושחזור — הושלם (30/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** תפריט פעולות (⋮) לשורת תנועה, **במסך התנועות בלבד** — מסך הבית ("פעילות אחרונה") ממשיך להשתמש ב-`renderTxList()` הקיים ללא כל שינוי.

- **UI**: כפתור ⋮ + dropdown עם פריט פעולה אחד ("העבר לארכיון" / "שחזר", תלוי `isArchived`), דרך `renderTxListWithActions()` חדשה — מקבילה מלאה ל-`renderTxList()` הקיים, לא נגעה בו.
- **פתיחה/סגירה**: `toggleRowMenu()`/`closeAllRowMenus()` + listener יחיד על `document` — תפריט אחד פתוח בכל רגע, נסגר בלחיצה מחוץ לו.
- **ארכוב/שחזור בפועל**: `handleRowMenuAction()` מחבר לתפריט את `archivePreviewItem()`/`unarchivePreviewItem()` (משלב ז׳.2.2), הקוראות אך ורק ל-`savePreviewItems()`/`renderAllPreviewScreens()` הקיימות.
- **בידוד**: כל כתיבה מוגבלת ל-`PREVIEW_DATA_KEY` בלבד; `family_finance_data`/`family_finance_cat_config` לא נגועים בשום שלב.
- **בדיקות**: `node --check` תקין; בדיקת **End-to-End מלאה בדפדפן אמיתי** (Edge/CDP, קליקים אמיתיים) — פתיחה/סגירה, מניעת ריבוי תפריטים, ארכוב פריט פעיל בקליק אמיתי, איתורו במסנן הארכיון, שחזורו בקליק אמיתי וחזרתו לרשימה הפעילה, מסך הבית ללא שום markup של תפריט וללא שינוי — **הכל עבר**, אפס שגיאות Console, אפס Exceptions.
- **`index.html`** — לא נגע בו כלל.

## שלב ז׳.3 — מחיקה סופית מתוך תפריט ⋮ — הושלם ומאומת, נשמר ב-commit (30/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** פריט שני לתפריט ⋮ הקיים (משלב ז׳.2), **במסך התנועות בלבד**: "מחק לצמיתות". מסך הבית ("פעילות אחרונה") ממשיך להשתמש ב-`renderTxList()` הקיים ללא שינוי — אין בו כל תפריט פעולות.

- **החלטת מוצר שאושרה**: `confirm()` פשוט של הדפדפן — ללא חלונית אישור מותאמת אישית.
- **מה נוסף**: `deletePreviewItem(id)` — מוצאת פריט לפי id, מסירה אותו **לחלוטין** ממערך `items` (`splice`, לא רק דגל `isArchived`), שומרת דרך `savePreviewItems()` הקיימת (`PREVIEW_DATA_KEY` בלבד) ומרנדרת מחדש דרך `renderAllPreviewScreens()` הקיימת; id חסר הוא no-op מלא (אין שמירה, אין רינדור). `handleDeleteMenuAction(actionBtn)` — הפונקציה המחוברת לכפתור החדש בתפריט: קוראת id מ-`data-item-id` (כמו `handleRowMenuAction` הקיים), מציגה `confirm()`, וקוראת ל-`deletePreviewItem()` רק אם המשתמש אישר; ביטול הוא no-op מלא (אין קריאה ל-`deletePreviewItem`, אין שמירה, אין רינדור, התפריט נשאר פתוח כפי שהיה). כפתור התפריט החדש מקבל class נוסף `tx-menu-item-danger` (צבע `--color-danger` הקיים) להבחנה ויזואלית מפעולת הארכוב/שחזור.
- **בטיחות**: אין נגיעה ב-`REAL_DATA_KEY`/`REAL_CONFIG_KEY`, אין נגיעה ב-`categoryConfig`, אין שינוי ב-`index.html`/`app.js`/`styles.css`, אין הוספת עריכה או הוספה.
- **בדיקות**: `node --check` תקין; `git diff --check` תקין (רק אזהרת CRLF/LF שגרתית של Git, לא שגיאה). **24 בדיקות VM/Node** (מחיקה תקינה כולל פריט מאורכב, id חסר הוא no-op מלא כולל אפס כתיבות נוספות, `categoryConfig` לא נגוע, `confirm()`-cancel הוא no-op מלא, `confirm()`-accept מוחק בדיוק את הפריט המבוקש, מקרה קצה `items=[]`, אימות markup שהתפריט קיים במסך תנועות ונעדר לחלוטין ממסך הבית) — **24/24 עברו**. **בדיקת Edge/CDP אמיתית** (שרת מקומי + Edge headless, WebSocket CDP, קליקים אמיתיים דרך `element.click()` כולל טיפול אמיתי בדיאלוג `confirm()` הילידי של הדפדפן דרך `Page.javascriptDialogOpening`/`Page.handleJavaScriptDialog`) — **20/20 עברו**: ביטול משאיר את הפריט בדיוק כפי שהיה (DOM + `localStorage`), אישור מוחק את הפריט המדויק מה-DOM ומ-`PREVIEW_DATA_KEY` בעוד שאר הפריטים נשארים, `family_finance_data`/`family_finance_cat_config` (עם ערכי-סמן שהוזרקו מראש) נשארו זהים ב-100% לאורך כל התרחיש, מסך הבית ללא כל markup של תפריט, id חסר (גם עם אישור) הוא no-op מלא, ואפס `exceptionThrown`/`console.error`/`console.warning`.
- **`index.html`** — לא נגע בו כלל.
- **שארית סביבת בדיקה**: פרופיל Edge הזמני לבדיקה זו נוצר בתיקיית scratchpad ייעודית (לא בשורש הפרויקט) והוסר בסיום — לא נוסף אף `edge_profile*.tmp/` חדש לשורש הפרויקט; 4 התיקיות הקיימות משלבים קודמים נותרו ללא שינוי.

## שלב ז׳.4 — עריכה Inline מתפריט ⋮ — הושלם ומאומת, נשמר ב-commit (30/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** פריט שלישי לתפריט ⋮ הקיים (משלבי ז׳.2/ז׳.3), **במסך התנועות בלבד**: "✏️ עריכה". מסך הבית ("פעילות אחרונה") ממשיך להשתמש ב-`renderTxList()` הקיים ללא שינוי — אין בו כל תפריט פעולות או עריכה.

- **החלטות מוצר שאושרו**: עריכה זמינה רק לפריטים פעילים (1א); לחיצה על "עריכה" מחליפה את השורה עצמה בטופס Inline עם כפתורי "שמור"/"ביטול" (2א); ולידציה נכשלת מציגה `alert()` פשוט כמו במערכת הקיימת, ללא רכיב שגיאה חדש (3א); ביטול מחזיר את השורה לתצוגה רגילה ללא שינוי/שמירה; מסנן התנועות הנוכחי (פעיל/ארכיון) נשמר גם אחרי ביטול וגם אחרי שמירה.
- **מה נוסף**: `previewEditingId` — משתנה מצב חדש (מקביל ל-`editingId` ב-`index.html`). `renderTxListWithActions()` הורחבה: שורה שה-id שלה שווה ל-`previewEditingId` מוחלפת כליל בטופס עריכה (הפריט המלא נשלף טרי מ-`items` לפי id — אותה מוסכמה כמו ב-`handleRowMenuAction()`), ופריט "✏️ עריכה" בתפריט מוצג רק כש-`!isArchived`. `buildPreviewEditFormHtml(item)` חדשה — העתקה מילולית של 5 ענפי הטופס מ-`index.html` (`dated`; `fixed` כולל שדה `cardLast4` מותנה; `variable`; `loan`; ברירת מחדל ל-`income`/שאר הסוגים — סכום בלבד), עם CSS ייעודי חדש (`.tx-edit-form`/`.tx-edit-group`/`.tx-edit-actions`/`.tx-edit-save`/`.tx-edit-cancel`) שאינו משתמש בשמות המחלקות הישנים של `index.html`. `togglePreviewCardLast4Field()` — מקבילה מדויקת ל-`toggleCardLast4Field()`. `savePreviewInlineEdit(id)` — העתקה מילולית של `saveInlineEdit()` (אותה ולידציה, אותם `alert()` מדויקים לכל סוג; בכשל — אין שמירה ו-`previewEditingId` לא מתאפס, כך שהטופס נשאר פתוח) שכותבת רק דרך `savePreviewItems()`/`renderAllPreviewScreens()` הקיימות. `cancelPreviewEdit()` ו-`handleEditMenuAction(actionBtn)` חדשות — ללא כתיבה, קוראות ל-`renderTransactionsScreenFromRealData(getCurrentTxFilterName())` כדי לשמר את המסנן הנוכחי.
- **בטיחות**: אין נגיעה ב-`REAL_DATA_KEY`/`REAL_CONFIG_KEY`, ב-`categoryConfig`, או ב-`index.html`/`app.js`/`styles.css`; לא נוספה יכולת הוספה; כל כתיבה דרך `savePreviewItems()` בלבד; id חסר/לא קיים הוא no-op מלא בכל אחת מ-3 הפונקציות החדשות.
- **בדיקות**: `node --check` ו-`git diff --check` תקינים. **26 בדיקות VM/Node** — לכל אחד מ-5 סוגי הפריטים: בניית הטופס הנכונה, שמירה תקינה, כשל ולידציה (alert יחיד, אין שמירה, עריכה נשארת פתוחה), ביטול (no-op מלא כולל אי-שינוי `previewEditingId` לפני הביטול), id חסר (no-op מלא), היעדר מוחלט של "עריכה" לפריט מאורכב, ובידוד מלא של `PREVIEW_*`/`categoryConfig` — **26/26 עברו**. **בדיקת Edge/CDP אמיתית** (שרת מקומי + Edge headless, WebSocket CDP, קליקים אמיתיים) — **18/18 עברו**: פתיחת עריכה בקליק אמיתי על ⋮ ואז "✏️ עריכה", טופס מוצג עם ערכים קיימים במדויק, שינוי שדה ושמירה בקליק אמיתי מעדכנים DOM ו-`PREVIEW_DATA_KEY` יחד, ביטול בקליק אמיתי משאיר את הנתון בדיוק כפי שהיה, ולידציה שגויה מציגה `alert()` אמיתי (דרך `Page.javascriptDialogOpening`/`Page.handleJavaScriptDialog`) והטופס נשאר פתוח, פריט מאורכב נטול "עריכה" בתפריט, מסך הבית נטול כל markup של תפריט/עריכה, `family_finance_data`/`family_finance_cat_config` (עם ערכי-סמן שהוזרקו) נשארו זהים ב-100%, אפס `console.error`/`console.warning`/`exceptionThrown`.
- **`index.html`** — לא נגע בו כלל.
- **שארית סביבת בדיקה**: פרופיל Edge הזמני לבדיקה זו נוצר בתיקיית scratchpad ייעודית (לא בשורש הפרויקט) והוסר בסיום — לא נוסף אף `edge_profile*.tmp/` חדש לשורש הפרויקט.

## שלב ז׳.5 — הוספת הכנסה/הוצאה/הלוואה מ-Quick Actions — הושלם, אומת ונשמר ב-commit `4c46b58` (30/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** יכולת הכתיבה האחרונה מתוך המיפוי המקורי: הוספת תנועה חדשה (הכנסה/הוצאה/הלוואה) דרך חלונית ה-Quick Actions הקיימת (⋯ עד כה מוצגת בלבד, ללא שמירה). מסך הבית ("פעילות אחרונה") לא נגע — היכולת פועלת אך ורק במסך התנועות.

- **החלטות מוצר שאושרו מראש**: 3 מסלולי הוספה נפרדים (הכנסה/הוצאה/הלוואה); בחירה ב-Quick Actions סוגרת את החלונית ופותחת טופס באזור ייעודי בראש מסך התנועות; "הוצאה" עוברת קודם דרך בורר סוג (קבוע/משתנה/מתוארך); רק טופס אחד פעיל בכל רגע (הוספה **או** עריכה Inline — לא שתיהן, נאכף בשני הכיוונים); ביטול הוא no-op מלא; לוגיקה/ולידציות זהות ל-`index.html`. **הבהרה נוספת שאושרה**: לאחר הוספה מוצלחת — איפוס `previewAddMode`/`previewAddCategoryKey`, מעבר מסך התנועות למסנן "פעיל" (`setTxFilter('active')`), ואז `renderAllPreviewScreens()`, בדיוק בסדר הזה; בכשל ולידציה — לא משתנה המסנן והטופס לא נסגר.
- **מה נוסף**: State חדש `previewAddMode`/`previewAddCategoryKey` (מקבילים ל-`previewEditingId`, לא נשמרים, מתאפסים בכל טעינה). `<div id="preview-add-form-area">` חדש בראש מסך התנועות. `startPreviewAdd(mode)` (מ-3 כפתורי ה-Quick Actions, שהוסרו מ-`disabled`), `chooseExpenseAddType(baseType)` (מ-3 כפתורי בורר ההוצאה), `cancelPreviewAdd()`, `buildPreviewAddFormHtml()`/`renderAddFormArea()`, `getCategoryKeysForBaseType()`/`getDefaultCategoryKeyForBaseType()` (בורר קטגוריה מוצג רק כשיש יותר מהתאמה אחת ל-baseType), `todayStr()` (מועתקת כלשונה מ-`index.html`), ו-`addPreviewItem()` — העתקה מילולית של `addNewItem()` (אותה בנייה של האובייקט, אותה ולידציה בדיוק לכל אחד מ-5 הענפים, אותם `alert()`). `renderAllPreviewScreens()` עודכנה לקרוא גם ל-`renderAddFormArea()` כך שהאזור נשאר מסונכרן אחרי כל פעולת כתיבה אחרת. `handleEditMenuAction()` עודכנה לאפס `previewAddMode`/`previewAddCategoryKey` בפתיחת עריכה, לאכיפת "טופס אחד בלבד" גם בכיוון ההפוך.
- **"הוצאה מתוארכת" (dated)**: אין מפתח מובנה ב-`categoryConfig` ל-`dated` (גם לא ב-`index.html`) — פריט כזה תמיד משויך לקטגוריה מותאמת אישית. לפי החלטה מאושרת: אם אין אף קטגוריה כזו, הכפתור מוצג עם `disabled` אמיתי (לא רק ויזואלי) + `title` רומז ("קודם צור קטגוריה מסוג 'חיוב חד-פעמי' במסך ההגדרות").
- **בטיחות**: כל כתיבה — רק דרך `savePreviewItems()` הקיימת → `PREVIEW_DATA_KEY` בלבד. אין קריאה/כתיבה חדשה ל-`REAL_DATA_KEY`/`REAL_CONFIG_KEY`, אין נגיעה ב-`categoryConfig` עצמו (לא נוצרת/משתנה קטגוריה). כל ה-id-ים החדשים בטופס ההוספה (`add-inc-title`, `add-fix-where` וכו') ייחודיים מול `index.html` ומול טופס העריכה Inline (`edit-*-<id>`).
- **בדיקות**: `node --check` ו-`git diff --check` תקינים. **58 בדיקות VM/Node** (הוספה מוצלחת/כושלת לכל אחד מ-5 הסוגים עם אותם `alert()` בדיוק, בורר "הוצאה" (קבוע/משתנה/מתוארך מבוטל כשאין קטגוריה, מופעל כשיש), ברירת מחדל/בורר קטגוריה כש-1/כש->1 קטגוריות תואמות, ביטול בכל שלב הוא no-op מלא, אכיפת "טופס אחד" בשני הכיוונים, בידוד מלא של `REAL_*`/`categoryConfig`) — **58/58 עברו**. **בדיקת Edge/CDP אמיתית** (שרת מקומי + Edge headless, WebSocket CDP, קליקים אמיתיים על FAB/Quick Actions/בורר-הוצאה/שמירה/ביטול, כולל `alert()` אמיתי על כשל ולידציה ואירוע `change` אמיתי לשדה כרטיס-אשראי) — **30/30 עברו**: הוספת הכנסה, הוספת הוצאה קבועה בכרטיס אשראי (עם כשל ולידציה אמיתי ואז תיקון), ביטול הלוואה בלי שריד, אכיפת "טופס אחד" כששני הכיוונים נבדקו, ואפס `console.error`/`console.warning`/`exceptionThrown` לאורך כל התרחיש. `family_finance_data`/`family_finance_cat_config` (עם ערכי-סמן) נשארו זהים ב-100% לאורך כל הבדיקה.
- **`index.html`** — לא נגע בו כלל.
- **שארית סביבת בדיקה**: פרופיל Edge הזמני לבדיקה זו נוצר בתיקיית scratchpad ייעודית (לא בשורש הפרויקט); לא נוסף אף `edge_profile*.tmp/` חדש לשורש הפרויקט (אומת ב-`git status` בסיום).

## שלב ח׳ — סבב בדיקות End-to-End מסכם ל-Preview v2 — הושלם (31/07/2026)
בוצע סבב בדיקות End-to-End מסכם ל-**[index-preview-v2.html](index-preview-v2.html) בלבד**, לפי תוכנית בדיקות מפורטת שאושרה מראש — **שלב בדיקות בלבד, ללא שום שינוי קוד**.

- **תוצאה: PASS מלא — 200/200 בדיקות עברו, אפס כשלים.**
  - **VM/Node: 132/132** — טעינה מול 3 דאטהסטים (עשיר/ריק/קצוות), פונקציות אגרגציה מול חישוב ייחוס עצמאי (לא מועתק מהקוד), כל 7 יכולות הכתיבה (הוספה לכל 5 הסוגים, עריכה Inline לכל 5 הסוגים, ארכוב, שחזור, מחיקה), ולידציות, ביטולים, אכיפת "טופס אחד בלבד" דו-כיוונית, ותרחישי קצה (תאריכים פגומים/חסרים, יתרה שלילית ופורמט הסימן, `categoryConfig` ריק).
  - **Edge/CDP: 68/68** — 3 תרחישי נתונים (עשיר/ריק/קצוות) בדפדפן Edge headless אמיתי, עם קליקים אמיתיים על כל הפעולות וטיפול אמיתי ב-`confirm()`/`alert()` דרך CDP.
- **כל 4 המסכים וכל 7 יכולות הכתיבה נבדקו יחד, ברצף אינטגרציה אחד** (בשונה משלבי ז׳ הקודמים, שבדקו כל יכולת בנפרד).
- **Persistence**: עבר — רענון דף אמיתי באמצע התרחיש שמר במדויק על מספר הפריטים ועל כל השינויים שנשמרו לפני הרענון (כולל מחיקה), ללא seed מחדש מהנתונים האמיתיים.
- **Console**: נקי לחלוטין — אפס `Runtime.exceptionThrown`, אפס `console.error`/`console.warning` לאורך כל הסבב.
- **בידוד `PREVIEW_*`**: עבר במלואו — `family_finance_data`/`family_finance_cat_config` נשארו זהים ב-100% לערכי-סמן שהוזרקו, בכל נקודת בדיקה; אימות נוסף (hook על `Storage.prototype.setItem/removeItem`) הראה שאף כתיבה/מחיקה לא כוונה אליהם.
- **לא נדרש תיקון קוד** — אפס כשלים נמצאו במהלך הסבב.
- **`index.html`, `app.js`, `styles.css`** — לא היו בהיקף הסבב ולא נבדקו.
- לא בוצעו Commit/Push/Merge/Deploy כחלק מהסבב עצמו (בדיקות בלבד).
- `edge_profile*.tmp/` — לא נוצרו חדשים (תשתית הבדיקה רצה מ-scratchpad חיצוני לפרויקט).

## המשימה האחרונה שבוצעה
ב-31/07/2026: הושלם סבב בדיקות End-to-End מסכם ל-Preview v2 (`index-preview-v2.html` בלבד) — 200/200 בדיקות עברו (132 VM/Node + 68 Edge/CDP), אפס כשלים, לא נדרש תיקון קוד. ראו סעיף "שלב ח׳" למעלה לפירוט מלא.

## Git Tag `preview-v2-baseline` (31/07/2026)
לאחר סיום שלב ח׳, נוצר Tag אנוטד `preview-v2-baseline` המצביע על commit `610fd62` — נקודת ביקורת יציבה ומאומתת של Preview v2 (200/200 בדיקות, read+write, כל 4 המסכים) לפני כל המשך פיתוח/שילוב.

## שלב 3 — מיפוי שילוב Preview v2 במערכת הראשית (מיפוי בלבד, ללא קוד) (31/07/2026)
בוצעה קריאה מלאה והשוואה בין `index.html`/`app.js`/`styles.css`/`index-preview-v2.html`: מיפוי פערים, כפילויות/סתירות, וסיכוני נתונים/UI לקראת שילוב עתידי. **ממצא מרכזי**: `app.js` מכיל לוגיקה עסקית **ישנה ושגויה** ביחס ל-`index.html` — אינו עותק תואם. הוצגו שתי אלטרנטיבות שילוב (א: inline יחיד כמו היום; ב: 3 קבצים אמיתיים — HTML+CSS+JS נפרדים) — **ללא בחירה בשלב זה**. הוחלט (עקרון קבוע): כל העתקת לוגיקה עסקית עתידית תילקח **מ-`index.html` בלבד**, לעולם לא מ-`app.js`. שלב מיפוי בלבד — לא בוצע שינוי קוד.

## שלב 3ב — תכנון הוספת יכולות חסרות ל-Preview v2: ניהול קטגוריות + תחזית (תכנון בלבד, ללא קוד) — הוחלט (31/07/2026)
נמצאו שתי יכולות שקיימות ב-`index.html` אך חסרות לגמרי ב-Preview v2: **ניהול קטגוריות** (הוספה/עריכה/מחיקה) ו-**תצוגת תחזית** (גרף+טבלה ל-6 חודשים). הוצג דוח חלופות מיקום/מבנה — **ההחלטות הבאות אושרו במפורש**:

1. **מיקום התחזית**: מסך **תובנות (Insights)** בלבד — לא מסך הבית, לא מסך חמישי נפרד.
2. **ניהול קטגוריות**: טפסים שנפתחים **בתוך הממשק** (inline/מודאלי) — **לא** שימוש ב-`prompt()` ילידי של הדפדפן.
3. **סדר עבודה מאושר** (5 תתי-שלבים, אחד בכל פעם עם עצירה לאישור):
   - **3ב.1** — לוגיקת ניהול קטגוריות (ללא UI)
   - **3ב.2** — UI ניהול קטגוריות
   - **3ב.3** — לוגיקת התחזית
   - **3ב.4** — שילוב התחזית במסך התובנות
   - **3ב.5** — Regression מלא
4. **כללי מחיקת קטגוריות** (מוצר): קטגוריות מובנות אינן ניתנות למחיקה; קטגוריה מותאמת אישית שיש לה תנועות (כולל בארכיון) אינה ניתנת למחיקה — מוצגת הודעה ברורה; **לא מוחקים תנועות אוטומטית** בשום מקרה.
5. **היקף מאושר מחדש**: `index.html`/`app.js`/`styles.css` **לא ישתנו כלל** בכל רצף 3ב; כל העבודה **רק** ב-`index-preview-v2.html`; כל כתיבה **רק** ל-`PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY`.

**סטטוס**: החלטות אושרו במפורש. שלב 3ב.1 בוצע ואומת (commit `d67b1db`), שלב 3ב.2 בוצע ואומת (commit `fb05be8`), שלב 3ב.3 הושלם ללא שינוי קוד, שלב 3ב.4 בוצע ואומת (commit `eeed2d0`), שלב 3ב.5 בוצע ואומת (ראו הסעיפים הבאים). **רצף 3ב כולו הושלם.**

## שלב 3ב.1 — לוגיקת ניהול קטגוריות (ללא UI) — הושלם ומאומת, נשמר ב-commit `d67b1db` (31/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** שכבת הלוגיקה לניהול קטגוריות — **ללא UI, ללא HTML/CSS, ללא event listeners** — לפי ההיקף המאושר.

- **מה נוסף**: `PREVIEW_BUILTIN_CATEGORY_KEYS`, `PREVIEW_CUSTOM_CATEGORY_TYPE_OPTIONS`, `PREVIEW_CATEGORY_DELETE_BLOCKED_REASON`, `previewEditingCategoryKey` (state, טרם בשימוש); `translateBaseType`/`pickEmojiForCategory` (מועתקות כלשונן מ-`index.html`); `isBuiltInPreviewCategoryKey`/`categoryHasPreviewItems` (בודקת גם תנועות בארכיון); `renamePreviewCategory`; `addPreviewCategory`; `deletePreviewCategory` (חוסמת קטגוריות מובנות ותנועות פעילות/בארכיון, לעולם לא מוחקת תנועות — סטייה מכוונת ומאושרת מהתנהגות `deleteCustomCategory()` ב-`index.html`, שמוחקת תנועות אוטומטית).
- **תיקון שני פערי הגנה שנמצאו בסקירת קוד לפני ה-commit** (אושרו במפורש לפני התיקון): (1) **ולידציית `baseType`** — `addPreviewCategory()` מקבלת רק ערכים מתוך `getAllowedPreviewCustomCategoryBaseTypes()` (נגזרת מ-`PREVIEW_CUSTOM_CATEGORY_TYPE_OPTIONS`, ללא רשימה כפולה); ערך לא חוקי → אין יצירה, אין שינוי `categoryConfig`, אין כתיבה ל-`localStorage`, מוחזר `null`. (2) **מניעת התנגשות מפתחות** — מפתח `custom_<timestamp>` שכבר קיים מקבל suffix מספרי (`custom_<timestamp>_1`, `_2`, ...) עד למציאת מפתח פנוי; לעולם לא דורס קטגוריה קיימת.
- **בטיחות**: אין שימוש ב-`REAL_DATA_KEY`/`REAL_CONFIG_KEY`; אין תלות ב-DOM/UI; `index.html`/`app.js`/`styles.css` לא נגעו בהם.
- **בדיקות**: `node --check` ו-`git diff --check` תקינים. **108/108 בדיקות VM/Node עברו** (כולל: כל בסיסי-ה-`baseType` המותרים, דחיית ערכים שרירותיים/ריקים ללא כל מוטציה, הוכחת שני מפתחות שונים בהתנגשות timestamp מאולצת, הוכחת אי-דריסת קטגוריה קיימת, חסימת מחיקה לקטגוריה מובנית ולקטגוריה עם תנועות פעילות/בארכיון עם `items` לא נוגע, ובידוד מלא של `REAL_*`).
- **המשך**: שלב 3ב.2 — UI ניהול קטגוריות. טרם התחיל.

## שלב 3ב.2 — החלטות מוצר (תכנון בלבד, טרם קוד) — אושרו (31/07/2026)
לפני תחילת המימוש, אושרו במפורש כללי העריכה/המחיקה וה-UI הבאים ל-**3ב.2 — UI ניהול קטגוריות**:

**כללי עריכת קטגוריות:**
- **קטגוריות מובנות**: ניתן לשנות שם. לא ניתן לשנות `baseType`. לא ניתן למחוק.
- **קטגוריות מותאמות אישית**: ניתן לשנות שם. שינוי `baseType` **אינו בהיקף 3ב.2**. ניתן למחוק **רק** כשאין לקטגוריה תנועות פעילות ואין תנועות בארכיון.

**כללי UI:**
- כפתור **עריכה** יוצג לכל הקטגוריות (מובנות ומותאמות כאחד).
- כפתור **מחיקה** יוצג **רק** לקטגוריות מותאמות אישית.
- **אין** שימוש ב-`prompt()`, `confirm()`, או טפסים ילידיים של הדפדפן.
- הוספה ועריכה יתבצעו בטפסים **inline** בתוך מסך ההגדרות.

**סטטוס**: החלטות אושרו במפורש. **טרם ניתן אישור לתחילת ביצוע (3ב.2)** — ממתין לאישור מפורש.

## שלב 3ב.3 — לוגיקת התחזית — הושלם ללא שינוי קוד (31/07/2026)
בבדיקת פתיחה לשלב 3ב.3 (מיפוי לפני מימוש) נמצא כי לוגיקת התחזית הנדרשת **כבר קיימת ומחוברת בפועל** ב-`index-preview-v2.html`, משלב מוקדם יותר בפרויקט שקדם לרצף 3ב (עותקים מדויקים מ-`index.html`: `computeForecast`, `getBillingRange`, `isBillingActiveInMonth`, ופונקציות נרטיב הבנויות עליהן בלבד — `getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming`, `getForecastWarning`, `getDatedMonthOverMonthChange`), קוראת אך ורק מ-`PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY` (ללא כתיבה), ומחוברת כבר בפועל למסך התובנות דרך `buildNarrative()`/`renderInsightsScreenFromRealData()`.

- **החלטה** (אושרה במפורש): לסמן את שלב 3ב.3 כמסופק, **ללא שינוי בקוד המוצר**.
- **אימות שבוצע**: השוואה אוטומטית של גוף הפונקציות `computeForecast`/`getBillingRange`/`isBillingActiveInMonth` מול `index.html` — זהות (למעט התאמת `itemsOverride` המתועדת בקוד עצמו, שאינה משנה התנהגות).
- **בדיקות**: נכתבה סוויטת בדיקות ייעודית וחדשה (VM/Node, זמנית בתיקיית scratchpad בלבד — **אינה חלק מה-repository**) שכיסתה לראשונה את לוגיקת התחזית ישירות: הכנסה, הוצאה קבועה (חודשי/שנתי/אשראי), "תשלומים שונים" מוחרג, הלוואה לפי טווח חיוב, חיוב חד-פעמי לפי חודש מדויק, החרגת פריטים בארכיון, מערך ריק, אי-השפעת קטגוריה מותאמת אישית על הסכום (baseType בלבד קובע), בידוד `itemsOverride`, ופונקציות הנרטיב הנגזרות — **38/38 עברו**. סוויטות 3ב.1 (111/111) ו-3ב.2 (112/112) הורצו מחדש ועברו ללא רגרסיה. `node --check` ו-`git diff --check` תקינים.
- **בטיחות**: `git diff --stat` ריק — אין קובץ פרויקט שהשתנה כלל בשלב זה.
- **המשך**: שלב 3ב.4 (בוצע, ראו סעיף ייעודי למטה) ואז 3ב.5 (בוצע, ראו סעיף ייעודי למטה).

## שלב 3ב.4 — שילוב UI התחזית במסך Insights — הושלם, אומת ונשמר ב-commit `eeed2d0` (31/07/2026)
נוסף בתוך **[index-preview-v2.html](index-preview-v2.html) בלבד** UI גרף+טבלת תחזית (6 חודשים קדימה) למסך התובנות (Insights), בהתבסס אך ורק על `computeForecast(items)` הקיים והמאומת (שלב 3ব.3) — ללא שינוי בלוגיקה העסקית עצמה.

- **החלטות מוצר שאושרו**: קופסת התחזית תמיד גלויה במסך Insights, ללא כפתור קיפול (שונה מ-`index.html`, שם היא בתוך אקורדיון בדשבורד); שם משתנה ה-state `previewForecastView` (תואם namespace `preview*` הקיים בקובץ, לא `forecastView` הבארה מ-`index.html`).
- **מה נוסף**: `niceRoundUp`, `barPath`, `attachForecastHandlers`, `renderForecast`, `toggleForecastView` — הועתקו כלשונן מ-`index.html` (גיאומטריה/סקיילינג/טולטיפ זהים), עם namespace חדש `preview-forecast-*` ל-DOM ids/classes (שונה מ-`forecast-*` של `index.html`) והתאמת שפת העיצוב לכרטיס הבהיר של קובץ זה (`--color-surface`/`--color-primary`/`--color-danger` במקום משתני הכהה של `index.html`). `renderForecast()` קורא ל-`computeForecast(items)` באופן מפורש (בניגוד לקריאה הגלובלית חסרת הארגומנט ב-`index.html`).
- **בטיחות**: קריאה בלבד מ-`items`/`categoryConfig` הטעונים כבר מ-`PREVIEW_*`; אין כתיבה חדשה לשום מפתח; `index.html`/`app.js`/`styles.css` לא נגעו בהם.
- **בדיקות**: `node --check` תקין. **34 בדיקות VM/Node** (רינדור אוטומטי בטעינה, מעבר גרף⇄טבלה דו-כיווני, עקביות סקאלה, מערך ריק, אי-תלות בקטגוריה מותאמת אישית) — **34/34 עברו**. `git diff --stat`: 183 שורות הוספה בלבד (0 מחיקה).
- **`index.html`** — לא נגע בו כלל.

## שלב 3ב.5 — Regression מלא וסגירת שלב 3ב — הושלם, אומת ונשמר ב-commits `46ad2eb`+`053a42b` (31/07/2026)
בוצע סבב Regression End-to-End מקיף ל-**[index-preview-v2.html](index-preview-v2.html) בלבד** לפני סגירת רצף 3ב — **שלב בדיקות (ותיקון באג אחד קטן שנמצא תוך כדי), ללא שינוי היקף**.

- **VM/Node**: כל 4 סוויטות 3ב.1–3ב.4 הורצו מחדש — **295/295 עברו** (111+112+38+34), אפס רגרסיה.
- **Edge/CDP (אמיתי, WebSocket ל-Edge headless, ללא Puppeteer — CDP גולמי דרך `fetch`/`WebSocket` המובנים ב-Node)**: תרחיש נתונים עשיר יחיד (7 פריטים מכל הסוגים כולל פריט בארכיון, 2 קטגוריות מותאמות אישית) שרץ ברצף אחד דרך כל התרחישים המבוקשים:
  - **ניהול קטגוריות**: שינוי שם לקטגוריה מותאמת אישית (קליק אמיתי + עדכון DOM+`localStorage`); ניסיון מחיקת קטגוריה עם תנועות — **נחסם** עם הודעת `alert()` אמיתית ("לא ניתן למחוק קטגוריה זו — קיימות לה תנועות..."), נתפסה ואושרה דרך `Page.javascriptDialogOpening`/`handleJavaScriptDialog`; הוספת קטגוריה חדשה (קליק אמיתי על טופס inline); מחיקת אותה קטגוריה החדשה (ריקה מתנועות) — **הצליחה** ללא `alert()`.
  - **Forecast**: 6 עמודות בגרף, קו-אפס, עמודה שלילית אחת (מחושבת עצמאית מראש, לא מועתקת מהקוד — ראו למטה); מעבר גרף⇄טבלה בקליק אמיתי; **ערכי הטבלה הושוו במלואם מול חישוב תחזית עצמאי** (נכתב מחדש ב-Node, לא הועתק מ-`computeForecast()`) — התאמה מלאה.
  - **עדכון Forecast אחרי שינוי נתונים אמיתי**: עריכת סכום פריט קיים (`fixed`, ⋮ → "✏️ עריכה", קליק אמיתי, שמירה) — הגרף/הטבלה התעדכנו אוטומטית (`renderAllPreviewScreens()`) לערכים חדשים, שהושוו שוב מול חישוב עצמאי מעודכן.
  - **שאר מסכי Preview v2**: מסנן פעיל/ארכיון במסך התנועות (קליק אמיתי, 6/1 שורות בהתאמה); מסך הבית (`hero-amount` השתנה בהתאם לאחר העריכה, ללא "Invalid Date"/"NaN").
  - **בטיחות**: hook על `Storage.prototype.setItem/removeItem` תיעד כל כתיבה בפועל לאורך כל הריצה — אפס כתיבות/מחיקות כוונו ל-`family_finance_data`/`family_finance_cat_config`; ערכי-סמן שהוזרקו מראש נשארו זהים ב-100% בסיום. אפס `Runtime.exceptionThrown`, אפס `console.error`/`console.warning`.
  - **תוצאה: 52/52 בדיקות Edge/CDP עברו** (לאחר תיקון הבאג שנמצא, ראו למטה) — **סה"כ 347/347 בדיקות (295 VM/Node + 52 Edge/CDP) עברו**.
- **באג אמיתי שנמצא ותוקן תוך כדי הסבב**: בבדיקה הראשונה (טרם התיקון) נכשלו 2 בדיקות — ערך התחזית השלילי בטבלה הוצג כ-`₪-9,200` במקום `-₪9,200`. אותרה הסיבה: `renderForecast()`'s טולטיפ+שורת טבלה (שהועתקו כלשונם מ-`index.html` בשלב 3ব.4) השתמשו בתבנית `'₪' + Math.round(net).toLocaleString()` — אותה תבנית שגויה שכבר תוקנה במקומות אחרים בקובץ זה (שלב ו׳.1, דרך `formatHomeCurrency()`) אך לא הוחלה על שני מקומות התחזית. **תוקן** ע"י שימוש ב-`formatHomeCurrency()` הקיים בשני המקומות (טולטיפ הגרף + תא הטבלה) — שינוי תצוגה בלבד (החלפת בניית מחרוזת), ללא נגיעה בחישוב/בלוגיקה העסקית. לאחר התיקון: כל 4 סוויטות VM/Node הורצו מחדש (295/295, אפס רגרסיה, כולל 3ব.4 עצמו) וכל סבב ה-Edge/CDP הורץ מחדש במלואו (52/52). `index.html` (שם אותה תבנית מקורית עדיין קיימת) **לא נגוע כלל**.
- **`git diff --stat`**: `index-preview-v2.html` — 2 שורות שונו (4 שורות בסה"כ, 2 הוספה+2 מחיקה — שינוי חד-שורתי כפול, לא הוספת קוד). `index.html`/`app.js`/`styles.css` — ריק (לא נגעו בהם כלל).
- **`edge_profile*.tmp/`**: לא נוצרו חדשים בשורש הפרויקט — פרופיל ה-Edge הזמני לבדיקה זו רץ מ-scratchpad חיצוני לפרויקט (כמו בכל שלב קודם).

## שלב 4.1 — מיפוי ותכנון שילוב Preview v2 במערכת הראשית — הושלם, ללא שינוי קוד (31/07/2026)
בוצע מיפוי השוואתי מלא בין `index.html`/`app.js`/`styles.css` לבין `index-preview-v2.html` לפי 10 אזורים (מבנה מסכים, State ומפתחות localStorage, טעינה/שמירה, לוגיקת תנועות, קטגוריות, Forecast, UI/CSS, פונקציות ייחודיות ל-Preview v2, פונקציות ישנות אסורות להעתקה, סיכונים) — **שלב תכנון/מיפוי בלבד, ללא שום שינוי קוד**.

- **ממצא מרכזי מאומת ב-diff ישיר (לא רק הערכה)**: `app.js` הוכח כתמונת מצב **ישנה ושגויה** מלפני שני שינויים עסקיים ב-`index.html` — חסר `toggleCardLast4Field()`/`cardLast4` לגמרי; `computeForecast`/`renderAll` ב-`app.js` כוללים `variable` בחישוב התזרים (הפוך מ-`index.html` הנוכחי) ואינם מחריגים `fixed` בכרטיס אשראי. **אסור לשמש כמקור העתקה בשום שלב עתידי** — עיקרון שהיה מתועד כבר קודם, כעת מאומת ישירות בקוד.
- `styles.css` נמצא כמעט תואם לחלוטין ל-CSS של `index.html` (פרט לבאג `.tabs` הידוע) — בניגוד ל-`app.js`.
- לוגיקת Forecast (`computeForecast`/`getBillingRange`/`isBillingActiveInMonth`/`parseDatesAndGetLeft`) זהה ביט-לביט בין `index.html` ל-Preview v2 — מוכנה לשימוש משותף ללא שינוי.
- כלל מחיקת קטגוריה שונה **במכוון** בין הקבצים (Preview v2 חוסם מחיקה עם תנועות; `index.html` מוחק תנועות אוטומטית) — דורש הכרעת מוצר מפורשת לפני כל cutover.
- **3 אסטרטגיות שילוב הוצגו ודורגו**: A (Cutover מלא), B (Dual-Mode/Feature Flag), C (Strangler הדרגתי לפי מסך). **הומלצה אסטרטגיה A**, מפוצלת ל-7 תתי-שלבים קטנים (4.2–4.8) עם Quality Gate ו-commit נפרד לכל אחד.
- **סטטוס**: המיפוי והאסטרטגיה הוצגו כדוח מלא. המשתמש אישר את תוכנית ההכנה של תתי-שלבים 4.2–4.5 **בלבד** — Cutover עצמו (4.6–4.8) ובחירת אסטרטגיה סופית (A/B/C) **עדיין לא אושרו**.
- **`index.html`/`app.js`/`styles.css`/`index-preview-v2.html`** — לא נגעו בהם כלל בשלב זה.

## שלב 4.2 — הגנות תאימות לנתונים ישנים — הושלם, ללא צורך בשינוי קוד (31/07/2026)
נבדק אם **[index-preview-v2.html](index-preview-v2.html)** מסוגל לטעון בבטחה נתוני `items`/`categoryConfig` אמיתיים וישנים/חלקיים — **שלב אימות/בדיקות בלבד; לא בוצע שום שינוי קוד**.

- **מיפוי שדות חסרים אפשריים**: `cardLast4` (מלפני הפיצ'ר), `where` כטקסט חופשי ישן (לפני select בנק/כרטיס), `notes`/`originalAmount`/`interest`/`day`/`total`/`start` חסרים ב-`loan`/`variable`/`fixed`, `period` חסר, `categoryConfig` חלקי (`baseType` חסר, פחות מ-4 מובנות), `displayCategory` תלוי (מצביע לקטגוריה שנמחקה/אינה קיימת), ו-JSON פגום בכל אחד משני מפתחות ה-Preview.
- **ממצא מרכזי, לאחר קריאת קוד שיטתית של כל אתר גישה לשדות אלה**: **דפוסי ההגנה הנדרשים כבר קיימים בפועל בקוד** — נבנו הדרגתית בשלבים קודמים (ד׳.2, E.1/E.2, G.2–G.5, 3ব.1) — קריאה בטוחה עם `||`/`&&` short-circuit בכל אתר שבו שדה עשוי להיות חסר; `try/catch` עם fallback בטוח (מערך/אובייקט ברירת מחדל) על JSON פגום ב-`loadPreviewItems()`/`loadPreviewCategoryConfig()`; **אין migration אוטומטי ואין כתיבה חזרה ל-`localStorage` בזמן טעינה/רינדור** — אומת ישירות (לא רק בקריאת קוד) בבדיקות חדשות.
- **נתונים מקוריים נשמרים ללא שינוי**: שדה חסר (למשל `cardLast4`) לא "מומצא"/נכתב לזיכרון או ל-`localStorage`; ערך `where` ישן/לא-מוכר לא מנורמל ולא נדרס — נשאר בדיוק כפי שהיה, עד שהמשתמש עצמו עורך ושומר את הפריט דרך ה-UI (בדיוק כמו ב-`index.html`).
- **בדיקות**: `node --check` תקין. נכתבו שני קבצי בדיקה חדשים ב-scratchpad בלבד (`test_stage_4_2_legacy_data.js`, `test_stage_4_2_cdp.js`) — **389/389 בדיקות עברו בסה"כ**:
  - **295 בדיקות רגרסיה קיימות** (3ব.1–3ב.4, הורצו מחדש ללא רגרסיה — טריוויאלי לאחר שלא בוצע שינוי קוד, אך אומת בפועל).
  - **81 בדיקות VM/Node חדשות לנתונים ישנים/חלקיים** — 8 תרחישים: `fixed` בלי `cardLast4`; `fixed` עם `where` חופשי ישן; פריטים עם שדות אופציונליים חסרים; `categoryConfig` חלקי + `displayCategory` תלוי; קטגוריה מותאמת קיימת עם תנועות; מערך ריק; JSON פגום ב-`PREVIEW_DATA_KEY`; JSON פגום/סוג שגוי ב-`PREVIEW_CONFIG_KEY`; ותרחיש-על משולב של כל הבעיות בבת אחת.
  - **13 בדיקות Edge/CDP חדשות** — תרחיש-על אמיתי בדפדפן Edge headless: טעינה מוצלחת, כל 4 המסכים מוצגים ללא קריסה מול נתונים חלקיים, מעבר גרף⇄טבלה בתחזית תקין, אפס `Runtime.exceptionThrown`, אפס `console.error`/`warning`.
- **בטיחות**: `family_finance_data`/`family_finance_cat_config` (`REAL_DATA_KEY`/`REAL_CONFIG_KEY`) נשארו זהים ב-100% לערכי-הסמן בכל 8 התרחישים ובתרחיש ה-Edge/CDP — אפס `setItem`/`removeItem` כוונו אליהם בכל בדיקה.
- **`git diff --stat`**: ריק — `index-preview-v2.html`, `index.html`, `app.js`, `styles.css` לא שונו כלל.
- **המשך**: שלב 4.3 — גיבוי נתוני משתמש אמיתי מתועד. טרם התחיל.

## שלב 4.3 — גיבוי נתוני משתמש אמיתי — הושלם כ"לא נדרש במצב הנוכחי" (31/07/2026)
לפני ביצוע גיבוי בפועל, נבדק אילו נתונים אמיתיים קיימים כרגע במחשב שבו מתבצעת העבודה — **שלב בדיקה/החלטה בלבד, ללא שום שינוי קוד**.

- **ממצא**: **אין כרגע נתונים אמיתיים** תחת `family_finance_data`/`family_finance_cat_config` במחשב זה. נמצאו רק מפתחות ה-Preview (`family_finance_preview_v2_data`/`family_finance_preview_v2_cat_config`).
- בטלפון של המשתמש קיימים נתוני Preview — **המשתמש אישר במפורש שאינם חשובים ואין צורך לגבות אותם**.
- **מסקנה**: מאחר שאין כרגע נתוני משתמש אמיתיים במחשב שדורשים הגנה לפני מעבר, **לא נוצר קובץ גיבוי** — אין מה לגבות.
- **בטיחות**: לא בוצעה שום כתיבה, מחיקה, או שינוי ב-`localStorage` (לרבות `family_finance_preview_v2_data`/`_cat_config`) — בדיקת המצב הייתה קריאה בלבד. לא בוצע שום שינוי קוד. `index.html`/`index-preview-v2.html`/`app.js`/`styles.css` לא שונו.
- **חשוב לעתיד**: אם יתווספו נתונים אמיתיים תחת `family_finance_data`/`family_finance_cat_config` במחשב זה (או בכל מכשיר אחר שממנו יתבצע cutover) **לפני** כל מעבר בפועל (שלבים 4.6–4.8), **יש לבצע גיבוי מחודש** לפי אותה שיטה (ראו הוראות הגיבוי שהוצגו בתחילת שלב 4.3) — הממצא הזה תקף רק לרגע הבדיקה, לא באופן קבוע.
- **המשך**: שלב 4.4 — יצירת קובץ מועמד עם מפתחות אמיתיים. טרם התחיל.

## שלב 4.4 — יצירת קובץ מועמד עם מפתחות אמיתיים — הושלם, נשמר ב-commit `72a315a` (31/07/2026)
נוצר `index-v3-candidate.html` — עותק מבוקר של `index-preview-v2.html`, מכוון למפתחות האמיתיים (`family_finance_data`/`family_finance_cat_config` במקום `PREVIEW_*`/`REAL_*`), עם מנגנון ה-seed/איפוס **מוסר לחלוטין** (לא רק מנוטרל — אין יותר "נתונים אמיתיים" נפרדים מ"נתוני Preview" להעתיק/לאפס ביניהם). לא נכתבה/שונתה לוגיקה עסקית או סכימת `items`/`categoryConfig`. `index.html`/`index-preview-v2.html`/`app.js`/`styles.css` לא נגעו כלל. **359/359 בדיקות עברו** (322 VM/Node מותאמות + 37 Edge/CDP חדשות), כולן אימתו: אפס `PREVIEW_*` נותר בקובץ, כתיבה רק ל-2 המפתחות האמיתיים, CRUD/קטגוריות/Forecast תקינים.

## שלב 4.5 — אימות מלא לפני Decision Gate — הושלם, ללא שינוי קוד (31/07/2026)
סבב אימות נוסף ומקיף על `index-v3-candidate.html`, כולל **432 בדיקות** (367 VM/Node כולל 45 בדיקות round-trip חדשות שהוכיחו ששדות legacy/לא-מוכרים שורדים עריכה+רענון ללא אובדן, ו-65 Edge/CDP כולל בדיקת עדכון-אוטומטי של Forecast ורענון דפדפן אמיתי). לא נמצא באג באפליקציה. נמצא ותוקן באג בהרנס-הבדיקות בלבד (scratchpad, לא בריפו).

## שלב 4.6 — Decision Gate מוצרי — הושלם, ללא שינוי קוד (31/07/2026)
שתי החלטות מוצר אושרו: (1) מחיקת קטגוריה מותאמת עם תנועות (כולל בארכיון) **נחסמת** לחלוטין, עם הודעה ברורה — אומת בקוד שהקובץ המועמד כבר תואם (`deletePreviewCategory`/`categoryHasPreviewItems`). (2) קבצים ישנים (`index-preview-v2.html`/`app.js`/`styles.css`) **לא יימחקו** ב-Cutover — נשארים זמנית, ניקוי יהיה שלב עתידי נפרד. הוצגה תוכנית Cutover מדויקת לשלב 4.7.

## שלב 4.7 — Cutover בפועל — הושלם, נשמר ב-commit `c63a43e` (31/07/2026)
`index.html` הוחלף בתוכן `index-v3-candidate.html` (`git rm index.html` + `git mv index-v3-candidate.html index.html`); `index-v3-candidate.html` אינו קיים יותר כקובץ נפרד. **לא נוצר קובץ "legacy" נוסף** — לפי החלטת המשתמש, ה-`index.html` הישן משוחזר אך ורק דרך Git (ראו Tag למטה), כדי לא להשאיר בריפו קובץ HTML נוסף שיכול להיפתח ולכתוב לאותם מפתחות אמיתיים. **Tag מקומי `pre-preview-v2-cutover`** נוצר לפני השינוי, מצביע על `72a315a` (הקומיט שלפני ה-Cutover) — זהו מנגנון ה-Rollback. `index-preview-v2.html`/`app.js`/`styles.css` לא נגעו כלל. **367/367 VM/Node + 65/65 Edge/CDP עברו** מול ה-`index.html` החדש (סה"כ 432/432), כולל אפס `PREVIEW_*`, כתיבה רק ל-2 המפתחות האמיתיים, אפס Exceptions/console errors.

## שלב 4.8 — אימות סופי, תיעוד, וסגירת Version 1.0 — הושלם (31/07/2026)
סבב אימות סופי לאחר ה-Cutover: מצב Git אומת (branch `feature/cockpit-preview-v2`, HEAD `c63a43e`, Tag `pre-preview-v2-cutover`→`72a315a`, עץ עבודה נקי פרט ל-`edge_profile*.tmp/`). **432/432 בדיקות עברו שוב** (367 VM/Node + 65 Edge/CDP) מול ה-`index.html` הראשי, בפרופיל Edge חדש ונקי לחלוטין. אומת ישירות בקוד (grep): אפס הופעה של `PREVIEW_DATA_KEY`/`PREVIEW_CONFIG_KEY`/`REAL_DATA_KEY`/`REAL_CONFIG_KEY`/`family_finance_preview_v2_*`/מנגנון seed/reset ב-`index.html`. אומת מבנה סופי: `index-preview-v2.html`/`app.js`/`styles.css` ללא שינוי; אין `index-v2-legacy.html`; אין `index-v3-candidate.html`. **גרסה 1.0 נסגרה** — לא בוצע שום שינוי בלוגיקה עסקית או בעיצוב בשלב זה (לא נמצא צורך).

## נקודת ההמשך הנוכחית — Version 1.0 סגורה
**כל רצף ה-Cutover (שלבים 4.4–4.8) הושלם ואומת במלואו.** `index.html` הראשי הוא כעת ארכיטקטורת Preview v2 המלאה (Cockpit, 4 מסכים, CRUD מלא, ניהול קטגוריות, Forecast) עם מפתחות ה-localStorage האמיתיים. **Rollback**: Tag מקומי `pre-preview-v2-cutover` → commit `72a315a` (המצב שלפני ה-Cutover); `git revert c63a43e` הוא נתיב ה-Rollback המהיר. **אין נתוני משתמש אמיתיים** שנדרשו לגיבוי לפני המעבר (נקבע בשלב 4.3, נכון לתאריך הבדיקה — ראו שם לתזכורת). **אין migration ואין שינוי סכימה** — `items`/`categoryConfig` נשארו זהים במבנה לאורך כל הרצף.

**המשימה הבאה, מחוץ להיקף 1.0**: ניקוי הקבצים הישנים (`index-preview-v2.html`, `app.js`, `styles.css`) — רק לאחר תקופת אימות חי ואישור מפורש נפרד; טרם תוזמן.

נושאים פתוחים נוספים: (1) בדיקות עתידיות שנותרו על תרחישי משתמש אמיתיים בפועל (לא רק נתוני בדיקה) — ימתינו לנתונים אמיתיים כשיהיו, (2) שגיאת favicon.ico (לא דחוף), (3) **תוקן** — פער היתרה השלילית (`₪-X` במקום `-₪X`) תוקן כבר בתוך Preview v2 (שלב 3ב.5) ולכן קיים גם ב-`index.html` הראשי החדש מאז ה-Cutover, (4) תיקיות `edge_profile*.tmp/` (untracked, שאריות בדיקה, אינן חלק מהפרויקט; ניסיונות מחיקה חוזרים נחסמים ע"י נעילת אנטי-וירוס) — ניתן למחוק ידנית כשנוח, (5) גורל `index-preview.html` (v1, ישן משלב מוקדם יותר) ותיקיית `Design/` — נושאים ישנים, עדיין פתוחים ולא קשורים לרצף 1.0.

# Version 1.1 — רצף פיתוח ישיר על `index.html` (החל 01/08/2026)

לאחר סגירת Version 1.0, העבודה ממשיכה **ישירות על `index.html`** (לא על קובץ Preview נפרד) — כל שלב נבדק ונשמר ב-commit ישירות על הקובץ החי, בענף `feature/cockpit-preview-v2`. `index-preview-v2.html`/`app.js`/`styles.css` נשארים בריפו ללא שינוי (ניקוי עתידי, ראו TODO.md) ולא נגעו באף אחד מהשלבים הבאים.

## 01/08/2026 — 5 שלבים ראשונים, שוחזרו מניתוח Git ב-03/08/2026 (לא תועדו בזמן אמת)
**הערה מתודולוגית**: 5 הרשומות הבאות מבוססות על ניתוח ישיר של הודעות commit ו-diff מלא מול `index.html` בפועל (git show/git diff), שבוצע ב-03/08/2026 — לא על זיכרון שיחה, מכיוון שהתיעוד לא עודכן בזמן אמת בשיחות שביצעו את השלבים הללו בפועל. ייתכן ששמות/מספרי-שלב מדויקים שנעשה בהם שימוש בשיחות המקוריות שונים מהכותרות התיאוריות שנבחרו כאן — הן לא הומצאו, אלא מתארות את התוכן בהתבסס אך ורק על ראיה ישירה בקוד. כל התיאורים מאומתים 100% מול קוד בפועל.

### אריחי קטגוריה ניתנים לסידור מחדש במסך הבית (commit `1041092`)
עיצוב מסך הבית שונה: הוסר המשפט-נרטיב מהכרטיס הירוק העליון (`hero-narrative`), והוסר כרטיס ה"יתרה" מרשת ה-Snapshot (3 כרטיסים → 2: הכנסות/הוצאות בלבד; היתרה כבר מוצגת במד הראשי). נוסף `#category-tiles-grid` חדש, מוזן אך ורק מ-`categoryConfig` האמיתי דרך פונקציה חדשה `renderCategoryTilesFromRealData()`.
- **`getCategoryMonthlyTotals(items, categoryConfig)`** (חדשה) — מחשבת סה"כ חודשי לכל קטגוריה, באותה לוגיקה בדיוק כמו הסכימה הגלובלית הקיימת (fixed שנתי÷12, variable/loan לפי תשלום נוכחי כל עוד `parseDatesAndGetLeft().left > 0`, dated רק בחודש הנוכחי, ארכיון מוחרג), עם נפילה בטוחה `item.displayCategory → item.type`.
- **לחיצה על אריח מסננת** את מסך התנועות לפי קטגוריה (`filterTransactionsByCategory(key)`, משתנה חדש `currentCategoryFilterKey` — לא נשמר ב-localStorage), בנוסף (לא במקום) לפילטר פעיל/ארכיון הקיים; שבב סינון עם כפתור הסרה (✕) מעל הרשימה.
- **סידור מחדש בגרירה**: לחיצה ארוכה (450ms) + Pointer Events (ביטול אם התזוזה >10px לפני שהטיימר הופעל) — הסדר נשמר במפתח localStorage **חדש** `family_finance_category_tile_order`, נפרד לגמרי מ-`family_finance_data`/`family_finance_cat_config`. פונקציית תיאום (`reconcileCategoryTileOrder()`) מוסיפה קטגוריות חדשות בסוף ומשמיטה קטגוריות שנמחקו, כותבת רק כשבאמת השתנה משהו.
- **אין שינוי בסכימת `items`/`categoryConfig`** — רק מפתח localStorage נוסף לסדר תצוגה בלבד.

### שיפור זרימת קטגוריות + כפתור הוספה הקשרי (commit `8e8be7d`, הוחלף כליל בשלב הבא)
שינוי ביניים: רשומות קטגוריה במסך ההגדרות הפכו להתרחבות/כיווץ בלחיצה (`previewExpandedCategoryKey`), עם כפתורי עריכה/מחיקה מוצגים רק כשהרשומה מורחבת; כפתור ה-FAB הוסתר לגמרי במסך הבית והפך "הקשרי" — במסך תנועות מסונן לפי קטגוריה, הוא פותח ישירות טופס הוספה לאותה קטגוריה (`startPreviewAddForCategory(key)`) במקום תפריט "פעולות מהירות". **כל מנגנון ההתרחבות/כיווץ הוחלף לחלוטין בשלב הבא (עמודי קטגוריה) — מתועד כאן רק להשלמת ההיסטוריה, אינו קיים יותר בקוד הנוכחי.**

### עמודי קטגוריה + זרימות הוספה ספציפיות לסוג (commit `e7a0465`)
עיצוב סופי יותר: לחיצה על קטגוריה (מהבית או מהגדרות) מנווטת תמיד למסך תנועות מסונן — "עמוד קטגוריה". מנגנון ההתרחבות/כיווץ מהשלב הקודם הוסר כליל; רשימת הקטגוריות בהגדרות חזרה להיות תוויות-לחיצה בלבד ללא פעולות inline. **עריכה/מחיקה של קטגוריה עברו לגור בתוך שבב הסינון** (`renderCategoryFilterIndicator()` הורחבה ל-3 מצבים: רגיל+✏️/🗑️, טופס שינוי שם, אישור מחיקה) — הלוגיקה של `renamePreviewCategory()`/`deletePreviewCategory()` עצמה לא השתנתה, רק מיקום ה-UI. מחיקת הקטגוריה שנצפית כרגע מנווטת חזרה להגדרות. נוסף `isOnCategoryPage()`/`updateFabVisibility()` מרוכזים, נקראים גם מ-`showScreen()` וגם מתוך רינדור מחדש של מסך התנועות. אין שינוי בסכימת נתונים.

### אריחי סיכום חדשים במסך הבית (commit `a683a8e`)
נוספו 2 "אריחי-סטטיסטיקה" מחושבים (לא מבוססי-קטגוריה) לרשת אריחי הבית, במחלקת CSS נפרדת (`.stat-tile`, לא `.category-tile`) כדי שלא ייכנסו למנגנון הגרירה של השלב הראשון:
- **`getVariableRemainingBalance(items)`** — סכום יתרת "תשלומים שונים" פעילים (סכום × תשלומים שנותרו).
- **`getLoansBalanceSummary(items)`** — `{principal, total}` על כל ההלוואות הפעילות; `principal` מחושב באמורטיזציה נכונה (`Bₖ = P·(1+r)^k − amount·[(1+r)^k−1]/r`), עם נפילה לחישוב ליניארי כש-`interest` אפס/לא תקין, ותקרת בטיחות `min(principal, amount×left)` למקרה נתוני הלוואה שהוזנו ידנית וכוללים חוסר-עקביות. **תוקן תוך כדי אותו commit** באג בנוסחה המקורית שהתעלמה מ-`amount` בפועל של ההלוואה — מתועד כתיקון עצמי בהערות הקוד.
- כפתור מעבר קרן↔סה"כ להלוואות נשמר במפתח localStorage **חדש** `family_finance_loan_balance_view` (`'principal'`/`'total'`, ברירת מחדל `'total'`).
- אמוג'י הוסרו מאריחי הבית (הקטגוריה עצמה ב-`categoryConfig` לא שונתה) לטובת `getHomeTileDisplayLabel()` שמנקה אמוג'י מוביל ומחיל תוויות-תצוגה קבועות ל-variable/loan; קטגוריית "הכנסות" הוצאה לגמרי מרשת אריחי הבית (הכרטיס העליון כבר מציג הכנסה). שני אריחי הסטטיסטיקה עם handler ריק כרגע (`handleStatTileClick`), placeholder מכוון למסך פירוט עתידי.
- **מפתח localStorage חדש**: `family_finance_loan_balance_view`. אין שינוי בסכימת `items`/`categoryConfig`.

### כותרת מסך תנועות מציגה את שם הקטגוריה (commit `a107247`)
תיקון ממוקד: כשמסך התנועות מסונן לפי קטגוריה, כותרת המסך (`#transactions-screen-title`) מציגה את שם הקטגוריה המלא (כולל אמוג'י) במקום "תנועות" קבוע, דרך פונקציה חדשה `updateTransactionsScreenTitle()`. אין שינוי בסכימת נתונים.

## 02/08/2026 — שלב 4.0.2 — ימי כניסה/ירידה ברירת מחדל לקטגוריה (commit `223c730`)
**שדה חדש בסכימה**: `categoryConfig[key].defaultDayOfMonth` (מספר 1–31, אופציונלי — נשמר רק כשתקין). זוהי התוספת המהותית הראשונה לסכימת הנתונים ברצף Version 1.1.
- **`getClampedBillingDate(year, monthIndex, day)`** (חדשה) — "קליפ" יום לא-תקין (למשל 31 בפברואר) ליום האחרון בפועל של אותו חודש, במקום לתת ל-`Date` לגלוש לחודש הבא. הוחל על `getBillingRange()` — **תיקון באג רטרואקטיבי** בחישוב קיים, לא רק תוספת לוגיקה חדשה.
- **`resolveEffectiveDay(item)`** (חדשה) — שרשרת נפילה מרכזית: `item.day` תקין → אחרת `defaultDayOfMonth` של הקטגוריה → אחרת `1`. מחליפה כמה `1`ים קשיחים שהיו קיימים בעבר בחישובי הבית.
- **שדה `item.day` מורחב** — עד כה קיים רק בהלוואות; מעתה זמין גם לעריכה/הוספה של `fixed`/`variable`/`income` ("יום כניסה" עבור הכנסה), ברירת מחדל ל-`resolveEffectiveDay(item)`. **ללא migration** — פריטים ישנים ללא השדה נופלים לשרשרת הנפילה בזמן קריאה/רינדור בלבד, לא נכתבים מחדש.
- UI: טופסי הוספה/עריכה של קטגוריה מקבלים שדה אופציונלי "יום ירידה/כניסה ברירת מחדל" (מוסתר לגמרי לקטגוריות `dated`); ריק מנקה ברירת מחדל קיימת (לא נופל ל-1).

## 02/08/2026 — שלב 4.0.2.1 — פתיחת עריכת תנועה בלחיצה על השורה (commit `b74ccdc`)
פריט "✏️ עריכה" הוסר מתפריט ⋮ של שורת תנועה; כל השורה (לפריטים פעילים בלבד) הפכה לבר-לחיצה: `class="tx-row tx-row-clickable"`, `tabindex="0"`, `role="button"`, `aria-label`, `onclick="handleTxRowClick(...)"`/`onkeydown="handleTxRowKeydown(...)"` (Enter/Space מפעילים, עם `preventDefault()` למניעת גלילה מ-Space). `handleTxRowClick()` יוצא ללא פעולה אם הלחיצה מקורה בכפתור ⋮ או בתוך התפריט הנפתח (`closest()`), כך שארכוב/שחזור/מחיקה/תפריט ממשיכים לעבוד ללא שינוי. אין שינוי בסכימת נתונים.

## 02/08/2026 — שלב 4.0.2.2 — ארכוב אוטומטי להתחייבויות שהסתיימו (commit `197535f`)
**שדות חדשים בסכימה**: `item.archiveReason` (`'completed'` לארכוב אוטומטי / `'manual'` לארכוב ידני) ו-`item.archivedAt` (תאריך YYYY-MM-DD). ארכוב ידני (`archivePreviewItem()`) עודכן באותו commit להגדיר גם הוא את שני השדות, לשם עקביות; שחזור (`unarchivePreviewItem()`) מוחק את שניהם, ומאפשר לפריט משוחזר להיבדק שוב בסבב עתידי. **ללא migration** — פריטים בארכיון מלפני השלב הזה נשארים בלי השדות.
- **`getAutoArchiveCandidates()`** (חדשה) — רק `loan`/`variable` פעילים, ללא `archiveReason` קיים, עם `start`+`total` תקינים, `parseDatesAndGetLeft().left === 0` וגם היום **אחרי** (לא ביום עצמו) תאריך החיוב האחרון בפועל. משתמשת מחדש ב-`getBillingRange`/`parseDatesAndGetLeft`/`resolveEffectiveDay` הקיימות, ללא לוגיקת תאריכים חדשה.
- **`runAutoArchiveSweep(items)`** — רץ פעם אחת בכל טעינת דף (מיד אחרי טעינת `items`/`categoryConfig`, לפני רינדור ראשון), שומר דרך `savePreviewItems()` הקיים (`family_finance_data` בלבד — אין מפתח חדש). אם נמצאו מועמדים: `showAutoArchiveToast(count)` — טוסט לא-חוסם חדש (`role="status" aria-live="polite"`, נעלם לבד אחרי 4 שניות), במפורש **ללא** `alert()`/`confirm()`.
- אין מפתח localStorage חדש — הכל בתוך `family_finance_data`.

## 03/08/2026 — שלב 4.0.3 — הפרדת "קטגוריות" מ"הגדרות" ובניית מסך הגדרות מלא (נשמר ב-commit `251b3e3`)
נבנה מסך הגדרות עצמאי חדש; המסך הישן (ניהול קטגוריות) שינה שם ל"קטגוריות" ללא שינוי לוגי. ניווט תחתון עבר מ-4 ל-5 טאבים: בית | תנועות | תובנות | קטגוריות | הגדרות.

**7 תת-מערכות חדשות במסך ההגדרות** (`SETTINGS_KEY` = `family_finance_settings`, `ACTIVITY_LOG_KEY` = `family_finance_activity_log`, נספרות עד `ACTIVITY_LOG_MAX = 200`, `APP_VERSION = '1.1.0 Preview'`):
1. **אבטחה** — PIN לפתיחת האפליקציה (כבוי כברירת מחדל), הגדרה/שינוי/הסרה, hash בלבד (SHA-256 דרך `crypto.subtle.digest`, לעולם לא ה-PIN עצמו), "שכחתי PIN" עם מילת אישור מדויקת "איפוס" (מבטלת רק PIN, לא נוגעת בנתונים פיננסיים); Auto-lock (כבוי כברירת מחדל, אפשרויות מיד/1/5/15/30 דקות) לפי `visibilitychange`.
2. **ביומטריה** — הוחלט במפורש שלא לממש/להציג בשום צורה (לא "בקרוב", לא toggle מנוטרל) — הושמטה כליל.
3. **מראה** — ערכת נושא בהיר/כהה/מערכת; צבע ראשי מתוך פלטה קבועה של 6 (ירוק/כחול/סגול/טורקיז/כתום/אפור כהה, ללא בורר צבע חופשי); גודל גופן קטן/רגיל/גדול (`document.body.style.zoom`).
4. **התראות** — בתוך האפליקציה בלבד (לא Push מערכת), 3 סוגים בדיוק: תשלום/הכנסה צפויים מחר, התחייבות שהסתיימה — כל אחד עם toggle נפרד.
5. **נתונים** — גיבוי JSON כולל את כל מפתחות `family_finance_*` (כולל יומן פעילות); שחזור עם ולידציה מלאה לפני כל כתיבה (קובץ פגום = אפס שינוי), תקציר למשתמש ואישור מפורש; ייצוא CSV (UTF-8+BOM, עמודות קבועות); איפוס מלא עם מסך אישור ייעודי ומילת אישור "איפוס".
6. **אפשרויות ניסיוניות** — תשתית ריקה בלבד, טקסט "אין כרגע אפשרויות ניסיוניות פעילות." — ללא toggles מדומים.
7. **אודות** + **יומן פעילות** — גרסה, "מה חדש", ורשימת 9 סוגי פעולה מתועדים (`category_created`/`renamed`/`deleted`, `default_day_changed`, `auto_archive`, `manual_archive`, `restore`, `backup`, `data_restore`) — לעולם לא PIN/hash/סודות.

**מפתחות localStorage חדשים**: `family_finance_settings`, `family_finance_activity_log`.

**בדיקות**: 45/45 VM/Node לוגיקה + 58/58 Edge/CDP Desktop (כולל תיקון הרנס-בדיקות: `fetch()` הוחלף ב-`curl`, נוסף auto-accept ל-`alert()` דרך `Page.javascriptDialogOpening`) + 9/9 Edge/CDP Mobile (390×844) + רגרסיה מלאה של שלבי 4.0.2/4.0.2.1/4.0.2.2 (Desktop+Mobile, אחרי עדכון selector אחד ב-2 סקריפטים ישנים שציפו למסך הישן תחת `showScreen('settings')` — כעת `showScreen('categories')`, שינוי צפוי ומאושר, לא רגרסיה באפליקציה). לא נמצא באג באפליקציה.

**Git**: נשמר ב-commit `251b3e3`, נדחף ל-`feature/cockpit-preview-v2`. לא בוצע merge ל-main, לא בוצע deploy ידני.

**המשך**: ממתין לבדיקה ידנית בנייד מהמשתמש; השלב הבא ב-Version 1.1 טרם הוגדר.

## Version 1.2 — UI/UX Refinement: ניווט ותפריטי הגדרות (03/08/2026)

לאחר שלב 4.0.3 של Version 1.1, אושרו שתי החלטות מוצר חדשות (ראו PROJECT_CONTEXT.md לפירוט הארכיטקטורה המעודכנת): (1) ביטול טאב "תנועות" מהניווט התחתון — כל תנועה שייכת לקטגוריה, וצפייה בכל התנועות יחד מתבצעת דרך שורת מערכת "📋 כל התנועות" בראש מסך קטגוריות; (2) פיצול מסך ההגדרות מרשימה שטוחה לתפריט-נושאים (עם תיאור קצר לכל נושא) + מסכי-משנה ייעודיים, כולל כפתור "← חזרה" בכל מסך-משנה. `APP_VERSION` עודכן ל-`'1.2.0 Preview'`.

בוצע מיפוי Truth/Blueprint/Gaps/Rank/Pushback/Reuse/Risks/Build-plan/Tests מלא לפני כתיבת קוד (ללא שינוי קוד בשלב זה) — כולל קריאה ישירה של `showScreen()`, מנגנון ה-FAB (`isOnCategoryPage()`), `filterTransactionsByCategory()`, ו-8 פונקציות-בניית-הסקשנים הקיימות של ההגדרות. אושרו 3 החלטות נוספות: יומן פעילות כנושא עצמאי שביעי (לא בתוך אודות/נתונים), כפתור "← חזרה" בכל מסך-משנה (לא להסתמך על הטאב התחתון בלבד), ותיאור קצר תחת כל נושא בתפריט הראשי.

**מימוש (4 שלבים, כולם בתוך `index.html` בלבד):**

- **שלב A — יסוד: `showScreen()` עם `SCREEN_NAV_ALIAS`**: מיפוי (`{transactions:'categories','settings-detail':'settings'}`) שמדליק את כפתור הניווט הקיים המתאים כשאין כפתור תואם-שם למסך (fallback רק כש-`getElementById('nav-'+name)` מחזיר `null` — התנהגות זהה-לחלוטין לכל מסך שעדיין יש לו כפתור). אפס שינוי UX נראה בשלב זה עצמו. 6/6 בדיקות VM/Node (הרצת ה-`<script>` האמיתי מהקובץ) עברו.
- **שלב B — ביטול טאב "תנועות"**: `nav-transactions` הוסר מהניווט התחתון (4 כפתורים: בית/תובנות/קטגוריות/הגדרות). נוספה שורת מערכת סטטית "📋 כל התנועות" בראש מסך קטגוריות (`class="category-row"`, זהה ויזואלית לשורות קטגוריה), עם `onclick="filterTransactionsByCategory(null)"` — שימוש חוזר מדויק בפונקציה הקיימת (כבר תמכה ב-`null` ללא כל שינוי). 22/22 בדיקות אינטגרציה (VM/Node על ה-`<script>` האמיתי מול DOM שנבנה מה-id-ים הסטטיים בפועל) עברו: FAB מוסתר ב"כל התנועות" (כפי שהיה גם קודם), גלוי בעמוד-קטגוריה; מסננים/⋮/עריכה/ארכוב פועלים ללא שינוי.
- **שלב C — פיצול מסך הגדרות**: מסך `screen-settings` הפך לתפריט-נושאים דק (7 שורות: אבטחה→מראה→התראות→נתונים→יומן פעילות→אפשרויות ניסיוניות→אודות, כל אחת עם אייקון+תיאור קצר+שברון); נוסף מסך חדש `screen-settings-detail` (כותרת+"← חזרה"+גוף) שמציג בכל פעם נושא אחד, ע"י קריאה ל-אחת מ-8 פונקציות `buildXSectionHtml()` הקיימות **ללא כל שינוי בפנימיותן**. `SETTINGS_TOPICS` (מערך תצורה) ו-4 פונקציות ניתוב חדשות (`openSettingsTopic`/`closeSettingsDetail`/`renderSettingsDetailScreen`/`refreshSettingsUI`). 11 קריאות פנימיות (theme/color/font/notifications/PIN forms/auto-lock/export) שהיו קוראות לרינדור-השטוח-הישן הוחלפו ל-`refreshSettingsUI()`, שמרעננת נכון את מסך-המשנה הפתוח (לא קופצת לתפריט). **באג אמיתי נמצא ותוקן תוך כדי הבנייה עצמה** (לפני שהגיע למשתמש): `SETTINGS_TOPICS` הוגדר במקור מאוחר מדי בקובץ ביחס לקריאת האתחול הראשונה (`var` אינו מקבל hoisting לערך, בניגוד ל-`function`) — גרם ל-`TypeError` אמיתי ב-boot; תוקן ע"י העברת ההגדרה למוקדם בקובץ (ליד `THEME_OPTIONS`), בטוח כי `buildXSectionHtml` כולן function declarations עם hoisting מלא. 57/57 בדיקות אינטגרציה עברו אחרי התיקון (כולל: סדר+תיאורים נכונים בתפריט, תוכן נכון בכל אחד מ-7 מסכי-המשנה, חזרה לתפריט, `nav-settings` נשאר מודגש בכל מסכי-המשנה, שינוי הגדרה בפועל נשאר על אותו מסך-משנה ולא מאפס אותו).
- **שלב D — רגרסיה משולבת**: תרחיש רציף אחד המשלב את שתי ההחלטות יחד: הוספת תנועה חדשה דרך "כל התנועות" הלא-מסונן → עריכת inline → ארכוב → שחזור → מחיקה סופית → חזרה לקטגוריות → מסך תובנות (Forecast) לא נפגע → מעבר על כל 7 נושאי ההגדרות אחרי סבב התנועות → הגדרת PIN → נעילה **ממסך שרירותי** (תובנות, לא הגדרות) → פתיחה עם קוד נכון. 14/14 בדיקות עברו.

**סה"כ 99 בדיקות VM/Node עברו** (6+22+57+14), כולן מריצות את ה-`<script>` **האמיתי** מהקובץ בפועל (לא עותק) מול DOM מזויף שנבנה אך ורק מה-id-ים הסטטיים שקיימים בפועל ב-HTML — כולל מספר בדיקות `node --check` על ה-`<script>` המלא בכל שלב. אין בפרויקט חבילת בדיקות אוטומטית קבועה; אלה סקריפטים חד-פעמיים ב-scratchpad, לא נכנסו לריפו.

**אין שינוי בסכימת הנתונים**: `items`/`categoryConfig`/6 מפתחות ה-localStorage הקיימים — ללא שינוי. אין מפתח חדש. `app.js`/`styles.css`/`index-preview-v2.html` — לא נגעו.

**Git**: כל 4 השלבים בוצעו על גבי `index.html` בלבד ב-branch `feature/cockpit-preview-v2`, ונשמרו יחד ב-commit `3f232c787fde75f40b630e9fcac8756c9066f471` ("feat: remove transactions tab and split settings into topic menu"). **תיקון תיעודי (31/08/2026)**: הניסוח הקודם כאן תיאר את השלב כ"טרם בוצע commit" — בפועל התיעוד הזה עצמו נכלל באותו commit; הניסוח לא עודכן אז בטעות (אותה תופעה בדיוק כבר קרתה ותוקנה בעבר לגבי שלב ז׳.4, ראו CHANGELOG.md).

**אימות ידני שבוצע**: Version 1.2 עברה בדיקת דפדפן אמיתית ב-Desktop וב-Mobile ואושרה במפורש ע"י המשתמש.

**המשך**: Version 1.2 סגורה ומאושרת. לפי מפת הדרכים המאושרת — Version 1.2.1 (תיקוני תיעוד, ראו למטה) ← Version 1.3 ← Production Release. טרם בוצע merge ל-`main`/Vercel Production.

## Version 1.3 — מנוע תזרים מזומנים מאוחד + מסך תובנות חדש (31/08/2026)

לאחר סגירת Version 1.2.1, פותח מנוע חישוב תזרים מזומנים חדש ומסך Insights מחודש, לפי מפת הדרכים המאושרת (Version 1.3 ← Production Release). כל הפיתוח בוצע ישירות על `index.html`, בארבעה שערים (Phase 2A–2D), כל אחד עם אישור מוצרי מפורש בנפרד ואימות מקביל (סוכני בדיקה עצמאיים + VM/Node + Edge/CDP אמיתי) לפני מעבר לשלב הבא. `APP_VERSION` עודכן ל-`'1.3.0 Preview'`.

### Phase 2A — Discovery (ללא קוד)
מיפוי מלא של המודל הפיננסי הקיים בפועל (מה מחושב בפועל, מה מאוחסן-בלבד, מה legacy), זיהוי פערי חישוב (אין מושג "יתרת חשבון" כלל באפליקציה עד לשלב זה; סתירות מתועדות בהערות הקוד עצמן בין `getMonthSnapshot`/`getCategoryMonthlyTotals`/`computeForecast` בטיפול ב-fixed/credit ובהלוואות), הצעת חוזה חישוב, ו-Blueprint UX ל-Insights. 4 סוכנים מקבילים ביצעו את המיפוי; סבב מעקב ייעודי לסמנטיקת "תשלומים שונים" (Variable) הוביל להחלטה שנשארה בתוקף לאורך כל הרצף: Variable נשאר tracking-only, מכיוון שההשפעה הכספית האמיתית שלו עלולה כבר להיכלל בפריט fixed/credit נפרד, והסכימה הקיימת לא יכולה להבחין בין שני המקרים בלי שדה קישור חדש.

### Phase 2B — מנוע התזרים המאוחד (קוד)
נוסף מודול פונקציות טהורות חדש (`generateCashflowEvents`, `buildCashflowSummary`) מיד אחרי `computeForecast()` הקיים — **אדיטיבי בלבד**, אינו מחליף ואינו נוגע באף פונקציה קיימת (`getMonthSnapshot`/`getCategoryMonthlyTotals`/`computeForecast`/`getTotalFixedCommitments`/`getVariableRemainingBalance`/`getLoansBalanceSummary` נשארו ביט-זהות).

**חוזה פיננסי מאושר:**
- `currentBalance` — שדה חדש בתוך `family_finance_settings` הקיים (ברירת מחדל `null` = "לא הוזן", מובחן במפורש מ-`0` מפורש) = היתרה האמיתית ברגע ההזנה — **לא** יתרת פתיחה היסטורית.
- **גבול "היום" (תוקן בשער נפרד, לאחר בדיקה שהעלתה סיכון ספירה כפולה)**: מכיוון שאין בסכימה סטטוס ביצוע/ledger/timestamp, לא ניתן לדעת אם אירוע שתאריכו הוא היום כבר השפיע בפועל על היתרה שהוזנה. לכן **כל אירוע שתאריכו הוא היום מוחרג לחלוטין** מכל מספר (שפל/סוף-חודש/האירוע הבא/ההכנסה הבאה/הסכום עד ההכנסה הבאה) — ההיטל המספרי מתחיל **ממחר**. חודשים עתידיים אינם כפופים לכלל זה (הם כבר כולם עתידיים).
- הכנסה/הוצאה קבועה: אירוע חודשי לפי `resolveEffectiveDay`. **fixed/credit נכלל** במנוע החדש (מיישב במכוון סתירה ישנה ומתועדת בין הפונקציות הקיימות — לא באג, החלטת מוצר מפורשת). הוצאה שנתית מחולקת ל-12 (נבדק ואומת מספרית — לא הסכום השנתי המלא בכל חודש).
- הלוואה: מאוחדת ל-`resolveEffectiveDay` (במקום `item.day` הגולמי) ונספרת לפי טווח גבייה פעיל (`isBillingActiveInMonth`, לא לפי "נשארו תשלומים" גרידא) — מתקן שני חוסרי-עקביות ידועים: הלוואה שטרם התחילה שנספרה כבר בחודש הנוכחי; מקור-יום לא-עקבי בין הלוואות לתשלומים משתנים.
- תשלומים משתנים (`variable`): tracking-only לצמיתות — אפס אירועים בזרם התזרים, כפי שהוחלט ב-Phase 2A.
- מתוארך: אירוע חד-פעמי בתוך אופק 6 החודשים.
- קיצוץ חודש קצר: `getClampedBillingDate` הקיים בלבד, ללא שינוי לוגיקה.
- יום משותף: צבירה **לפי יום קלנדרי שלם** (לא לפי סדר אירוע-אירוע) — אין יתרת-ביניים תוך-יומית נחשפת בכלל; סדר תצוגה (הכנסה→קבועה→הלוואה→מתוארך) הוא לתצוגה בלבד, מוכח בבדיקות שלא משפיע על אף חישוב יתרה.
- נתונים חסרים: מדדים לא-ניתנים-לחישוב מחזירים `null`, לעולם לא 0 מומצא (למשל "אין הכנסה עתידית מוגדרת" — לא "₪0").
- יתרה שלילית: מוצגת כפי שהיא, לא נחתכת — סימון בלבד, ללא חסימת פעולות.
- אופק תחזית: 6 חודשים, כקודם.
- **ללא שינוי ארכיטקטוני**: אין backend, אין שרת, אין service worker, אין מפתח localStorage חדש (`currentBalance` נוסף לתוך `family_finance_settings` הקיים; לולאת המיזוג-לברירת-מחדל הגנרית שכבר קיימת ב-`loadAppSettings()` טיפלה בשדה החדש ללא צורך בקוד מיוחד — תאימות לאחור אוטומטית).
- אומת ב-40+ בדיקות VM/Node על ידי סוכנים עצמאיים, בשני סבבי שער נפרדים (חוזה חישוב ראשוני + תיקון גבול "היום" לאחר ביקורת ייעודית), כולל הוכחה מספרית מלאה לכל אחד מכללי ה-fixed/שנתי/הלוואה/variable ותרחישי קצה אמיתיים (יום אחרון בחודש, גלישת שנה, פברואר).

### Phase 2C — חיבור ל-UI + מסך Insights חדש (קוד)
מסך Insights קיבל hero חדש "מצב להיום" (עם עורך יתרה נוכחית מוטמע — קלט מספרי + כפתור שמירה, קורא ל-`setCurrentBalance()` הקיים בלבד, ללא מפתח localStorage חדש), 5 כרטיסי סיכום — **האירוע הכספי הבא**, **ההכנסה הבאה**, **צפוי לרדת עד ההכנסה הבאה**, **נקודת השפל** (מהיום/מחר עד סוף החודש, לא נקודת שפל היסטורית), **תחזית סוף חודש** — כולם מבוססים אך ורק על `buildCashflowSummary()`, ללא כפל-חישוב בשכבת ה-UI. וציר תזרים מתרחב (תצוגה מקדימה של 3 אירועים + "הצג הכל", ניזון מ-`summary.timelineEvents` — חשיפה טהורה של אותו זרם אירועים שכבר משמש את `nextEvent`/`nextIncome`, אין חישוב שני). גרף/טבלת התחזית ל-6 החודשים הקיימים נשארו במבנה זהה לחלוטין, רק מקור הנתונים שונה ל-`buildCashflowSummary().months` (net = הפרש endOfMonth מ-startingBalance לכל חודש). תוסף CSS מינימלי בלבד: `--color-danger-bg` (מקביל ל-`--color-warning-bg` הקיים) לכרטיסים שערכם שלילי.

**החלטת סמנטיקה קריטית (Home מול Insights), שאושרה ואומתה בשער ייעודי נפרד:** `hero-amount`/`snapshot-income`/`snapshot-expenses` במסך הבית **נשארו ללא שינוי בכוונה תחילה** — הם מייצגים "תמונת מצב חודשית" (`getMonthSnapshot`, הכנסה פחות הוצאה של החודש הקלנדרי הנוכחי, לא מעוגן ליתרה אמיתית), מושג מוצרי שונה מהותית מתחזית התזרים המעוגנת-ליתרה של Insights — **לא הוחלט להמיר את מספר הבית למספר של Insights**, ולא בוצע. הבית ממשיך להציג כרטיס "האירוע הבא" מבוסס המנוע החדש (מידע ניטרלי, לא הערכתי-פיננסי). כרטיס "אזהרת תחזית" הישן (מבוסס `getForecastWarning`/`computeForecast` הישנים) **הוסר לחלוטין** ממסך הבית — לא רק חובר-מחדש למנוע אחר — כדי שלא יהיו שני מספרים סותרים המתיימרים שניהם להיות "התחזית הקדימה הרשמית" לאותו חודש. **מסך Insights הפך לבעלים הבלעדי** של מושג "אזהרת תחזית קדימה" (מבוסס `findFirstNegativeCashflowMonth(buildCashflowSummary(...))` החדש — פונקציית סריקה תצוגתית-בלבד, אינה משנה את `isNegative` של המנוע עצמו שנשאר מוגבל לחודש הנוכחי בלבד).

אומת בשני סבבי בדיקה מקבילים (5 סוכנים עצמאיים בכל סבב: חוזה UI פיננסי, ביקורת צרכן-כפול/מקור-אמת, Desktop Edge/CDP אמיתי, Mobile Edge/CDP אמיתי, התמדה/תאימות לאחור) ושער תיקון סמנטי שלישי — כולל תיקון אמיתי אחד שנמצא ותוקן במהלך הביקורות: כרטיס אזהרה ב-`attention-list` של הבית יכול היה לסתור את `hero-amount` באותו מסך בדיוק, עקב הבדל בכלל fixed/credit (המנוע החדש כולל credit במכוון, `getMonthSnapshot` מחריג אותו במכוון) — נפתר ע"י **הסרת הכרטיס מהבית**, לא ע"י שינוי כלל ה-fixed/credit המאושר במנוע (שינוי כזה היה אסור במפורש).

### Phase 2D — תיעוד וסגירה (31/08/2026)
תיעוד זה (CURRENT_STATUS.md/CHANGELOG.md/PROJECT_CONTEXT.md/TODO.md). `APP_VERSION` עודכן מ-`'1.2.0 Preview'` ל-`'1.3.0 Preview'`.

### Phase 2E — עוגן יתרה (Balance Anchor) — מחליף את מודל ה-currentBalance הידני (31/08/2026)

**עדכון מוצרי מאושר, מחליף את מודל ה-`currentBalance` שתואר בפירוט ב-Phase 2C לעיל.** לאחר בדיקת דפדפן ידנית ראשונית של המשתמש, הוחלט: המודל שבו המשתמש מזין "יתרה נוכחית" מחדש בכל פעם **אינו** המודל הסופי של Version 1.3. המודל הסופי המאושר הוא **עוגן יתרה**:

- **מודל נתונים**: שני שדות חדשים בתוך `family_finance_settings` הקיים — `anchorBalance` (מספר) ו-`anchorDate` (תאריך מקומי `YYYY-MM-DD`). המשמעות: "ביום `anchorDate`, היתרה האמיתית בחשבון הייתה `anchorBalance`." המשתמש מזין את היתרה **פעם אחת** בהתחלה — לא נדרשת הזנה יומית.
- **"יתרה משוערת להיום"** — מחושבת אוטומטית: `anchorBalance` + כל אירועי תזרים כשירים **אחרי `anchorDate` בדיוק** ועד היום **(כולל)**. אירועים ביום העוגן עצמו ולפניו **מוחרגים לחלוטין** (כבר משתקפים בסכום העוגן מעצם הגדרתו). אף אירוע לא מיושם פעמיים — אותו זרם אירועים יחיד (`generateCashflowEvents`, נקרא פעם אחת בכל בנייה) מוזן גם לשלב ה"השלמה" (anchor→היום) וגם לתחזית הקדימה (מחר ואילך), עם טווחים זרים לוגית ומוכחים כאלה (`≤היום` מול `>היום`). **הערך המחושב מתועד במפורש כאומדן, לא כיתרת בנק חיה/אמיתית.**
- **תחזית קדימה** — ממשיכה בדיוק כמו קודם, רק שמתחילה מ"יתרה משוערת להיום" (במקום מהיתרה שהוזנה ישירות): מחר ואילך, אותו כלל today-boundary (אירועי-היום מוחרגים גם מהתחזית קדימה, לא ניתן להניח שכבר קרו/לא קרו).
- **פישוב ("עדכן יתרה בפועל")** — פעולה אחת, זהה מבנית להגדרה הראשונית: הסכום שהוזן הופך ל-`anchorBalance` החדש, התאריך המקומי הנוכחי הופך ל-`anchorDate` החדש. מוחלף לגמרי, לא מצטבר על העוגן הישן (אומת: עוגן ישן + התאמה חדשה = ערך העוגן החדש בדיוק, ללא "אפייה" של דלתא ישנה).
- **תאימות `currentBalance` הישן**: `resolveBalanceAnchor()` מעדיף `anchorBalance`/`anchorDate` תקינים; בהיעדרם, אם `currentBalance` הישן (מ-Phase 2B/2C) מוגדר מפורשות — מטופל כ**עוגן legacy מתוארך "היום"**, בקריאה-בלבד, **לעולם לא נכתב אוטומטית חזרה ל-localStorage** (אומת: קריאות חוזרות = אפס כתיבות) — אותה שיטת "lazy fallback, no migration" כמו `resolveEffectiveDay` הקיימת. אין תאריך היסטורי מומצא. משתמש עם `currentBalance` ישן ימשיך לראות בדיוק את אותו מספר (ללא סחיפה) עד לפישוב מפורש ראשון. אפס מפורש נשאר עוגן תקף; לא-מוגדר נשאר מובחן משניהם.
- **UX סופי**: לפני הגדרה — "הגדר יתרת בסיס" + טופס גלוי ישירות. אחרי הגדרה — "יתרה משוערת להיום" + תאריך העוגן מוצג בעדינות (subnote) + כפתור "עדכן יתרה בפועל" (טופס ההתאמה **מכווץ כברירת מחדל**, נשאר מכווץ גם אחרי רענון דף — **תוקן באג אמיתי** שנמצא בסבב בדיקת דפדפן: המצב "מכווץ" הוסק בטעות ממחרוזת `style.display` דו-משמעית; תוקן ע"י דגל מצב JS מפורש `balanceAnchorFormExpanded`).
- **כל הכללים הפיננסיים המאושרים נשמרו ללא שינוי**: fixed/credit נכלל, הוצאה שנתית÷12, הלוואות לפי `resolveEffectiveDay`+טווח גבייה פעיל, `variable` נשאר tracking-only, מתוארך חד-פעמי, אופק 6 חודשים, יתרה שלילית = אזהרה בלבד, חישוב יום-אטומי דטרמיניסטי.
- **ללא שינוי ארכיטקטוני נוסף**: אין מפתח localStorage חדש (שני השדות בתוך `family_finance_settings` הקיים), אין מיגרציה הרסנית, אין מקור-אמת שני.

**אימות**: 11/11 בדיקות חישוב (עוגן=היום/אתמול/כמה ימים אחורה, אירועי גבול תאריך, גלישת חודש/שנה, אפס מפורש, שלילי, פישוב נקי) — PASS. 8/8 בדיקות התמדה/תאימות (הגדרה→reload, אפס, הגדרות ישנות-ישנות, `currentBalance` legacy, round-trip גיבוי/שחזור) — PASS. 6/6 ביקורת מקור-אמת/ספירה-כפולה — CONFIRMED, אין סיכון. שני סבבי בדיקת דפדפן אמיתי (Edge/CDP, Desktop+Mobile 390×844) — כולל סבב ממוקד סופי שאימת ספציפית את תיקון ה-collapse-אחרי-רענון — **PASS מלא, אפס שגיאות/אזהרות קונסולה**.


**Legacy/נדחה (מוגדרות בקוד אך לא בשימוש חי — לא נמחקו, ניקוי אופציונלי בעתיד):** `getForecastWarning`, `getBiggestUpcomingCharge`, `getProjectedBalanceAfterUpcoming` — נקראות כיום רק דרך `buildNarrative()`, שהשדות הרלוונטיים שלה (`sentences.forecastWarning`/`sentences.projectedBalanceAfterUpcoming`) כבר לא נקראים משום מסך חי. בהזק ויזואלי קצר של כרטיס "אזהרת תחזית" ממוק (`renderMockUI()`/`MOCK_DATA.insights`) שמצויר לרגע לפני שהרינדור האמיתי דורס אותו — התנהגות bootstrap קיימת מלפני Phase 2C כולה (לא רגרסיה של השלב הזה), נדחה ללא תיקון.

**לא שונה בכלל במהלך Version 1.3**: favicon, ניקוי `index-preview-v2.html`/`app.js`/`styles.css`, מבנה הניווט, מבנה 4 המסכים, `main`/Production/Vercel.

**עדכון עובדתי (31/08/2026) — `deploy_lookup_tmp.json`**: קובץ זמני לא-קשור (cache של תגובת GitHub Deployments API, מעולם לא tracked ב-Git) נמחק בטעות ב-Phase 2D על ידי סוכן בדיקה מקבילי, בניגוד להנחיית ההיקף שניתנה לו; מכיוון שלא היה tracked, לא ניתן היה לשחזרו מ-Git. חיפוש תלויות מלא בריפו אימת אפס תלות פונקציונלית בקוד/סקריפטים/תיעוד/קונפיגורציית Vercel/GitHub Integration — האירוע סווג **Non-Product**, ללא השפעה על האפליקציה או על Version 1.3. הקובץ לא שוחזר ולא נוצר מחדש. **תיקון תהליכי**: מעתה, סוכנים מקבילים אסורים למחוק/לנקות/להחזיר קבצים לא-קשורים או untracked.

**עדכון מצב (01/09/2026)**: Version 1.3 (כולל Phase 2E — עוגן יתרה, המודל הסופי והמאושר) בוצעה ונדחפה בשני commits ל-`feature/cockpit-preview-v2`: `28a494499b068df549a19c95eb205f56bf901464` ("feat: add Version 1.3 cash-flow insights") ו-`22bdda128c779cc82b57ae9ee5d6bbc6250bfe1c` ("feat: add balance anchor model"). אומתה ידנית מול Vercel Preview, כולל התמדת עוגן-היתרה אחרי F5 אמיתי. לאחר הכנת שחרור (`APP_VERSION` `'1.3.0 Preview'` → `'1.3.0'`, commit `420a273`), **Version 1.3.0 שוחררה ל-Production (01/09/2026)**: מוזגה ל-`main` ב-commit `ee6701ae276c0fba668c7c23a345c528e64326ab`, נדחפה, ותויגה `v1.3.0`. דיפלוי Vercel Production הושלם בהצלחה. אומתה ידנית כ-LIVE ב-https://finance-nahom10.vercel.app (Home/Insights/עוגן-יתרה-אחרי-F5-אמיתי/Categories/Settings — הכל PASS). עוגן Rollback שמור: תגית `pre-v1.3-production` → `75c0d2b8ff53c7e5b663d3d696bbb7cb8140293c`.

### Version 1.3.1 — Patch Release Preparation (01/09/2026)

**רקע**: מאז שחרור Version 1.3.0 ל-Production, בוצעו על גבי הענף שתי חבילות עבודה נוספות. **תיקון עובדתי (02/09/2026, Milestone 7.1)**: בניגוד לניסוח המקורי כאן ("טרם שוחררו") — אומת ב-Git שהשתיים **כבר נמצאות ב-`origin/main`** (ולפיכך ב-Production, בהנחת דיפלוי אוטומטי דרך אינטגרציית Git שמתועדת למעלה), כ-commits נפרדים שקדמו לתחילת השיחה הנוכחית: `bb6d8cd` ("feat: improve category installment summaries and collapse state") ו-`3b93d86` ("fix: restore backups safely in Version 1.3.1"), שניהם אבות-קדמונים של `c83f12834f04ec6654f4709b777e248f5c0096c1` (ה-baseline שאומת בתחילת השיחה). התיעוד כאן פשוט לא עודכן בזמן אמת כשזה קרה. `APP_VERSION` ב-`c83f128` הוא כבר `'1.3.1'`.

**1. שיפורי UI בעמודי קטגוריה** (uncommitted עד לשלב זה):
- **סיכום כרטיס מכווץ להלוואות**: תחת התאריך הקיים — "תשלום X/Y" (תשלומים ששולמו מתוך הסך הכל) + "יתרה ₪Z" (יתרת הלוואה נותרת). הסכום החודשי האדום, הכותרת, התאריך והתנהגות הקליק נשארו ללא שינוי לחלוטין. X/Y מבוסס `parseDatesAndGetLeft()` הקיים; Z מבוסס פונקציה חדשה `getLoanRemainingBalance(it)` שחולצה **מתוך** הנוסחה הקיימת של `getLoansBalanceSummary()` (ללא שכפול לוגיקה עסקית, אומת equivalence מלא לפני/אחרי).
- **אותו סיכום להוצאות "תשלומים שונים"/variable**: אותה תבנית בדיוק (תשלום X/Y + יתרה ₪Z), מבוסס `getVariableItemRemainingBalance(it)` — פונקציה חדשה שחולצה באותו אופן מתוך `getVariableRemainingBalance()` הקיימת. `variable` נשאר **tracking-only** — לא נוסף לזרם אירועי התזרים (`generateCashflowEvents()` לא נגע כלל).
- **פריסה רספונסיבית ללא ellipsis**: המידע המשני (תאריך + תשלום X/Y + יתרה ₪Z) עוטף (`flex-wrap`) לשורה נוספת במסכים צרים (390px/360px אומתו) במקום להיחתך — כל שלושת הערכים תמיד גלויים במלואם.
- **קריסת כל פריט קטגוריה מורחב בעת עזיבת הקטגוריה**: `previewEditingId` (המצב המשותף היחיד שמתעד איזה פריט פתוח לעריכה, על פני כל סוגי הקטגוריות) מתאפס במרוכז בתוך `showScreen()` (נקודת המעבר המשותפת לכל שינוי מסך, כולל מעבר בין קטגוריות דרך `filterTransactionsByCategory()`) ובתוך `clearCategoryFilter()` (הנתיב היחיד שעוזב קטגוריה בלי לעבור דרך `showScreen()`). אין שמירה של מצב-הרחבה ב-localStorage.

**2. תיקון שורש-בעיה בשחזור גיבוי (Data Restore)** — פגם בעל השפעה גבוהה:
- **הבעיה שנמצאה**: `family_finance_loan_balance_view` נשמר כמחרוזת גולמית (`total`/`principal`, ללא מרכאות JSON) בעוד שכל שאר מפתחות `family_finance_*` מוצפנים ב-JSON, ו-`isValidBackupShape()` דרש שכל ערך יעבור `JSON.parse()` בהצלחה — כתוצאה מכך **כל** גיבוי שהאפליקציה עצמה ייצרה, עבור כל משתמש שנגע אי-פעם בטוגל "קרן/סה\"כ" באריח יתרת ההלוואות, נדחה במלואו בהודעת "קובץ הגיבוי אינו תקין" — גם כשהקובץ תקין לחלוטין.
- **התיקון**: `saveLoanBalanceView()` כותב כעת `JSON.stringify(view)`. `loadLoanBalanceView()` קורא בעדיפות את הצורה החדשה, עם נפילה חזרה לקריאת ערך legacy גולמי כשה-JSON.parse נכשל — **ללא כתיבה מחדש בקריאה** (lazy fallback, כמו `resolveEffectiveDay`). `isValidBackupShape()` קיבל חריג צר וממוקד **רק** למפתח הזה: מקבל גם את הצורה החדשה וגם `total`/`principal` גולמי-legacy, דוחה כל ערך אחר — כל שאר המפתחות שומרים על הדרישה המחמירה המקורית ללא שינוי.
- **הקשחת בטיחות השחזור**: `confirmRestoreBackup()` הפך ל-**best-effort transactional restore עם compensating rollback** (לא אטומיות אמיתית — ל-localStorage אין מנגנון עסקה על פני כמה מפתחות): צילום-מצב (`snapshotFamilyFinanceStorage()`) של כל מפתחות `family_finance_*` נלקח לפני כל כתיבה; אם כתיבה כלשהי נכשלת, מתבצע ניסיון rollback מלא (`restoreFamilyFinanceSnapshot()`), עם סדר עדיפות מכוון (המפתחות המשמעותיים פיננסית — `data`/`cat_config`/`settings` — משוחזרים ראשונים, כדי שגם אם ה-rollback עצמו יכול להשלים רק חלקית, הנתונים החשובים ביותר יישמרו). **שני תוצאים מדווחים במפורש, בלי לערבב ביניהם**: כתיבה נכשלה + rollback הצליח → "שחזור נכשל... הנתונים הקודמים שוחזרו במלואם"; כתיבה נכשלה + גם ה-rollback נכשל → "שגיאה קריטית... לא ניתן להבטיח שהמצב המקורי שוחזר במלואו". באף אחד משני מקרי הכשל אין `reload()` ואין דיווח הצלחה כוזב.
- **אומת (VM + Edge/CDP אמיתי, כמה סבבים)**: round-trip מלא כולל `anchorBalance=0`, שני הצורות (JSON חדש + legacy גולמי) בגיבוי חי ובקובץ גיבוי, כשל-כתיבה-חלקי עם rollback מוצלח, כשל-rollback-עצמו (עם הוכחה בפועל שסדר-העדיפות מגן על הנתונים החשובים), קבצים לא-תקינים (JSON פגום/מבנה שגוי/ערך שרירותי לא-תקין) — כולם נדחים בבטחה, אפס כתיבה. אפס שגיאות קונסולה בכל הסבבים.

**3. הכנת שחרור Version 1.3.1**: `APP_VERSION` עודכן מ-`'1.3.0'` ל-`'1.3.1'` (patch release — תיקון פגם בעל השפעה גבוהה + שיפורי UI, ללא שינוי schema/מפתחות localStorage/APP_VERSION-breaking).

**תיקון עובדתי (02/09/2026, Milestone 7.1)**: הניסוח הקודם כאן טען ש-Version 1.3.1 "נשמרה" יחד עם Milestones 3–6 באותו commit מקומי, וש-Production נשאר ב-Version 1.3.0 — **שגוי בשני הפרטים**. כפי שתוקן למעלה: Version 1.3.1 (כולל תיקון השחזור ותקציר הקטגוריות) **כבר הייתה חלק מ-`origin/main`/Production לפני תחילת השיחה** (`c83f128`, `APP_VERSION='1.3.1'`, `schemaVersion:1`, **ללא** Goals). commit `4fc1b81d382b7e5a1024abdbf8d8e181ff1ea005` ("feat: add forecast and savings goals") מכיל **רק** את עבודת Milestones 3–6 (תחזית/יעדים/תזכורת/Backup schemaVersion 2/נגישות/ניגודיות) שנבנתה *על גבי* אותו baseline שכבר-1.3.1 — זהו החלק שעדיין מקומי ולא-נדחף. **מצב Production הנוכחי (הידוע-אחרון): APP_VERSION 1.3.1, schemaVersion 1, ללא מסך יעדים/Forecast/תזכורת** — Milestones 3–6 טרם הגיעו ל-Production.

## Milestones 3–6 — מסך תחזית, מערכת יעדי חיסכון, תזכורת חודשית, Backup schemaVersion 2 (02/09/2026, ישירות על `index.html`, נשמר ב-commit `4fc1b81d382b7e5a1024abdbf8d8e181ff1ea005`)

עבודה שבוצעה על גבי Version 1.3.1 (עדיין באותו commit, לפני שאושר ה-commit). כל האימות בוצע ב-Edge headless אמיתי (`puppeteer-core`), פרופיל דפדפן זמני ומבודד בכל הרצה, שרת HTTP מקומי — אף פעם לא נגד נתוני משתמש אמיתיים. מעל 200 assertions נפרדות בסך הכול לאורך Milestone 6 בלבד (בנוסף לבדיקות Milestones 3–5 המוקדמות יותר).

**Milestone 3 — מסך "תחזית" (📈)**: מחליף בתוכן את "תובנות" (`תובנות`→`תחזית`), **באותו `id="screen-insights"`** — לא מסך כפול. בורר 30/60/90 יום עם cutoff מדויק בימי-קלנדר (לא ימי-עסקים). קטע "תחזית חודשית מורחבת" (6 חודשים, מכווץ כברירת מחדל) — **מבוסס `buildCashflowSummary()`/מנוע התזרים המאוחד בלבד**, אותו מנוע בדיוק שמזין את 30/60/90 (אומת: התאמה מדויקת בין הגרף/הטבלה לחישוב-יד בכל חודש, כולל גבולות חודש-ראשון/אחרון/אופק-6-חודשים). ציר-זמן קרוב עם toggle 3↔4+ אירועים.

**Milestone 4 — מסך "יעדים" (Goals)**: יעד חיסכון עצמאי (`family_finance_goals`, מפתח חדש) — מושג נפרד לגמרי מהוצאה/הכנסה/הלוואה: תכנון חיסכון לקראת יעד עתידי (למשל רכב/חופשה), לא מעקב תזרים שוטף. יעד יכול להיות שטוח (סכום+תאריך יעד אחד) או מחולק ל**רכיבים** (components) — כל רכיב עם שם/סכום/**מועד-יעד עצמאי משלו** (יורש את תאריך היעד של ההורה כשלא הוגדר לו תאריך נפרד). הקצאת החיסכון הנצבר (`savedAmount`+`confirmedTransfers`) בין רכיבים: **earliest-deadline-first** — הרכיב עם המועד הקרוב ביותר מקבל מהבריכה המשותפת קודם, במלואו, לפני שהיתרה עוברת לרכיב הבא. Backup **schemaVersion 2**: ייצוא V2 תמיד כולל `family_finance_goals` (גם `[]` כשאין יעדים כלל); שחזור V2 דורש יעדים תקינים לחלוטין (all-or-nothing, ולידציה strict לפני כל כתיבה); **שחזור V1 (גיבוי מלפני קיום יעדים) משמר יעדים קיימים כברירת מחדל** — checkbox מפורש ולא-מסומן-כברירת-מחדל למחיקה מכוונת.

**Milestone 5 — תזכורת חיסכון חודשית**: נפתחת אוטומטית ב/אחרי ה-2 לכל חודש (ובכל טעינת אפליקציה טרייה כל עוד יעד עדיין לא מטופל לתקופה הנוכחית — התנהגות זו לא שונתה ב-Milestone 6, מאושרת כפי שהיא), לכל יעד פעיל ולא-מושלם שטרם קיבל העברה מאושרת בתקופה הנוכחית. **דחייה ("הזכר לי מאוחר יותר") ו-Escape הן session-only** — לא נכתב דבר ל-storage, לא נשמר לאחר reload; אך **per-goal/per-period בלבד** (תוקן ב-Milestone 6 — ראו למטה), לא סאפרשן גלובלי שהיה חוסם יעד אחר שנוצר באותה סשן. אישור מלא כותב רשומת `confirmedTransfers` אחת מדויקת לכל יעד (הסכום המוצע בדיוק, לא פיצול). הקצאה מותאמת-אישית מאפשרת סכום שונה/אפס לכל יעד בנפרד — יעד עם 0 נשאר pending ומוצג שוב ברענון טרי. Guard נגד כתיבה כפולה בלחיצה/Enter חוזרים, ובקריאה ישירה חוזרת לפונקציה. legacy `confirmedTransfers` בפורמט `{date,amount}` בלבד (מלפני Milestone 5) ממשיך להתקבל ולהיחשב.

**Milestone 6 — ביקורת אינטגרטיבית + תיקונים** (הריצה מקבילה של 5+ סוכני-ביקורת קריאה-בלבד, Edge headless אמיתי, בכמה סבבים):
- **Overflow אופקי** (35px ב-360px, 5px ב-390px, מסך בית בלבד) — גורם שורש מדויק: `.category-tile`/`.stat-tile` (פריטי CSS Grid `repeat(3,1fr)`) חסרו `min-width:0`, מנע מכלל ה-ellipsis הקיים כבר בקוד להצטמצם. תוקן ב-`min-width:0` בלבד — ללא `overflow-x:hidden`, ללא חיתוך פקדים.
- **Suppression גלובלי→per-goal/per-period**: `reminderDismissedThisSession` (בוליאני יחיד) הוחלף ב-`reminderSuppressedTokens` (טוקנים `period::goalId`, in-memory בלבד, לא נשמר).
- **נגישות**: כפתור "הזכר לי מאוחר יותר" 36px→44px; תגיות-סטטיסטיקה ("יתרת תשלומים שונים"/"יתרת הלוואות") הפכו נגישות למקלדת (`tabindex`/`role`/Enter-Space); פוקוס משוחזר נכון אחרי הרחבת/כיווץ כרטיס יעד (בעבר הועבר ל-`<body>`).
- **קוד מת הוסר**: `computeForecast()`+3 פונקציות-עוטפות — אומת בדיוק (grep+runtime, אפס קריאה חיה, buildNarrative()'s relevant fields מעולם לא נקראו ע"י שום UI חי) לפני ההסרה. `isBillingActiveInMonth`/`getBillingRange` (שיתוף עם המנוע המאוחד) לא נגעו.
- **ולידציית Goals strict אמיתית**: `normalizeGoal`/`normalizeComponent`/`isValidGoalsArrayStrict` נכתבו מחדש — כל שדה נבדק לפי טיפוס גולמי (`typeof`) *לפני* כל trim/עיגול/ברירת-מחדל; בוטלו: יצירת `id` אוטומטית ליעד חסר-id, קבלת מחרוזת-מספרית כ-number (`parseFloat` שקט), ברירת-מחדל שקטה של `createdAt`/`updatedAt` חסרים ל-"now". נוסף: התאמה מדויקת חובה בין `targetAmount` שמור לסכום הרכיבים המאומתים ליעד-עם-רכיבים. אומת ללא רגרסיה על 8 תרחישי שימוש אמיתי מלאים דרך ה-UI (כולל 2,000,000 קומבינציות סכום-רכיבים אקראיות ב-brute-force, אפס אי-התאמה).
- **ניגודיות WCAG**: `--color-text-muted` (light mode בלבד) הוכהה מ-`#6b7280` ל-`#616774` — שני כשלי AA אמיתיים (`.app-header .subtitle` 4.432:1, `.insight-title` 4.116-4.385:1, שניהם מתחת ל-4.5:1) תוקנו, מינימום נמדד לאחר התיקון 4.831:1 על כל הרקעים/הערכות. Dark mode לא נגע.
- **נשאר לא-מטופל (החלטת מוצר, לא תוקן)**: מסך "תחזית חודשית מורחבת" לא מציג מצב-ריק ייעודי כשאין עוגן יתרה (בניגוד ל-30/60/90); הוספת מצב-ריק חדש נחשבת redesign ולא אושרה.

**לא בוצע ולא נבדק**: מכשיר Android/iOS פיזי אמיתי (כל הבדיקה: Edge headless במחשב), Vercel Preview, Production. `APP_VERSION` נשאר `'1.3.1'` (ללא שינוי לאורך Milestones 3–6 — לא היה שינוי schema-breaking). `schemaVersion` הגיבוי נשאר `2`. אין service worker.

**מפת דרכים**: Commit מקומי **הושלם (02/09/2026, commit `4fc1b81d382b7e5a1024abdbf8d8e181ff1ea005`)** ← Push (ממתין לאישור מפורש) ← Production Release (ממתין לאישור מפורש).

**השלב הבא (מודולריזציה) בוצע — ראו סעיף "Milestones 8–9" למטה.**

## Milestones 8–9 — מודולריזציה: פיצול `index.html` ל-3 קבצים, אימות מלא, ותיעוד (03/09/2026, ישירות על `index.html`, על גבי Milestones 3–6 + הקשחת timestamp)

**היקף**: פירוק מבני בלבד — קובץ יחיד (`index.html`, כלל ה-HTML/CSS/JS inline) פוצל ל-3 קבצים: `index.html` (מבנה/markup בלבד), `styles.css` (CSS מלא), `app.js` (JavaScript מלא). **ללא שינוי בשום לוגיקה עסקית, פונקציה, חישוב, או התנהגות** — פעולת חילוץ (extraction) טהורה, לא re-write.

**מצב מוצא**: `app.js`/`styles.css` כבר היו קיימים בריפו כקבצים untracked-לוגית-אך-tracked-ב-git, אך **הוכחו stale ולא-מקושרים** (ראו הסעיף ההיסטורי למעלה) — תוכנם היה עותק ישן/שגוי מלפני Cutover ל-Preview v2 (חסר `toggleCardLast4Field`, `computeForecast`/`renderAll` שם עדיין כללו `variable` בתזרים בניגוד לגרסה החיה). **הוחלט ובוצע: אופציה 2 מרשימת ה-TODO הפתוחה** — חיבור אמיתי, עם החלפה מלאה של התוכן הישן בתוכן המדויק שחולץ מ-`index.html` החי (לא מיזוג, לא שמירה חלקית של הישן).

**חילוץ**: בלוק `<style>` יחיד (שורות 18–769 במקור, 752 שורות) הועתק ביט-לביט ל-`styles.css`; בלוק `<script>` יחיד (שורות 1079–7950 במקור, 6872 שורות) הועתק ביט-לביט ל-`app.js`; `index.html` נשאר עם ה-markup בלבד (328 שורות) וטוען את שני הקבצים דרך `<link rel="stylesheet" href="styles.css">` ו-`<script src="app.js"></script>`. אין CSP בפרויקט (אומת, אין מגבלת קישור). אין bundler/transpiler/module/תלות חיצונית חדשה — script רגיל, לא `type="module"` (היה משנה נראות global של הפונקציות ל-`onclick=` inline handlers).

**תיקון שנמצא ותוקן תוך כדי החילוץ (לא נשאר כפגם)**: הניסיון הראשון השתמש בנתיבים **מוחלטים משורש** (`/styles.css`, `/app.js`), בהתאמה למוסכמה הקיימת כבר ב-`<head>` עבור `/manifest.webmanifest`/`/icons/...`. בבדיקה ישירה (`file://` בדפדפן אמיתי, לא רק ניתוח סטטי) התברר שזה **שובר את עקרון הארכיטקטורה המוצהר** "קובץ יחיד ללא build/שרת — ניתן לפתוח ישירות בדפדפן" (ראו PROJECT_CONTEXT.md, "החלטות ארכיטקטורה קיימות"): נתיב מוחלט תחת `file://` מתפענח מול **שורש הכונן** (למשל `file:///styles.css`), לא מול תיקיית הקובץ — כלומר לחיצה כפולה על `index.html` הייתה טוענת עמוד ללא CSS/JS בכלל (אפליקציה שבורה לחלוטין). **תוקן**: הוחלף לנתיבים **יחסיים** (`styles.css`, `app.js`) — עובדים נכון גם ב-`file://` (בדיקה ישירה: `hasHome=true`, 5 פקדי ניווט, רקע-CSS מוחל, `collectAppLocalStorageBackup`/`showScreen` זמינות — אפליקציה פונקציונלית לחלוטין) וגם תחת שרת HTTP (Vercel/מקומי). **הערה**: פקדי `/manifest.webmanifest`/`/icons/...` הקיימים מראש **כבר** נכשלים תחת `file://` (4 בקשות `ERR_FILE_NOT_FOUND` נצפו בבדיקה) — זו התנהגות **קיימת מלפני עבודה זו**, לא רגרסיה של המודולריזציה, ומחוץ להיקף התיקון (favicon/manifest בלבד, לא שוברת את האפליקציה).

**אימות שלמות חילוץ (לפני/אחרי, לא רק static)**:
- **פונקציות**: 289/289 הצהרות `function` זהות (השוואת `grep` ממוין) — אפס נשאר ב-`index.html` אחרי החילוץ, אפס אבד.
- **CSS**: 254/254 חוקי CSS (ספירת `{` פותח בבלוק הסגנון המדויק בלבד, לא בכל הקובץ) זהים בין המקור ל-`styles.css`.
- **מפתחות אחסון**: כל 7 מפתחות `family_finance_*` (`_data`/`_cat_config`/`_settings`/`_activity_log`/`_goals`/`_category_tile_order`/`_loan_balance_view`) מופיעים באותה תדירות לפני/אחרי (בין `index.html`+`app.js` יחד).
- **מזהי מסך/ניווט**: כל 12 מזהי `id="screen-*"`/`id="nav-*"` זהים.
- **Handlers**: מדגם רחב (13 שמות handler שונים, כולל `postponeGoalsReminder`/`confirmReminderFullTransfer`/`toggleGoalExpand`/`handleTxRowClick`) נבדק ב-occurrence-count מדויק — **תואם 1:1** בין המקור לבין (`index.html`+`app.js`) המשולבים; ההבדל היחיד שנצפה בבדיקת ביניים הוסבר: קריאות ה-handler בתוך מחרוזות HTML שנבנות דינמית ב-JS (כרטיסי יעד, תזכורת) עברו ל-`app.js` יחד עם שאר קוד ה-JS — כצפוי, לא אובדן.
- `node --check app.js` — תקין. `git diff --check` — נקי (ללא בעיות רווח/שורת-סוף).

**אימות דפדפן אמיתי (Edge headless, `puppeteer-core`, פרופיל זמני מבודד, שרת HTTP מקומי משרת את שורש הריפו)**:
- **טעינת משאבים**: `index.html`/`styles.css`/`app.js`/`manifest.webmanifest` — כולם `200`, `Content-Type` נכון (`text/html`/`text/css`/`application/javascript`/`application/manifest+json`). אפס בקשות כושלות.
- **קונסולה/עמוד**: אפס `console.error`/`console.warning`/`pageerror` בכל תרחיש (טעינה ראשונית, seed+reload, ניווט בין 5 מסכים, מסך יעדים, יצוא גיבוי).
- **ניווט**: כל 5 פקדי הניווט (`nav-home`/`nav-insights`/`nav-goals`/`nav-categories`/`nav-settings`) נלחצים בפועל (לא רק DOM-presence) ללא שגיאה.
- **רספונסיבי**: 360/390/768/1280px — אפס גלישה אופקית (`scrollWidth<=clientWidth`) בכל הרוחבים, עם נתוני-סימון הכוללים טקסט עברי ארוך וסכומים גדולים.
- **ערכות**: light ו-dark מוחלות עם רקעים שונים ונכונים (`rgb(244,245,247)`/`rgb(20,22,26)`) — מוכיח ש-`styles.css` החדש באמת נטען ומיושם (לא נשאר על ברירת-מחדל של דפדפן).
- **מסך יעדים**: מוצג (`display!=='none'`) ללא שגיאה.
- **Backup**: `collectAppLocalStorageBackup()` נגיש דרך `app.js` החדש ומתנהג בדיוק כפי שמתועד בהערת הקוד עצמה (`null` כש-`goalsState.valid===false`) — לא באג, לא רגרסיה.
- **השוואת Baseline הכרחית**: אותה סקריפט בדיקה בדיוק, עם אותם נתוני-סימון בדיוק, הורץ גם מול הגרסה **שלפני** הפיצול (`git show HEAD` הקודם כקובץ יחיד עצמאי) — התוצאה (`hero-amount`, `goalCardPresent`, ניווט, אפס שגיאות) **זהה לחלוטין** בין לפני/אחרי. זו ההוכחה המכרעת שהפיצול עצמו לא שינה שום התנהגות נצפית.

**Offline/Cache**: **אין Service Worker בפרויקט** (אומת, לא נוסף אחד — מחוץ להיקף ומחוץ לאישור). התנהגות offline אמיתית **אינה מובטחת** כעת שה-CSS/JS חיצוניים (בדיוק כפי שלא הייתה מובטחת גם לפני הפיצול, כשהיה קובץ יחיד — לא נוצר רגרסיה, רק לא נוסף שיפור). טעינה מקוונת רגילה (הרוב המכריע של השימוש בפועל) אינה מושפעת.

**לא בוצע ולא נבדק**: מכשיר Android/iOS פיזי אמיתי, Vercel Preview, Production. `APP_VERSION` נשאר `'1.3.1'`, `schemaVersion` נשאר `2` — ללא שינוי. אין תלות חיצונית חדשה, אין framework, אין מסך HTML נפרד לכל מסך (SPA יחיד נשמר).

**מפת דרכים**: Commit מקומי **הושלם (03/09/2026)** ← Push (ממתין לאישור מפורש) ← Production Release (ממתין לאישור מפורש).
