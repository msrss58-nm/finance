# CURRENT_STATUS.md — FamilyFinance PRO

## מצב הפרויקט כרגע
פרויקט בשלב מוקדם-בינוני. יש אפליקציה עובדת בקובץ [index.html](index.html) יחיד, עם כל הפיצ'רים המתוארים ב-README ממומשים בקוד **ומאומתים בהרצה בפועל** (ראו סעיף הבא). **קיים Git repository מקומי** בתיקייה (אותחל ואושר ב-29/07/2026), ענף `main`. אין remote, אין push.

## מה עובד — מאומת בהרצה בפועל (לא רק קריאת קוד)
בתאריך 29/07/2026 בוצעה בדיקת end-to-end אמיתית: הפעלת שרת מקומי (`python -m http.server`), טעינת index.html בדפדפן Edge headless, ואינטראקציה בפועל (מילוי טפסים, לחיצות כפתורים אמיתיות, רענון דף) דרך Chrome DevTools Protocol. כל הבדיקות הבאות **עברו בהצלחה**:
- **טעינת הדף** — נטען עד `document.readyState === 'complete'` ללא שגיאות
- **דשבורד** — מוצג עם יתרה פנויה ואריחי סיכום לכל קטגוריה
- **הוספת תנועה** — נבדק עם תנועת הכנסה: מילוי טופס + לחיצת כפתור אמיתית עדכנו את היתרה מיידית
- **שמירה ב-localStorage** — הנתונים נשמרו ושרדו רענון דף מלא (Page.navigate מחדש), מספר הפריטים והיתרה זהים לפני ואחרי
- **ניהול קטגוריות** — יצירת קטגוריה מותאמת אישית עבדה והשתקפה גם ברשימת הניהול וגם בטופס ההוספה
- **תחזית חודשית** — הגרף (SVG) הציג 6 עמודות כצפוי לאחר פתיחת האקורדיון
- **ארכיון** — ארכוב תנועה ומעבר בין הטאבים "פעילות"/"ארכיון" עבדו כמצופה
- **Console** — לא נלכדה אף שגיאה או אזהרה (`exceptionThrown`, `console.error/warn`, `Log` ברמת error/warning) לאורך כל תרחיש הבדיקה
- טעינה/שמירה ל-localStorage (מפתחות `family_finance_data`, `family_finance_cat_config`)
- עריכה inline, שחזור מארכיון, מחיקה סופית — **נבדקו בפועל (29/07/2026) על פריט מסוג `variable`** במסגרת בדיקת השינוי בלוגיקת התזרים (ראו סעיף למטה); שאר סוגי הפריטים (fixed/loan/dated/income) — עדיין לא נבדקו במפורש לעריכה/מחיקה, ראו TODO.md
- הגנת XSS בסיסית באמצעות `escapeHtml()`

הבדיקה בוצעה על פרופיל דפדפן זמני ומבודד — נתוני localStorage האמיתיים של המשתמש בדפדפן שלו לא נגעו כלל.

## app.js ו-styles.css — מאומת שאינם מקושרים ואינם בשימוש בפועל
נבדק ישירות ב-29/07/2026 על ה-HTML **כפי שנטען בפועל בדפדפן** (`document.documentElement.outerHTML`): אין שום הפניה ל-`app.js` (לא `<script src>`) ואין שום הפניה ל-`styles.css` (לא `<link>`). כלומר [app.js](app.js) ו-[styles.css](styles.css) הם קבצים כפולים לחלוטין מבחינת תוכן (עותק כמעט זהה של הלוגיקה וה-CSS מתוך index.html), אך **אינם פעילים באפליקציה בשום צורה**.

**נקודה פתוחה שנותרה ללא הכרעה** (לא הוכרעה בשלב הזה, ממתינה להנחיית המשתמש): מה לעשות עם app.js ו-styles.css — למחוק, לחבר בפועל ל-index.html, או להשאיר כקבצי טיוטה מתועדים במפורש. ראו TODO.md.

נמצא גם הבדל תחביר קטן בין הגרסאות: ב-[styles.css:47](styles.css#L47) הכלל `tabs { ... }` חסר נקודה בתחילת הסלקטור (צריך להיות `.tabs`), בעוד שבגרסה ה-inline בתוך index.html זה נכון (`.tabs { ... }`). לא משפיע על התנהגות האפליקציה כל עוד styles.css אינו נטען.

## מה לא עובד
לא זוהו תקלות בבדיקת ה-end-to-end שבוצעה (ראו זרימות שנבדקו לעיל). זרימות שטרם נבדקו בפועל (הלוואות, תשלומים, חישובי תאריכים, עריכה inline) מפורטות ב-TODO.md תחת "בדיקות עתידיות" — עד שייבדקו, מצבן נשאר "ממומש בקוד, לא מאומת בהרצה".

## תקלות ידועות
- הבדל תחביר CSS ב-[styles.css:47](styles.css#L47) שתואר לעיל — לא משפיע על index.html כי הקובץ אינו נטען בפועל.
- אין הודעות שגיאה, TODO, FIXME, או הערות "known issue" בקוד עצמו.

## שינויים מקומיים שלא נשמרו (לא ב-commit)
לפני ה-commit הנוכחי: `index.html` שונה (פיצ'ר אמצעי חיוב ל"הוצאות קבועות", ראו למטה), וקובצי התיעוד (CURRENT_STATUS.md, CHANGELOG.md, TODO.md, WORKFLOW.md) עודכנו במקביל — כולם נשמרים יחד ב-commit אחד בסיום העדכון הזה.

## שינוי לוגי #1: "תשלומים שונים" הוצאו מחישוב התזרים (29/07/2026, נשמר ב-commit `056e7c4`)
לפי בקשת המשתמש: "תשלומים שונים" (`variable`) מייצגים רכישה בכרטיס אשראי, וההשפעה בפועל על התזרים אמורה להגיע רק מהחיוב בכרטיס האשראי (שהמשתמש רושם בנפרד, ידנית, כפריט `dated` נפרד) — לא מהעסקה בתשלומים עצמה. שינוי זה מונע ספירה כפולה.

**מה שונה ב-[index.html](index.html):**
- ב-`renderAll()`: הוסרה הוספת `item.amount` ל-`monthlyExpenses` מענף `variable` — כך "תשלומים שונים" כבר לא מופחתים מ"יתרה פנויה". חישובי `catStats[cKey].monthly`/`totalLeft` (אריחי המעקב) **נשארו כפי שהיו**.
- ב-`computeForecast()`: ענף `variable` כבר לא מוסיף ל-`sumExp`.
- **לא שונה**: לוגיקת `loan`, מבנה הנתונים ב-localStorage, עריכה, ארכוב, שחזור, מחיקה, `app.js`, `styles.css`.

**בדיקה (29/07/2026, שרת מקומי + Edge headless, פרופיל מבודד):** תרחיש הכנסה ₪10,000 + כרטיס אשראי ₪2,000 + תשלומים שונים ₪500/חודש → יתרה פנויה ₪8,000 (לא ₪7,500), תחזית מפחיתה רק את חיוב הכרטיס, ארכוב/עריכה/מחיקה פועלים, נתונים ישנים נטענים ללא שינוי מבנה. כל 9 הבדיקות עברו. נמצאה שגיאת Console אחת לא-קשורה (`favicon.ico` 404, ראו TODO.md).

## שינוי לוגי #2: אמצעי חיוב ל"הוצאות קבועות" — בנק / כרטיס אשראי (29/07/2026)
לפי בקשת המשתמש: השדה הקיים "איפה יורד" (עבור `fixed` בלבד) הפך משדה טקסט חופשי לשדה בחירה (`select`) עם שתי אפשרויות בדיוק: **חשבון בנק** (ברירת מחדל) ו-**כרטיס אשראי**. בבחירת "כרטיס אשראי" מוצג שדה נוסף ומותנה ל-4 הספרות האחרונות של הכרטיס (`cardLast4`), עם ולידציה שדורשת בדיוק 4 ספרות.

**החלטה מאושרת לגבי אריח הדשבורד "הוצאות קבועות":** האריח ממשיך להציג את **סך כל** ההוצאות הקבועות — גם בנק וגם כרטיס אשראי — לצורך מעקב מלא, ללא קשר לאמצעי החיוב. לעומת זאת, **היתרה הפנויה** ו-**התחזית ל-6 חודשים** כוללות **רק** הוצאות קבועות שאמצעי החיוב שלהן הוא "חשבון בנק"; הוצאות קבועות בכרטיס אשראי ממשיכות להישמר, להופיע בפירוט, בעריכה ובארכיון, אך אינן נכללות ביתרה הפנויה או בתחזית (ההשפעה בפועל מגיעה מחיוב כרטיס האשראי שהמשתמש רושם בנפרד — כמו בשינוי #1).

**מה שונה ב-[index.html](index.html):**
- טופס הוספה: `fix-where` הפך מ-`<input type="text">` ל-`<select>` (bank/credit), עם שדה מותנה חדש `fix-card-last4`.
- פונקציה חדשה `toggleCardLast4Field()` להצגה/הסתרה של שדה 4 הספרות.
- `addNewItem()` ו-`saveInlineEdit()` (ענף `fixed`): ולידציה בדיוק 4 ספרות (`/^\d{4}$/`) בשמירה כשנבחר כרטיס אשראי; איפוס `cardLast4` ל-`''` כשנבחר בנק. שמירה נכשלת (עם `alert`, ללא איבוד נתונים) אם הערך אינו תקין.
- טופס העריכה inline: אותה בחירה + שדה מותנה, עם ברירת מחדל לפי `item.where === 'credit'`.
- תצוגת פריט: "איפה יורד" מציג כעת "חשבון בנק" או "כרטיס אשראי ****XXXX".
- `renderAll()` ו-`computeForecast()`: הוצאה קבועה עם `item.where === 'credit'` לא נכללת ב-`monthlyExpenses`/`sumExp`; `catStats[cKey].monthly` (אריח הדשבורד) **לא שונה** — ממשיך לכלול הכל, כפי שאושר.
- **לא שונה כלל**: `loan`, `variable`, `dated`, `income`, ארכיון/שחזור/מחיקה (גנריים), `app.js`, `styles.css`.

**בדיקה (29/07/2026, שרת מקומי + Edge headless, פרופיל מבודד):** תרחיש הכנסה ₪10,000 + שכירות-בנק ₪3,000 + מנוי חדר כושר-כרטיס ₪200 → יתרה פנויה ₪7,000, תחזית ₪7,000, תצוגה "כרטיס אשראי ****1234" תקינה. ולידציה חסמה הוספה/עריכה עם ערך לא-מספרי ועם פחות מ-4 ספרות. עריכה בין בנק↔כרטיס עדכנה את `cardLast4` והחישוב כראוי בשני הכיוונים. ארכוב/שחזור/מחיקה על פריט כזה פעלו. אריח "הוצאות קבועות" הציג ₪3,150 (סכום מלא, בנק+כרטיס) בעוד היתרה הפנויה נשארה ₪7,000 — בדיוק ההתנהגות שאושרה. כל 14 הבדיקות המהותיות עברו; נמצאה שוב אותה שגיאת Console לא-קשורה (`favicon.ico` 404).

## המשימה האחרונה שבוצעה
ב-29/07/2026: יישום ובדיקת שינוי לוגי #2 (אמצעי חיוב ל-fixed), ועדכון קובצי התיעוד לקראת commit משותף.

## נקודת ההמשך הנוכחית
שני השינויים הלוגיים (תשלומים שונים ← לא בתזרים; הוצאות קבועות בכרטיס אשראי ← לא בתזרים) מיושמים ונבדקים. נותרו נקודות פתוחות קודמות: (1) מה לעשות עם app.js/styles.css הלא-מקושרים, (2) בדיקות עתידיות שנותרו (הלוואות, חישובי תאריכים, עריכה/ארכוב על `loan`/`dated`/`income`), (3) שגיאת ה-Console של favicon.ico (לא דחוף, ראו TODO.md).
