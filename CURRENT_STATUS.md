# CURRENT_STATUS.md — FamilyFinance PRO

## מצב הפרויקט כרגע
פרויקט בשלב מוקדם-בינוני. יש אפליקציה עובדת בקובץ [index.html](index.html) יחיד, עם כל הפיצ'רים המתוארים ב-README ממומשים בקוד **ומאומתים בהרצה בפועל** (ראו סעיף הבא). **קיים Git repository מקומי** בתיקייה (אותחל ואושר ב-29/07/2026), ענף `main`, עם commit בסיס אחד (`chore: establish documented project baseline`). אין remote, אין push.

## מה עובד — מאומת בהרצה בפועל (לא רק קריאת קוד)
בתאריך 29/07/2026 בוצעה בדיקת end-to-end אמיתית: הפעלת שרת מקומי (`python -m http.server`), טעינת index.html בדפדפן Edge headless, ואינטראקציה בפועל (מילוי טפסים, לחיצות כפתורים אמיתיות, רענון דף) דרך Chrome DevTools Protocol. כל הבדיקות הבאות **עברו בהצלחה**:
- **טעינת הדף** — נטען עד `document.readyState === 'complete'` ללא שגיאות
- **דשבורד** — מוצג עם יתרה פנויה ואריחי סיכום לכל קטגוריה
- **הוספת תנועה** — נבדק עם תנועת הכנסה: מילוי טופס + לחיצת כפתור אמיתית עדכנו את היתרה מיידית
- **שמירה ב-localStorage** — הנתונים נשמרו ושרדו רענון דף מלא (Page.navigate מחדש), מספר הפריטים והיתרה זהים לפני ואחרי
- **ניהול קטגוריות** — יצירת קטגוריה מותאמת אישית עבדה והשתקפה גם ברשימת הניהול וגם בטופס ההוספה
- **תחזית חודשית** — הגרף (SVG) הציג 6 עמודות כצפוי לאחר פתיחת האקורדיון
- **ארכיון** — ארכוב תנועה ומעבר בין הטאבים "פעילות"/"ארכיון" עבדו כמצופה
- **Console** — לא נלכדה אף שגיאה או אזהרה (`exceptionThrown`, `console.error/warn`, `Log` ברמת error/warning) לאורך כל תרחיש הבדיקה
- טעינה/שמירה ל-localStorage (מפתחות `family_finance_data`, `family_finance_cat_config`)
- עריכה inline, שחזור מארכיון, מחיקה סופית — **נבדקו בפועל (29/07/2026) על פריט מסוג `variable`** במסגרת בדיקת השינוי בלוגיקת התזרים (ראו סעיף למטה); שאר סוגי הפריטים (fixed/loan/dated/income) — עדיין לא נבדקו במפורש לעריכה/מחיקה, ראו TODO.md
- הגנת XSS בסיסית באמצעות `escapeHtml()`

הבדיקה בוצעה על פרופיל דפדפן זמני ומבודד — נתוני localStorage האמיתיים של המשתמש בדפדפן שלו לא נגעו כלל.

## app.js ו-styles.css — מאומת שאינם מקושרים ואינם בשימוש בפועל
נבדק ישירות ב-29/07/2026 על ה-HTML **כפי שנטען בפועל בדפדפן** (`document.documentElement.outerHTML`): אין שום הפניה ל-`app.js` (לא `<script src>`) ואין שום הפניה ל-`styles.css` (לא `<link>`). כלומר [app.js](app.js) ו-[styles.css](styles.css) הם קבצים כפולים לחלוטין מבחינת תוכן (עותק כמעט זהה של הלוגיקה וה-CSS מתוך index.html), אך **אינם פעילים באפליקציה בשום צורה**.

**נקודה פתוחה שנותרה ללא הכרעה** (לא הוכרעה בשלב הזה, ממתינה להנחיית המשתמש): מה לעשות עם app.js ו-styles.css — למחוק, לחבר בפועל ל-index.html, או להשאיר כקבצי טיוטה מתועדים במפורש. ראו TODO.md.

נמצא גם הבדל תחביר קטן בין הגרסאות: ב-[styles.css:47](styles.css#L47) הכלל `tabs { ... }` חסר נקודה בתחילת הסלקטור (צריך להיות `.tabs`), בעוד שבגרסה ה-inline בתוך index.html זה נכון (`.tabs { ... }`). לא משפיע על התנהגות האפליקציה כל עוד styles.css אינו נטען.

## מה לא עובד
לא זוהו תקלות בבדיקת ה-end-to-end שבוצעה (ראו זרימות שנבדקו לעיל). זרימות שטרם נבדקו בפועל (הלוואות, תשלומים, חישובי תאריכים, עריכה inline) מפורטות ב-TODO.md תחת "בדיקות עתידיות" — עד שייבדקו, מצבן נשאר "ממומש בקוד, לא מאומת בהרצה".

## תקלות ידועות
- הבדל תחביר CSS ב-[styles.css:47](styles.css#L47) שתואר לעיל — לא משפיע על index.html כי הקובץ אינו נטען בפועל.
- אין הודעות שגיאה, TODO, FIXME, או הערות "known issue" בקוד עצמו.

## שינויים מקומיים שלא נשמרו (לא ב-commit)
נכון לרגע כתיבת שורות אלו: `index.html` שונה (לוגיקת "תשלומים שונים", ראו למטה) ועדיין **לא בוצע commit** לשינוי — ממתין לאישור המשתמש. קובצי התיעוד (CURRENT_STATUS.md, CHANGELOG.md, TODO.md) עודכנו גם הם ועדיין לא ב-commit.

## שינוי לוגי אחרון: "תשלומים שונים" הוצאו מחישוב התזרים (29/07/2026)
לפי בקשת המשתמש: "תשלומים שונים" (`variable`) מייצגים רכישה בכרטיס אשראי, וההשפעה בפועל על התזרים אמורה להגיע רק מהחיוב בכרטיס האשראי (שהמשתמש רושם בנפרד, ידנית, כפריט `dated` נפרד) — לא מהעסקה בתשלומים עצמה. שינוי זה מונע ספירה כפולה.

**מה שונה ב-[index.html](index.html):**
- ב-`renderAll()` (סביב שורה 854): הוסרה השורה `monthlyExpenses += item.amount;` מענף `variable` — כך "תשלומים שונים" כבר לא מופחתים מ"יתרה פנויה". חישובי `catStats[cKey].monthly` (אריח "יורד החודש") ו-`catStats[cKey].totalLeft` (אריח "סך הכל יתרה") **נשארו כפי שהיו** — התצוגה, המעקב והפירוט לא נפגעו.
- ב-`computeForecast()` (סביב שורה 710): ענף `variable` כבר לא מוסיף ל-`sumExp` — התחזית ל-6 חודשים כבר לא מפחיתה "תשלומים שונים".
- **לא שונה**: לוגיקת `loan` (הלוואות), מבנה הנתונים ב-localStorage, עריכה, ארכוב, שחזור, מחיקה, `app.js`, `styles.css`.

**בדיקה שבוצעה (29/07/2026, שרת מקומי + Edge headless על פרופיל זמני ומבודד):**
תרחיש: הכנסה ₪10,000 + חיוב כרטיס אשראי (`dated`) ₪2,000 + תשלומים שונים ₪500/חודש. כל 9 הבדיקות עברו:
- יתרה פנויה = **₪8,000** (לא ₪7,500) ✅
- "תשלומים שונים" ממשיך להציג ₪500, סכום מקור, מספר תשלומים שנותרו ותאריך סיום ✅
- תחזית: חודש נוכחי = ₪8,000 (רק כרטיס האשראי מופחת), חודש הבא = ₪10,000 (התשלומים לא מופחתים) ✅
- ארכוב/שחזור, עריכה, מחיקה של פריט `variable` — כולם פועלים כרגיל, והיתרה לא משתנה עקב עריכה ✅
- נתונים בפורמט ישן (שמדמים משתמש קיים לפני השינוי) נטענו ללא שגיאה, עם כל השדות המקוריים ✅
- **נמצאה שגיאת Console אחת, לא קשורה לשינוי**: `Failed to load resource: 404` עבור `/favicon.ico`. אומת מול log השרת המקומי (`GET /favicon.ico 404`) — זהו ניסיון אוטומטי של הדפדפן לשלוף `favicon.ico` כקובץ בנתיב השורש, בנוסף לאייקון ה-data-URI המוגדר ב-`<link rel="icon">`. **לא קשור ללוגיקת החישוב**, קיים גם ללא השינוי הזה, ואין לו השפעה תפקודית — רק רישום ב-Console. נוסף ל-TODO.md כפריט נמוך-עדיפות.

## המשימה האחרונה שבוצעה
ב-29/07/2026: יישום שינוי לוגי ב-`index.html` (ניתוק "תשלומים שונים" מהתזרים והתחזית), בדיקת end-to-end מלאה לתרחיש הספציפי שהוגדר, ועדכון קובצי התיעוד. **טרם בוצע commit** לשינוי — ממתין לאישור מפורש.

## נקודת ההמשך הנוכחית
השינוי בקוד בוצע ונבדק בהצלחה, אך עדיין לא נשמר ב-Git (`git status` יראה `index.html` כ-modified ביחס ל-commit הבסיס). ממתין לאישור המשתמש לביצוע commit. נותרו גם נקודות פתוחות קודמות: (1) מה לעשות עם app.js/styles.css הלא-מקושרים, (2) בדיקות עתידיות שנותרו (הלוואות, חישובי תאריכים, עריכה inline על סוגים נוספים), (3) שגיאת ה-Console של favicon.ico (לא דחוף).
