# PROJECT_CONTEXT.md — FamilyFinance PRO

## מטרת הפרויקט
אפליקציית ווב לניהול תקציב משפחתי. מטרתה לתת תמונה מיידית של "יתרה פנויה חודשית" (הכנסות פחות הוצאות), לנהל סוגי תנועות כספיות שונות (הכנסות, הוצאות קבועות, תשלומים, הלוואות, חיובים חד-פעמיים), ולספק תחזית ל-6 חודשים קדימה.

האפליקציה מתוכננת להיות **קלה, ללא שרת, ללא build, וללא תלות בספריות חיצוניות** — ניתן לפתוח את `index.html` ישירות בדפדפן (גם דרך `file://`, ללא שרת), בתנאי ש-`styles.css`/`app.js` נשארים באותה תיקייה (ראו "עדכון 03/09/2026 — Milestones 8–9" למטה).

## קהל היעד
משתמש בודד / משפחה המנהלים את התקציב שלהם באופן עצמאי, ללא צורך בהתקנה, שרת, או חשבון. השימוש מיועד למכשיר ודפדפן בודדים (הנתונים אינם מסונכרנים בין מכשירים).

## מבנה המערכת
- **עדכון 03/09/2026 — Milestones 8–9 (מודולריזציה)**: המבנה **כבר אינו קובץ יחיד**. [index.html](index.html) מכיל כעת מבנה/markup בלבד ו-2 הפניות (`<link rel="stylesheet" href="styles.css">`, `<script src="app.js"></script>`, נתיבים יחסיים במפורש); [styles.css](styles.css) הוא כעת ה-CSS המלא והבלעדי; [app.js](app.js) הוא כעת ה-JavaScript המלא והבלעדי. פירוק מבני טהור — ללא שינוי לוגיקה/פונקציה/חישוב כלשהו, ראו CURRENT_STATUS.md, סעיף "Milestones 8–9", לפירוט האימות המלא. האפליקציה נשארת SPA יחיד (אין מסמכי HTML נפרדים למסכים), וללא build/bundler/framework/תלות חיצונית חדשה.
- **קובץ יחיד ומרכזי (היסטורי, לפני 03/09/2026)**: [index.html](index.html) — הכיל HTML + CSS (בתוך `<style>`) + JavaScript (בתוך `<script>`), הכל inline. זהו היה הקובץ שרץ בפועל בדפדפן.
- **עדכון 31/07/2026 — Cutover הושלם (Version 1.0)**: `index.html` הוא כעת ארכיטקטורת ה-**Cockpit** (בעת הסגירה: 4 מסכים — בית/תנועות/תובנות/הגדרות + ניווט תחתון + FAB Quick Actions), עם מפתחות ה-localStorage המקוריים (`family_finance_data`/`family_finance_cat_config`). זהו תוצר Cutover מ-`index-preview-v2.html` (commit `c63a43e`). מבנה הנתונים (`items`/`categoryConfig`) ולוגיקת החישוב הבסיסית **לא השתנו** — רק העטיפה/הממשק. ה-`index.html` ה"ישן" (טרום-Cockpit) נשמר אך ורק דרך Git — Tag מקומי `pre-preview-v2-cutover` → commit `72a315a`.
- **עדכון 03/08/2026 — Version 1.1**: לאחר Version 1.0, הפיתוח המשיך ישירות על `index.html`. המסך "הגדרות" הישן שינה שם ל-**"קטגוריות"**, ונוסף מסך **"הגדרות"** נפרד (ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.1").
- **עדכון 03/08/2026 — Version 1.2 (UI/UX Refinement)**: טאב "תנועות" הוסר מהניווט התחתון — **4 טאבים כעת** (בית/תובנות/קטגוריות/הגדרות); צפייה בכל התנועות היא שורת מערכת "📋 כל התנועות" בראש מסך קטגוריות. מסך "הגדרות" פוצל מרשימה שטוחה לתפריט 7 נושאים (עם תיאור קצר לכל אחד) + מסכי-משנה ייעודיים עם כפתור "← חזרה". סה"כ **6 מסכים כעת** (5 טאבים לוגיים + מסך-משנה אחד ללא טאב עצמאי — ראו "המסכים והמודולים" למטה, מעודכן). ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.2".
- **עדכון 31/08/2026 — Version 1.3 (מנוע תזרים מזומנים מאוחד + עוגן יתרה + מסך תובנות חדש)**: נוסף מודול חישוב חדש ואדיטיבי (`generateCashflowEvents`/`buildCashflowSummary`) שמייצר תחזית תזרים מעוגנת ל**עוגן יתרה** — `anchorBalance`+`anchorDate` (שני שדות ב-`family_finance_settings` הקיים; "ביום anchorDate היתרה הייתה anchorBalance"). המשתמש מזין יתרה **פעם אחת** בהתחלה; "יתרה משוערת להיום" מחושבת אוטומטית קדימה מהעוגן, ללא צורך בהזנה יומית; פעולת "עדכן יתרה בפועל" מבצעת פישוב (עוגן חדש = הסכום שהוזן + היום). שדה `currentBalance` הישן (מודל ביניים קודם באותה גרסה) נשאר בסכימה כ-fallback קריאה-בלבד בלבד לתאימות לאחור — ראו `appSettings` למטה. מסך **תובנות** קיבל hero "יתרה משוערת להיום"/"הגדר יתרת בסיס" + טופס עוגן, 5 כרטיסי סיכום, וציר תזרים מתרחב, כולם מבוססי המנוע החדש בלבד. מסך **הבית נשאר ללא שינוי במכוון** — ממשיך לייצג "תמונת מצב חודשית" (מושג שונה מהותית מתחזית התזרים המעוגנת-יתרה של תובנות). `APP_VERSION` עודכן ל-`'1.3.0 Preview'`, ולאחר אימות Preview ידני שוחרר ל-Production ב-`'1.3.0'` (01/09/2026, commit `ee6701ae276c0fba668c7c23a345c528e64326ab`, tag `v1.3.0`). **תיקון עובדתי (02/09/2026, Milestone 7.1)**: מאז אותו שחרור, שני commits נוספים (`bb6d8cd`, `3b93d86` — תיקון שחזור-גיבוי + תקציר קטגוריות, "Version 1.3.1") כבר הגיעו ל-`origin/main`/Production לפני תחילת השיחה הנוכחית — **Production הידוע-אחרון הוא כעת `APP_VERSION 1.3.1`** (`schemaVersion:1`, ללא Goals), לא 1.3.0. עבודת Milestones 3–6 (מסך תחזית/יעדים/תזכורת/`schemaVersion 2`) שנבנתה לאחר מכן נשארת מקומית ולא-נדחפת. ראו CURRENT_STATUS.md/CHANGELOG.md, סעיף "Version 1.3" (כולל Phase 2E — עוגן יתרה, המודל הסופי) ו-"Milestones 3–6".
- **[index-preview-v2.html](index-preview-v2.html)** ("Preview v2" / "Cockpit", tracked): הגרסה שממנה בוצע ה-Cutover — עדיין קיימת בריפו במקביל, עם מפתחות localStorage **נפרדים** (`family_finance_preview_v2_*`), לצורכי השוואה/רגרסיה. **לא נמחקת עדיין** (הוחלט בשלב 4.6) — ניקוי יהיה שלב עתידי נפרד, רק לאחר תקופת אימות חי.
- ~~**קבצים נפרדים קיימים אך לא מקושרים בפועל**~~ — **היסטורי, תוקן ב-Milestones 8–9 (03/09/2026)**: [app.js](app.js) ו-[styles.css](styles.css) הכילו עד אז עותק **ישן ושגוי** של הלוגיקה/ה-CSS (לא מקושרים ל-`index.html`, אסורים כמקור העתקה). כעת **הוחלפו במלואם** בתוכן המדויק שחולץ מ-`index.html` החי, ו-`index.html` **כן** מקושר אליהם בפועל. ראו למעלה ("עדכון 03/09/2026") ו-CURRENT_STATUS.md לפירוט האימות.
- **[index-preview.html](index-preview.html)** ("Preview v1", untracked): ניסיון עיצוב ראשון מוקדם יותר — התברר כמתיחת-פנים על המבנה הישן, נזנח לטובת Preview v2. ראו CURRENT_STATUS.md.
- **[Design/](Design/)** (untracked, לא נוצר על ידי Claude): 5 מסכי Google Stitch (Tailwind CDN + נכסים ממותגים) ששימשו כהשראה עיצובית בלבד. **לא לשימוש ישיר בקוד** — CDN חיצוני ונכסים ממותגים (לוגואי בנקים/Apple/Netflix) שאין להכניס לאפליקציה. ראו CURRENT_STATUS.md לניתוח מלא.
- **[README.md](README.md)**: תיעוד קיים מהמפתח הקודם, מתאר את הפיצ'רים ברמת המשתמש (מתאר את הגרסה הטרום-Cockpit — לא עודכן בעקבות ה-Cutover).

## עיצוב Cockpit — הושלם והוא הארכיטקטורה הפעילה (31/07/2026)
העיצוב המלא (לא reskin) הושלם ועבר Cutover ל-`index.html`. העקרונות שאושרו ויושמו:
- **עקרון 10 השניות**: פתיחת האפליקציה עונה מיידית (בלי אינטראקציה) על "מה מצבי / האם אני בסדר / מה הבא שדורש תשומת לב".
- **מטאפורת Cockpit**: לוח מחוונים (מד ראשי + מדי-משנה + נורות אזהרה + פעילות אחרונה), לא רשימת נתונים.
- **עקרון "סיפור הכסף"**: כל מספר מלווה במשפט אנושי (מצב/סיבה/מה בקרוב/מה דורש תשומת לב), ברמת דיוק **תואמת בדיוק** למה שהנתונים בפועל מאפשרים (למשל: `loan`/`dated` יש להם תאריך מדויק → "בעוד X ימים"; `fixed` אין לו יום-בחודש שמור → רק "צפוי החודש", לעולם לא תאריך מומצא).
- **תובנות מבוססות תזרים/התחייבויות בלבד** — לא הרגלי-צריכה/קטגוריות-צריכה, כי האפליקציה לא אוספת פירוט ברמת קנייה בודדת.
פירוט מלא של ה-Wireframe, מיפוי כל תובנה לנתון קיים, ותוכנית המימוש המלאה (כולל רצף ה-Cutover 4.4–4.8) — ב-CURRENT_STATUS.md.

## הטכנולוגיות
- HTML5, CSS3 (קובץ `styles.css` נפרד מ-03/09/2026 — Milestones 8–9; היה inline `<style>` לפני כן), JavaScript ES5-style (var, function declarations — לא משתמשים ב-class/let/const/arrow functions/מודולים; קובץ `app.js` נפרד מ-03/09/2026, script רגיל ולא `type="module"`)
- ללא framework, ללא bundler, ללא package.json, ללא node_modules
- אחסון נתונים: `localStorage` בדפדפן בלבד. **7 מפתחות נכון ל-02/09/2026**: `family_finance_data`, `family_finance_cat_config` (מקוריים); `family_finance_category_tile_order` (סדר אריחי בית, Version 1.1); `family_finance_loan_balance_view` (תצוגת קרן/סה"כ, Version 1.1); `family_finance_settings`, `family_finance_activity_log` (מסך הגדרות, שלב 4.0.3); `family_finance_goals` (יעדי חיסכון, Milestone 4, 02/09/2026, ראו "מבנה הנתונים" למטה) — כולם עם קידומת `family_finance_`, נכללים באופן גורף בגיבוי/שחזור JSON (backup `schemaVersion:2` מאז הוספת `family_finance_goals`). **עדכון (Version 1.3.1)**: `family_finance_loan_balance_view` נכתב כעת בקוד JSON תקין (`JSON.stringify`, כמו כל שאר המפתחות) — קריאה תומכת גם בצורת legacy גולמית קודמת, ללא כתיבה מחדש. שחזור גיבוי (`confirmRestoreBackup()`) הוא כעת **best-effort transactional restore עם compensating rollback**: צילום-מצב של כל מפתחות `family_finance_*` נלקח לפני כל כתיבה, ובכשל כתיבה מתבצע ניסיון שחזור מלא של המצב הקודם (לא אטומיות אמיתית — ל-localStorage אין עסקה מובנית על פני כמה מפתחות; כשל גם ב-rollback עצמו מדווח במפורש כשגיאה קריטית, לעולם לא כהצלחה כוזבת).
- RTL ועברית כברירת מחדל (`<html lang="he" dir="rtl">`)
- גרפים: SVG בנוי ידנית (לא ספריית גרפים חיצונית)

## המסכים והמודולים (ארכיטקטורת Cockpit, מעודכן ל-02/09/2026 — Milestone 4)
מעטפת אפליקציה קבועה (כותרת עליונה + ניווט תחתון עם **5 טאבים** (בית/תחזית/יעדים/קטגוריות/הגדרות, מאז הוספת "יעדים" ב-Milestone 4) + כפתור הוספה צף/FAB, הקשרי). **`showScreen(name)` תומכת ממשק במסכים נוספים ללא טאב עצמאי** — `transactions` ו-`settings-detail` מגיעים אך ורק ניווט תוכני (לחיצה על שורה/כפתור, לא טאב), ו-`SCREEN_NAV_ALIAS` (Version 1.2, Stage A) שומר על הדגשת הטאב "אב" הרלוונטי (קטגוריות/הגדרות בהתאמה) בזמן שהם מוצגים:
1. **בית (Cockpit)** — מד ראשי ("כמה פנוי לי להוציא", `getMonthSnapshot()` — **נשאר ללא שינוי ב-Version 1.3 במכוון**, מושג "תמונת מצב חודשית" שונה מהותית מתחזית התזרים המעוגנת-יתרה של מסך תובנות, ראו שם), Snapshot חודשי מצומצם (הכנסות/הוצאות — היתרה כבר במד הראשי), **התראות בתוך האפליקציה** (3 סוגים, ראו "הגדרות" למטה), "מה דורש תשומת לב" — **Version 1.3**: כרטיס "האירוע הכספי הבא" בלבד (`buildCashflowSummary().nextEvent`, מידע ניטרלי לא-הערכתי); כרטיס "אזהרת תחזית" הישן (`computeForecast`-מבוסס) **הוסר לחלוטין** מכאן כדי שלא יסתור את hero-amount באותו מסך — מסך תובנות הוא הבעלים הבלעדי של אזהרות תחזית קדימה כעת. רשת **אריחי קטגוריה** (ניתנים לסינון-בלחיצה וסידור-מחדש בגרירה, סדר נשמר ב-`family_finance_category_tile_order`) + 2 **אריחי-סטטיסטיקה** קבועים ("יתרת תשלומים שונים", "יתרת הלוואות" עם toggle קרן/סה"כ נשמר ב-`family_finance_loan_balance_view`), "פעילות אחרונה".
2. **תנועות** (`screen-transactions`, **ללא טאב ניווט עצמאי מ-Version 1.2**) — נגיש רק דרך שורת המערכת "📋 כל התנועות" (ללא סינון קטגוריה) או דרך אריח/שורת קטגוריה ("עמוד קטגוריה", עם סינון). רשימה כרונולוגית מאוחדת (id יורד), מסנן פעיל/ארכיון + **סינון-לפי-קטגוריה אופציונלי**; כותרת המסך משתנה בהתאם. אזור הוספה ייעודי (Quick Actions: הכנסה/הוצאה קבועה/משתנה/מתוארכת/הלוואה; בעמוד קטגוריה ה-FAB פותח טופס ישיר לאותה קטגוריה, **מוסתר** ב"כל התנועות" הלא-מסונן — כפי שהיה גם קודם), **לחיצה על כל שורה פעילה פותחת עריכה inline** (לא דרך תפריט), תפריט ⋮ לכל שורה (ארכוב/שחזור, מחיקה סופית עם `confirm()`). **ארכוב אוטומטי**: הלוואות/תשלומים שונים שכל תשלומיהם הסתיימו עוברים לארכיון בטעינת הדף (טוסט לא-חוסם, לא `alert()`).
3. **תחזית (📈, לשעבר "תובנות" — שם הוחלף ב-Milestone 3, 02/09/2026, אותו `screen-insights`, לא מסך כפול)** — **Version 1.3**: מסך זה הוא כעת התצוגה הקדימה (forward-looking) הרשמית של תזרים המזומנים, מעוגנת ל**עוגן יתרה** (`anchorBalance`+`anchorDate` — לא יתרה מוזנת-מחדש בכל פעם). Hero דו-מצבי: לפני הגדרה — "הגדר יתרת בסיס" + טופס גלוי ישירות; אחרי הגדרה — "יתרה משוערת להיום" (מחושבת אוטומטית, לא מוזנת ידנית) + תאריך העוגן מוצג בעדינות + כפתור "עדכן יתרה בפועל" (פישוב — טופס מכווץ כברירת מחדל, כולל אחרי רענון דף). 5 כרטיסי סיכום (האירוע הכספי הבא / ההכנסה הבאה / כמה צפוי לרדת עד ההכנסה הבאה / נקודת השפל / תחזית סוף חודש — כולם מבוססי `buildCashflowSummary()` בלבד), ציר תזרים מתרחב (`summary.timelineEvents`), ושני כרטיסי-מעקב שנשארו כפי שהיו (חיובי כרטיס אשראי — `getFixedCreditCardTotals`, יתרת הלוואות — `getLoansRemainingSummary`, שני מושגי tracking שונים מהותית מתזרים ולא הוחלפו) + כרטיס "אזהרת תחזית" (כעת מבוסס המנוע החדש — ראו "לוגיקה עסקית חשובה" למטה). Forecast: גרף SVG + טבלה, תחזית 6 חודשים קדימה — מבוסס כעת על `buildCashflowSummary()` (היה `computeForecast()` עד Version 1.3).
4. **יעדים (🎯, `screen-goals`, חדש — Milestone 4, 02/09/2026)** — מסך יעדי חיסכון עצמאי, מושג נפרד לגמרי מתזרים/הוצאות (ראו `goals` ב-"מבנה הנתונים" למעלה לפירוט מלא). רשימת יעדים פעילים/ארכיון (טאב-מסנן, כמו תנועות), כרטיס-לכל-יעד עם הרחבה/כיווץ בלחיצה (וגם במקלדת — Enter/Space, `role="button" tabindex="0"`), טופס יצירה/עריכה inline, ניהול רכיבים (הוספה/עריכה/הסרה) עם מועד-יעד עצמאי לכל רכיב, ארכוב/שחזור. תזכורת חיסכון חודשית (Milestone 5) — מודל בפני עצמו, נפתח אוטומטית מעל כל מסך ב/אחרי ה-2 לחודש.
5. **קטגוריות** — ניהול קטגוריות מלא: הוספה/שינוי שם/מחיקה (נחסמת אם יש תנועות, כולל בארכיון), **יום כניסה/ירידה ברירת מחדל אופציונלי** (`defaultDayOfMonth`, מוסתר לקטגוריות `dated`), טפסים inline בתוך הממשק (ללא `prompt()` ילידי). **Version 1.2**: שורת מערכת קבועה "📋 כל התנועות" בראש המסך (מעל רשימת הקטגוריות עצמה), `onclick="filterTransactionsByCategory(null)"` — הכניסה היחידה שנותרה לתצוגת-כל-התנועות הלא-מסוננת אחרי הסרת טאב "תנועות".
6. **הגדרות** — **Version 1.2**: פוצל מרשימה שטוחה לתפריט 7 נושאים (`screen-settings`, כל שורה עם אייקון+שם+**תיאור קצר**+שברון, ע"י `SETTINGS_TOPICS`) + מסך-משנה יחיד וגנרי (`screen-settings-detail`, כותרת+**כפתור "← חזרה"**+גוף) שמציג נושא אחד בכל פעם ע"י קריאה לאחת מ-8 פונקציות `buildXSectionHtml()` הקיימות **ללא שינוי בתוכנן**. סדר הנושאים: אבטחה (PIN אופציונלי + hash בלבד + Auto-lock; **ללא ביומטריה בכל צורה**) → מראה (ערכת נושא + צבע ראשי מפלטה קבועה של 6 + גודל גופן) → התראות (בתוך האפליקציה בלבד, 3 סוגים) → נתונים (גיבוי/שחזור JSON מלא + ייצוא CSV + איפוס עם מסך אישור ייעודי) → יומן פעילות (עד 200 רשומות, 9 סוגי פעולה — נושא עצמאי, לא בתוך אודות/נתונים) → אפשרויות ניסיוניות (תשתית ריקה כרגע) → אודות (`APP_VERSION`, "מה חדש").

**Quick Actions (FAB)**: לחיצה פותחת 3 כפתורים (הכנסה/הוצאה/הלוואה) — מוסתר לגמרי במסך הבית, מוצג רק בעמוד קטגוריה (`screen-transactions` עם סינון פעיל). "הוצאה" עוברת דרך בורר סוג (קבוע/משתנה/מתוארך). כל פעולת כתיבה (הוספה/עריכה/ארכוב/שחזור/מחיקה/ניהול קטגוריות) פעילה במלואה ושומרת ל-`family_finance_data`/`family_finance_cat_config`.

## מבנה הנתונים

### `categoryConfig` (נשמר תחת `family_finance_cat_config`)
אובייקט שממופה מפתח קטגוריה → `{ label: string, baseType: string, defaultDayOfMonth?: number }`.
קטגוריות מובנות: `income`, `fixed`, `variable`, `loan`.
קטגוריות מותאמות אישית: מפתח בפורמט `custom_<timestamp>`.
`baseType` אפשרי: `income` | `fixed` | `variable` | `loan` | `dated`.
`defaultDayOfMonth` (**חדש, Version 1.1 שלב 4.0.2**): מספר 1–31, אופציונלי — נשמר רק כשתקין; לא רלוונטי לקטגוריות `dated`. משמש כברירת מחדל ל-`item.day` דרך `resolveEffectiveDay()` כשהפריט עצמו לא הגדיר יום.

### `items` (נשמר תחת `family_finance_data`)
מערך אובייקטים, כל אחד עם `id` (timestamp), `type` (=baseType), `displayCategory` (מפתח הקטגוריה), `isArchived` (bool), ושדות ספציפיים לפי סוג:
- **income**: `title`, `amount`, `day` (**חדש, Version 1.1** — "יום כניסה", אופציונלי)
- **fixed**: `title`, `amount`, `where`, `period` (חודשי/שנתי), `notes`, `cardLast4`, `day` (**חדש, Version 1.1**, אופציונלי)
- **variable**: `title`, `originalAmount`, `amount` (חודשי), `total` (מס' תשלומים), `start` (תאריך), `day` (**חדש, Version 1.1**, אופציונלי)
- **loan**: `title`, `originalAmount`, `amount`, `where`, `interest`, `day` (יום חיוב בחודש — קיים מקודם, לא חדש), `total`, `start`
- **dated**: `title`, `amount`, `start` (תאריך חיוב)
- **משותף לכולם (`archiveReason`/`archivedAt`, חדש, Version 1.1 שלב 4.0.2.2)**: `archiveReason` (`'completed'` — ארכוב אוטומטי, או `'manual'` — ארכוב ידני, מוגדר גם לפריטים לא-loan/variable מאז השלב הזה) ו-`archivedAt` (תאריך YYYY-MM-DD). שני השדות נמחקים בשחזור מהארכיון. **ללא migration** — פריטים בארכיון מלפני השלב הזה נשארים בלי השדות; פריטים ללא `item.day` נופלים לשרשרת `resolveEffectiveDay()` (`item.day → categoryConfig[key].defaultDayOfMonth → 1`) בזמן קריאה/רינדור בלבד, לא נכתבים מחדש.

### `appSettings` (נשמר תחת `family_finance_settings`, חדש — Version 1.1 שלב 4.0.3)
`{ theme, primaryColor, fontSize, pinHash, pinEnabled, autoLockMinutes, currentBalance, anchorBalance, anchorDate, notifications: {upcomingPayment, upcomingIncome, completedObligation}, experimentalFlags: {} }`. `pinHash` הוא SHA-256 hex בלבד (`crypto.subtle.digest`) — ה-PIN הגולמי לעולם לא נשמר.

`anchorBalance`/`anchorDate` (**חדש, Version 1.3, Phase 2E — המודל הסופי**): `anchorBalance` (מספר או `null`) + `anchorDate` (מחרוזת `YYYY-MM-DD` מקומית או `null`) — "ביום anchorDate היתרה האמיתית הייתה anchorBalance". `null`/`null` פירושו "לא הוגדר עוגן מעולם". המשתמש מזין את היתרה **פעם אחת** (הגדרה ראשונית או פישוב מפורש דרך "עדכן יתרה בפועל") — לא נדרשת הזנה חוזרת; "יתרה משוערת להיום" מחושבת אוטומטית מהעוגן קדימה. משמשות את מנוע התזרים (`buildCashflowSummary()`/`resolveBalanceAnchor()`) — ראו "לוגיקה עסקית חשובה" למטה.

`currentBalance` (**legacy, Version 1.3 Phase 2B/2C, הוחלף ב-anchorBalance/anchorDate ב-Phase 2E**): מספר או `null`, נשאר בסכימה **ללא שינוי** לתאימות לאחור בלבד — אינו נכתב יותר ע"י אף UI חי; נקרא רק כ-fallback קריאה-בלבד ע"י `resolveBalanceAnchor()` כאשר אין `anchorBalance`/`anchorDate` תקינים, ומטופל כעוגן legacy מתוארך "היום" (ללא תאריך היסטורי מומצא, ללא כתיבה אוטומטית חזרה).

### `activityLog` (נשמר תחת `family_finance_activity_log`, חדש — Version 1.1 שלב 4.0.3)
מערך `{ts, action, detail}`, מוגבל ל-200 רשומות אחרונות (`ACTIVITY_LOG_MAX`). `action` אחד מ-9 ערכים קבועים: `category_created`/`category_renamed`/`category_deleted`/`default_day_changed`/`auto_archive`/`manual_archive`/`restore`/`backup`/`data_restore`. לעולם לא כולל PIN/hash/סודות.

### `goals` (נשמר תחת `family_finance_goals`, חדש — Milestone 4, 02/09/2026)
מערך אובייקטי-יעד. **מושג נפרד לגמרי מ-`items`**: יעד חיסכון עצמאי לקראת מטרה עתידית (רכב/חופשה/וכו'), לא הוצאה/הכנסה/הלוואה שוטפת — לא נכלל בזרם אירועי התזרים (`generateCashflowEvents`) ולא בתחזית.
- כל יעד: `{id, title, dueDate (YYYY-MM-DD), targetAmount, savedAmount, components: [], isArchived, createdAt, updatedAt, confirmedTransfers: []}`.
- **ללא רכיבים**: `targetAmount` מוזן ישירות. **עם רכיבים**: `targetAmount` הוא **תמיד** סכום הרכיבים (מקור-אמת יחיד, לא שדה עצמאי-לעריכה) — כל רכיב `{id, name, amount, dueDate|null}` עם מועד-יעד עצמאי משלו (יורש את תאריך היעד של ההורה כש-`dueDate` הוא `null`).
- **הקצאה בין רכיבים** (לצורך תכנון קצב חיסכון): earliest-deadline-first — החיסכון הנצבר (`savedAmount`+סכום `confirmedTransfers`) מוקצה לרכיב עם המועד הקרוב ביותר קודם, במלואו, לפני שהיתרה עוברת לרכיב הבא.
- `confirmedTransfers`: ledger של העברות מאושרות ע"י תזכורת Milestone 5 (ראו למטה) — `{date, amount}` חובה (תואם-לאחור legacy); `id`/`confirmedAt`/`reminderPeriod (YYYY-MM)`/`source` אופציונליים, נוספים ע"י התזכורת עצמה.
- **ולידציה strict אמיתית** (`isValidGoalsArrayStrict`/`normalizeGoal`/`normalizeComponent`, הוקשחו ב-Milestone 6): כל שדה נבדק לפי טיפוס גולמי (`typeof`) לפני כל trim/עיגול — אין coercion של מחרוזת-מספרית ל-number, אין ID אוטומטי ליעד/רכיב חסר-id, אין ברירת-מחדל שקטה ל-`createdAt`/`updatedAt` חסרים; יעד-עם-רכיבים דורש התאמה מדויקת בין `targetAmount` שמור לסכום הרכיבים. כשל ולידציה = הדאטה המקומי כולו נחשב פגום (all-or-nothing, לא סינון חלקי) — אותה פונקציה משמשת גם לטעינה מקומית וגם לשחזור גיבוי V2.
- **תזכורת חיסכון חודשית** (Milestone 5): נפתחת אוטומטית ב/אחרי ה-2 לחודש לכל יעד לא-מטופל בתקופה הנוכחית. דחייה/Escape — session-only, per-goal/per-period (טוקנים in-memory, לא persisted), לא גלובלי.

## לוגיקה עסקית חשובה
- **יתרה פנויה חודשית** = סכום כל ה-`income` פחות כל ההוצאות הפעילות לחודש הנוכחי (לא כולל תנועות בארכיון).
- **תשלומים/הלוואות**: חישוב "כמה תשלומים נותרו" ו"תאריך סיום" מבוסס על פונקציות `getBillingRange` + `parseDatesAndGetLeft`, שמחשבות את חודש החיוב הראשון בפועל (תלוי אם יום ההתחלה >= יום החיוב), ואז סופרות חודשים שעברו ביחס להיום.
- **הוצאה קבועה שנתית**: מחולקת ל-12 בחישוב החודשי (`amount / 12`).
- **תחזית 6 חודשים** (מסך "תחזית", מעודכן — Milestone 3/6, 02/09/2026): `computeForecast()` הישן **הוסר** (הוכח קוד מת ב-Milestone 6 audit — call-graph מלא הראה אפס קריאה חיה). התחזית הכעת-יחידה מבוססת כולה על `buildCashflowSummary()`/מנוע התזרים המאוחד: כל חודש = `endOfMonth - startingBalance` מאותו זרם אירועים אחד שמזין גם את תצוגת 30/60/90 יום — אין יותר שני מנועי-תחזית עצמאיים באפליקציה.
- **`getClampedBillingDate()`** (Version 1.1, שלב 4.0.2): `getBillingRange()` "קולט" יום חיוב לא-תקין לחודש נתון (למשל 31 בפברואר) ליום האחרון בפועל של אותו חודש, במקום לתת ל-`Date` לגלוש לחודש הבא — תיקון רטרואקטיבי לכל חישובי טווח חיוב.
- **`resolveEffectiveDay(item)`** (Version 1.1, שלב 4.0.2): שרשרת נפילה מרכזית ליום החיוב/כניסה האפקטיבי — `item.day` תקין → אחרת `categoryConfig[item.displayCategory].defaultDayOfMonth` → אחרת `1`.
- **ארכוב אוטומטי** (Version 1.1, שלב 4.0.2.2): בכל טעינת דף, הלוואות/תשלומים-שונים פעילים שכל תשלומיהם כבר חויבו (`parseDatesAndGetLeft().left === 0`) והתאריך היום אחרי תאריך החיוב האחרון בפועל — עוברים אוטומטית לארכיון (`archiveReason:'completed'`), עם טוסט לא-חוסם (לא `alert()`).
- **מחיקת קטגוריה מותאמת אישית** — **בארכיטקטורת Cockpit הנוכחית, נחסמת** אם יש לה תנועות (כולל בארכיון); לעולם לא מוחקת תנועות אוטומטית (החלטת מוצר מפורשת, שונה מהתנהגות `index.html` הטרום-Cockpit הישן).
- **XSS protection**: כל טקסט המוזן ע"י המשתמש עובר `escapeHtml()` לפני הזרקה ל-innerHTML.
- **מנוע תזרים מזומנים מאוחד + עוגן יתרה (`generateCashflowEvents`/`buildCashflowSummary`/`resolveBalanceAnchor`/`setBalanceAnchor`, חדש — Version 1.3)**: שכבה נוספת ואדיטיבית (לא מחליפה את `getMonthSnapshot`/`computeForecast` הקיימות, שממשיכות לשרת את מסך הבית ואת אריחי הקטגוריה כפי שהיו). בונה זרם אירועים אחד (`{date, amount, itemId, type, title}`) מ-income/fixed/loan/dated (fixed **כולל** credit במכוון; loan לפי `resolveEffectiveDay`+טווח גבייה פעיל; variable **לעולם לא** מייצר אירוע — tracking-only). שני שלבי חישוב מזרם האירועים האחד הזה: (1) **"יתרה משוערת להיום"** = `anchorBalance` + כל אירוע אחרי `anchorDate` בדיוק ועד היום (כולל) — אירועים ביום העוגן ולפניו מוחרגים לחלוטין; (2) **תחזית קדימה** ל-6 חודשים, ממחר, החל מ"יתרה משוערת להיום" (לא מהעוגן הגולמי). **גבול "היום" בתחזית קדימה**: אירוע שתאריכו הוא היום מוחרג לחלוטין מהתחזית קדימה — מכיוון שאין ledger/timestamp שיוכיח אם כבר קרה בפועל, ההיטל המספרי מתחיל ממחר; חודשים עתידיים אינם כפופים לכלל זה. יתרה שלילית מוצגת כפי שהיא (סימון בלבד, ללא חסימה); מדדים לא-ניתנים-לחישוב מחזירים `null` (למשל אין הכנסה עתידית מוגדרת), לעולם לא 0 מומצא. ראו CURRENT_STATUS.md, סעיף "Version 1.3" (כולל Phase 2E — עוגן יתרה), לפירוט המלא כולל כל ההחלטות שאושרו.

## החלטות ארכיטקטורה קיימות
- **עדכון (03/09/2026, Milestones 8–9, אושר במפורש)**: העיקרון עצמו לא השתנה — פתיחה ישירה **ללא שרת/build** — אך המימוש עבר מקובץ HTML יחיד ל-3 קבצים (`index.html`/`styles.css`/`app.js`) עם נתיבי-הפניה **יחסיים** שנבחרו במפורש כדי לשמר את יכולת ה-`file://` (ראו CURRENT_STATUS.md, סעיף "Milestones 8–9", כולל תיעוד תיקון נתיב מוחלט שנוסה ונכשל תחת `file://`). "קובץ יחיד" כשלעצמו **אינו** עוד תיאור מדויק של המבנה — "ללא שרת/build/framework" כן נשאר תקף במלואו.
- **קובץ יחיד במכוון (היסטורי, לפני 03/09/2026)** — כדי לאפשר פתיחה ישירה ללא שרת/build. זו החלטה מוצהרת ב-README.
- **ES5-style JavaScript** (var, function) — לא ברור אם זו החלטה מכוונת (תאימות) או פשוט הסגנון שנבחר. **דורש אימות** אם יש כוונה לשמר תאימות לדפדפנים ישנים.
- **localStorage בלבד, ללא backend** — מוצהר ב-README כהחלטה מכוונת, עם "רעיונות לשיפור עתידי" שכוללים סנכרון ענן.
- **גרפים ב-SVG ידני** ולא בספריית chart — כנראה כדי לשמור על "ללא תלות בספריות חיצוניות".

## דברים שאסור לשנות בלי אישור
- **אין לשנות קוד קיים** בשלב זה (הנחיית המשתמש בשיחה הנוכחית).
- מבנה מפתחות ה-localStorage (`family_finance_data`, `family_finance_cat_config`) — שינוי יגרום לאובדן נתונים קיימים של משתמשים.
- העיקרון של "ללא build/שרת" (פתיחה ישירה, כולל `file://`) — שינוי ארכיטקטוני מהותי שדורש אישור מפורש. **"קובץ יחיד" באופן ספציפי כבר אינו רלוונטי** מאז Milestones 8–9 (03/09/2026, אושר במפורש) — המבנה כעת 3 קבצים (`index.html`/`styles.css`/`app.js`), אך העיקרון הרחב יותר (ללא שרת/build/framework) נשמר.
- מבנה השדות באובייקטי `items` — קוד תצוגה, עריכה ותחזית תלויים בשמות השדות המדויקים.
- **"הקפאת לוגיקה עסקית" לעיצוב מחדש (אושרה 29/07/2026, קוימה לאורך כל הרצף עד ל-Cutover ב-31/07/2026)**: בבניית ה-UI החדש (Cockpit) ובמעבר ל-`index.html` — הלוגיקה העסקית, מבנה הנתונים, וכללי בנק/כרטיס אשראי/תזרים **לא השתנו כלל** (אומת שוב בשלב 4.8). פונקציות החישוב (`renderAll`/`computeForecast`/`parseDatesAndGetLeft` וכו') הועתקו כלשונן; שכבת "מנוע הסיפור" תוספתית-בלבד וקוראת-בלבד. אין שדה "יום בחודש" ל-`fixed` — נשאר רעיון עתידי, לא יושם.
- **ללא backend/DB/Supabase/התחברות** בשלב זה — נאסר במפורש, כולל בהקשר לרעיונות מ-Design/ (צ'אט AI, חיבור בנק) שדורשים זאת.
